# 打包字体目录（可选）
#
# 将开源许可的楷体子集放入此目录（.ttf / .otf / .ttc 均可），
# 例如：FZXingKai.ttf（方正新楷简体子集，需自备授权字体文件）。
#
# 运行时 ui/theme.py 的 register_fonts() 会自动注册此目录内的字体，
# 并让全局 KAI 指向它，作为缺省楷体（KaiTi）的回退。
# 若目录为空或无可用字体，则回退系统楷体（Windows 自带 KaiTi）。
#
# 注意：字体文件受版权保护，请仅放入你拥有合法授权的字体。
