# 宋祚 · 全量审查报告（2026-09-18）

> **审查方式**：10 路并行深度审查（结算经济 / 状态存档命令 / AI 管线 / 写状态通道与机制 / 后端安全与 Rust 对拍 / 前端 / 内容与文档 / 测试质量 / 启动打包 / 端到端可玩性）+ 主审独立复核。
> **证据标准**：所有结论均带 `文件:行`；关键结论附**实跑复现**。全部脚本落 `_dev_tools/_scratch/audit-2026/`，**未修改 `game/` 与 `tests/` 下任何源文件**。
> **解释器**：`D:\codebuddy\.audit-venv\Scripts\python.exe`（Python 3.13.14，装齐 `requirements.txt` + pytest + numpy + ruff）。运行前须 `PYTHONPATH=''` 并清 `CODEBUDDY_*` 变量（否则 sitecustomize shim 造成大量假失败）。

---

## 一、总体结论

| 维度 | 状态 |
|---|---|
| 自动化测试 | ❌ **379 passed / 11 failed**（非文档声称的"全绿"；390 项） |
| 核心写状态通道 | ❌ **P0：`state_applier` 悬空引用，赈济工具 100% 失效** |
| 经济守恒 | ❌ **2 个 P0 级守恒破口**（粮食销毁、穿底造币） |
| 长局稳定性 | ⚠️ 不崩、无 NaN、人口总账对齐；但 5 年内民心/皇威确定性归零 |
| 安全 | ❌ 运行中的 Electron 产物**未重编译**，R2-4 安全修复从未生效；快照存在漏脱敏项 |
| 文档-代码一致性 | ❌ 24 项确认漂移；文档常量门禁脚本自身跑不起来 |
| 可分发/可启动 | ❌ 干净机器上 `start.bat` 必然失败；`npm run dist` 无法独立完成 |
| 版本控制 | ❌ **仓库无 `.git`**，无法 bisect/回归定位（README 引用的提交号无法复核） |
| 工程门禁 | ❌ 零 CI、无 `conftest.py`、覆盖率 55%、前端 0 测试 |

**一句话**：**测试套件是红的、AI 改状态主线是断的、跑起来的前端是三天前的旧产物、干净机器启动不了**——而 README 与机制文档仍在描述一个已经不存在（或从未生效）的理想架构。

---

## 二、P0 清单（去重后 9 条）

> 13 条原始 P0 中有 4 条为同一根因（`_resolve_path_value`），已合并。

### P0-1 · `_resolve_path_value` 悬空引用 → 赈济工具 100% 失效
- **位置**：`engine/state_applier.py:292`（`def` 被上一行注释吞掉）、`:710`（致命调用）、`:677`（潜伏同源）、`:855`（管道入口）
- **根因**：一次"删除死代码 `_cascade_faction_power`"的编辑把注释与 `def` 挤到同一行 → `def` 消失，原函数体（293–325）因缩进与 `_cascade_farmer_wealth` 的 `return []`（:283）同层，被解析为**不可达残骸**。AST 实证 `_cascade_farmer_wealth` 的 `end_lineno=325`，模块无 `_resolve_path_value`。文件仍能 `ast.parse` 与 import，**只有走到经济写路径才炸**。
- **影响面（已精确界定，纠正分报告的过宽表述）**：`applier_pipeline` 全库**只有 1 个生产调用点**——`ai/client_utils.py:793-794` 的 `relief_grant`。`core/free_effect.py` 有**自己独立的守恒层**（`_money_pools`/`_grain_pools`/`_money_feasible`），不经过 `state_applier`；正常回合走 `_economy_ai` 槽位 + 12 步结算。**故 P0 破坏的是赈济这一条工具链，不是"全部 AI 经济路径"**；但 `state_applier` 自述为"AI changes 唯一应用层入口"（:10-15）与本调用图不符，属独立的架构债（见 P1）。
- **实测**：`applier_pipeline` 处理最普通的成对守恒变更 → `NameError`；`changes` **整批丢弃且 `rejected` 为空**；`advance_and_settle` 不中断、不回滚整月（异常被 `client_utils.py:810` 就地吞掉）；混合批（豁免字段+钱粮 `add`）连合法字段一并作废。
- **修复验证（主审）**：运行时补回该函数后，4 个受影响用例**全部转绿**（仅剩刻意删除 cascade 规则导致的 1 个过期用例）。
- **复现**：`_scratch/audit-2026/_repro_p0_and_treasury.py`；补丁验证 `_scratch/audit-2026/audit_patch.py`
- **修复**：把 `:292` 拆为两行（注释单独成行、`def` 顶格），删除 `_cascade_farmer_wealth` 内 30 行残骸；并决定 `factions.*.power` 级联规则是"恢复"还是"正式废弃（连带删 `test_cascade_faction_power`）"。
- 来源：C-1 / D-1 / H-1 / J-1（4 路独立确认）

### P0-2 · 测试写真实玩家存档目录（数据破坏风险）
- **位置**：测试套件缺 `conftest.py`，`SAVE_DIR` 未重定向；`core/save_load.py` 清理只删 `.json` 不删 `.db`
- **实测（主审复核）**：`C:\Users\william\Documents\宋祚\saves` 现存 `slot_1/2/4/5/6/9.db`、`slot_0_dialogue.db(-shm/-wal)`，时间戳 **20:53–21:00**（正落在 pytest 窗口内），且 **`.json` 全无、`.db` 全留**——与"清理不对称"特征完全一致。
- **影响**：任何人跑一次全量 pytest 就可能**覆盖玩家真实存档**；且残留 `.db` 使 `save_slots` 显示为可用槽位。
- **注**：该目录同时有他人启动的 `backend.server` 实例在写（`-wal` 于 21:00 后仍在增长），故**写入归属不能 100% 归因于 pytest**；但 `.json/.db` 残留模式与 H-2 描述吻合，风险成立。
- **修复**：优先落 `conftest.py`：统一 `sys.path` + autouse 把 `SAVE_DIR` 重定向到 tmp + 复位 `CHANGE_LOG`（一次同时解决 H-2/H-4/H-12）。
- 来源：H-2（主审复核）

### P0-3 · 运行中的 Electron 产物过期 → R2-4 安全修复从未生效
- **位置**：`frontend/src/main/index.ts`（源码，mtime **2026-09-18 19:53**，9023 B） vs `frontend/out/main/index.js`（**实际运行产物**，mtime **2026-09-15 11:30**，5615 B）
- **实测（主审复核）**：
  - 源码已修：`:185 sandbox: true`、`:153 isSafeExternalUrl`、`:162 openExternalIfSafe`、`:197 will-navigate` 白名单。
  - 产物仍旧：`:122 sandbox: false`、`:127 electron.shell.openExternal(url)`（任意协议、无白名单）。
- **影响**：**"源码已修、产物未编译"**——只静态读 `src/` 会误判 R2-4 已修复。实际运行的桌面端仍是沙箱关闭 + 任意协议外联。
- **复现**：`Get-Item src\main\index.ts,out\main\index.js` + `Select-String out\main\index.js -Pattern "sandbox|openExternal"`
- **修复**：重编译前端产物；并把"产物新鲜度"纳入发布检查。
- 来源：F-1

### P0-4 · 干净机器上启动器必然失败
- **位置**：`start.bat:15,32-33,36-37,42,68-73`
- **根因**：`VENV_PY = game\.venv\Scripts\python.exe` **从不生成**（全库仅 start.bat 引用，且 `.gitignore:26` 忽略），回落到裸 `python`——本机 PATH 上**没有 python**。
- **实测**：复刻该流程 → `errorlevel=9009` → `:InstallDeps` → pip 9009 → `:DepsFail` → **Electron 永不启动**；提示还误导为"依赖安装失败（网络或代理？）"。
- **影响**：分发版首次运行即失败，且给出错误归因。
- **复现**：`_scratch/audit-2026/repro_start_python.bat`
- 来源：I-1

### P0-5 · 士绅囤粮超硬上限强制出清 → 粮食凭空销毁
- **位置**：`core/settlement_steps.py:2033-2042`
- **证据**：超硬上限出清把 **100% 余量**从账上抹去、只按 3% 记损耗 → 行级归因（`sys.settrace`）**24 月凭空销毁 2.65 亿石**；77 月 `_settle_civilian_hoard:2036` Δ粮 = **−12.95 亿石**。
- **复现**：`run_line_trace.py _settle_civilian_hoard all`、`run_months.py 120`
- 来源：A-1

### P0-6 · 国库穿底后支出仍全额发民间 → 新造货币 == 新增亏空
- **位置**：`core/settlement_steps.py:2928-2963,3003-3015`
- **证据**：60 月实测 `Δmoney=+39,318,557` vs `Δ亏空=+39,321,197`（**1:1**）——国库无力支付时仍向 POP 全额发放，凭空造币，且被 `deficit_depth` 掩盖成"亏空"。
- **复现**：`run_line_trace.py _settle_finance all`、`run_months.py 120`
- 来源：A-2

### P0-7 · 叙事与数值之间无一致性护栏（欺骗性成功）
- **位置**：`core/commands.py:595`（`event_narrative` 唯一生产调用**不传 state**）+ `ai/client.py:1404-1421`
- **证据**：同一失败输入——"说谎"脚本产出「臣已发两浙路仓廪赈济…民心稍纾」而**国帑/民心分文未动**（seed1/2/3 共 14 次命中）；"诚实"脚本则正确报告未能落地。同时 `minister_memory` 记 `['赈 两浙路']`、图谱加 `promises` 边 = **记忆记为已办**。程序侧无任何拦截，完全取决于模型是否配合。
- **影响**：玩家无法区分"办了"与"说办了"，且"记了"。
- 来源：J-2（并见 C-2：叙事护栏生产零覆盖）

### P0-8 · Rust 平行后端 8 项财政常量漂移
- **位置**：`songzuo_server/src/constants.rs` vs `game/content/data.py`
- **漂移**：`WINE_COIN_BASE` 60万↔10万（**6×**）、`TAX_COLOR_RATE` 0.40↔1.0（**2.5×**）、`MONTHLY_EXP_CIVIL_BASE` 88万↔130万、`CHANGPING_LOW` 0.9↔0.6、`CANAL_LOSS_BASE` 0.06↔0.04、`SPARROW_RAT` 0.012↔0.01、`SALT_CAPACITY_BASE` 1.85e8斤↔231、`SALT_POP_BASE` 8000万口↔5065万；Rust `default_prefectures()` 仍 12 路 Σ户 9990 万（Python 已改 20 路 2000 万户/8000 万口）。
- **影响**：README 声称"根治远程后端常量漂移"，实际只覆盖 Python 薄壳；切 Rust 后端 = 另一套经济。
- 来源：G-1 / E-13

### P0-9 · 文档常量门禁脚本自身跑不起来且覆盖不足
- **位置**：`_dev_tools/game-docs/analysis/check_docs_constants.py`
- **实测**：按文档方式运行 → `ModuleNotFoundError`（`sys.path` 指到 game-docs）exit 1；加 `PYTHONPATH` 后报 4 处漂移 exit 1；**只覆盖 12 个常量**（含 2 个重复键），本轮 11 项新漂移它一项都没覆盖。
- **影响**：唯一的文档-代码一致性防线是失效的。
- 来源：G-2

---

## 三、P1 清单（按主题，去重后约 30 项）

### A. 守恒与经济（A-3~A-8、D-4、D-6、D-10、D-11）
| 编号 | 位置 | 一句话 |
|---|---|---|
| A-3 | `settlement_steps.py:752-755` | 流民 5% 上限被**双向**钳制 → 超额流民静默灭失（77 月 138,718 人） |
| A-4 | `settlement_steps.py:2153-2156` | 畜栏产肉 `imperial_treasury +=` **无买方** → 每月凭空造 40,000 贯 |
| A-5 | `settlement_steps.py:2150-2152` | 酒坊每月把常量**永久累加**进月度酒课率、无上限（60万→370万贯/月，内帑 100万→1.52亿） |
| A-6 | `settlement_steps.py:1998-2018` | `p["grain"]` 兼作年产与现货 → 囤粮按现货扣年产，**永久削减税基** |
| A-7 | `settlement_steps.py:1769-1784` | 农卖方权重保底 0.5 **覆盖**"不超盈余"约束 → 超卖 24 倍，蚕食已修的存粮安全垫 |
| A-8 | `settlement_steps.py:2085-2110` | 工程 `defense_add` 直接造兵、次月兵 POP 重聚合**凭空造人**（与已修 `arm_strength` 同类未同步） |
| D-4 | `core/registries.py:295-303` | 粮饷 `// len(paths)` **丢余数** → 实测国库扣 17 贯仅 5 贯入库，**12 贯销毁** |
| D-6 | `core/focus_mechanic.py:477-479` | 政务国策无上限累加 `waste_reform.savings`，财政步按**全量**冲减（`MONTHLY_EXP_CIVIL_BASE=880000` 无 0 下限）→ 约 59 月后常费转负，**国库凭空进钱** |
| D-10 | `core/free_effect.py:93-101,225-230` | 太仓粮先扣后判池 → 无粮池时凭空销毁 |
| D-11 | `core/estate_mechanic.py:77-80,99-100` | 家产四账不闭合：窖藏 `hoard` 无对手账户（实测 Σ=−4796） |

### B. 事件与回合（D-3、D-12、A-13、A-14、J-4、J-5、J-13、B-13）
| 编号 | 位置 | 一句话 |
|---|---|---|
| D-3 / J-4 | `core/commands.py:602-607` | 清除谓词用 `title in e["message"]`，而 message 存的是 **desc** → **已处理事件永不清除**；`title=="事件"` 时又误清压力事件。历史"全清"bug 只换了形态，未真修好 |
| D-12 / J-4 | `core/commands.py:606` | `active_events` **无任何上限**（60 回合 58 条；主审 240 月实测累积 34 条且不清） |
| J-5 | — | 「库藏空虚」危机因上条只触发一次 |
| A-13 | `settlement_steps.py:63,95-108` | `active_decrees` 月末被**整表替换** → `duration=12` 的政令次月即消失 |
| A-14 / B-13 | `settlement.py:341-345` | 终局分支早退**不推进回合** → 重复调用永久空转（实测第 78 月起 43 次无进展） |
| J-13 | `core/commands.py` | `advance_month` 事件永不入 `event_history`（58 vs 0） |
| D-5 | `core/era_mechanic.py:79-81` | `era_state.` 在白名单内却被每月整体重算抹平 → **死写入通道**（`era_migrate` 同死） |

> **主审补充（事件通道）**：压力驱动的 `_settle_events → _trigger_event`（`settlement_steps.py:2462`）造出的事件**无 `title`/`choices`/`desc`/`effects`**——实测 12 个类别全部只产出一行 `[事件] X 爆发！`，**对状态零影响**，且因无 `title` 无法被 `/api/resolve_event` 按 title 反查（404）。同时阈值 80 而压力仅 +1/月 → **60 月内一次都不触发**（主审 60/240 月回放 `active_events` 长期为 0 或仅累积该通道产物）。

### C. AI 管线（C-2~C-4、C-13、C-17、E-4）
| 编号 | 位置 | 一句话 |
|---|---|---|
| C-2 | `ai/client.py:1404-1421` | 叙事护栏（数值区间/人物查表/来源闭集）**生产零覆盖**（唯一调用不传 state）→ 2026-09"人物护栏回喂"修复**实际从未发生** |
| C-3 | `ai/client.py:928-941` | 复读检测阈值 0.6 + `_prev_texts` **全局跨契约共享** → 正常新叙事被判复读换模板（实测差异度 0.63~0.80），模板还写进召对缓存与对话记忆库 8 回合 |
| C-4 | `ai/client_utils.py:187-198` | 安全词表误伤战争题材（屠城/凌迟/砍头）→ 命中即整单丢弃改模板，且**无 `_error`**，静默降级 |
| C-13 | `ai/client.py:62-76` | 脱敏失败回退**原始摘要 JSON**（fail-open）→ 精确国库/派系/兵力全量进 prompt（潜伏） |
| C-17 | `ai/narrative_guard.py:22,29-44` | 数值护栏单位闭集不全、不支持「亿」、不容空格 → 十万亩/十万两/一亿口 全部绕过（实测 10 例） |
| E-4 | `ai/client.py:28-30,388,401` | `fetch_models` 的 `_http_get_json` **未在导入清单**（定义在 `client_utils.py:65`）→ NameError 被 `except Exception` 吞 → **恒返回硬编码模型表 + `ok:true`**，R2-2 的 SSRF 加固成永不执行的死代码（主审 ruff F821 独立发现） |

### D. 后端与安全（E-1、E-2、E-3、E-5、E-6、E-7、E-8、E-9、E-10、E-11、E-12、E-14）
| 编号 | 位置 | 一句话 |
|---|---|---|
| E-3 | `backend/server.py:66-102` | 回环判定吃 uvicorn 已信任的 `X-Forwarded-For` → 同机反代下 `XFF:127.0.0.1` 冒充本机、**未配 token 即全放行**（实测）。与 E-18 串联在"同机反代+未设 token"部署下应升级为 **P0** |
| E-5 | `backend/server.py:454-457` | `/api/readouts` 直出 `finance.corruption_ded` **贪腐真值 14706.6**（注释自称"隐性不进UI"） |
| E-6 | `backend/server.py:192-231` | 快照**漏脱敏**：`pops.*.wealth`（士绅 12,388,198）、`spy_network` 原样下发（loyalty/corruption/estate 已正确档位化） |
| E-7 / C-8 | `backend/server.py:201-225` | `_ai_failures → ai_failures` 分支写在 `startswith("_")` 过滤**之后** → 不可达，**AI 失败披露从未生效** |
| E-8 | `songzuo_server/src/state.rs:86,301,335,337` | Rust 直序列化 GameState → loyalty/corruption **真值**、secret_loyalty、spy_network 明文下发 |
| E-9 | `songzuo_server/src/server.rs:9-19` | Rust 仅 6 端点 vs 前端实调 19 个 → 切 Rust 后 **14 个 404** |
| E-10 | `main.rs:27-33` + `server.rs:164-179` | Rust 默认 `0.0.0.0` + `allow_origin(Any)` → 任意网页可驱动其 `/api/*` |
| E-1 | `backend/server.py:398-404` | `/api/load` 失败先赋值 `None` 清空全局会话 → 之后全端点 409（实测） |
| E-2 / I-2 | `backend/server.py:326-345` | 未配 AI 时 `/api/advance` 抛未捕获 `AIRuntimeError` → **500 裸 `Internal Server Error`**，无 `AI_NOT_CONFIGURED`，且与自身 docstring「本地降级」矛盾 |
| E-11/E-12/E-14 | `settle.rs`/`save.rs` | Rust 折色双全额计账；俸禄被 1,000,000 贯旧盘子顶替；**存档实际不互通**却宣称可直接互读（实测 granary 差 1e4、`finance_readout` AttributeError） |

### E. 存档与命令时序（B-1、B-2、J-7、J-10）
| 编号 | 位置 | 一句话 |
|---|---|---|
| B-1 / J-10 | `save_load.py:258-260` + `commands.py:507-513` | 读档时 `memory.turn` 先按存档对齐又被 SQLite 覆盖（记忆库每回合落盘、主存档只在手动/正月更新）→ **读旧档后 AI "记得"未来**（实测 state.turn=0 / memory.turn=20） |
| B-2 | `save_load.py:245` | `int(schema_version)` 在 try 之外 → 非整数直接抛 ValueError 穿透读档（`/api/load` 500），且损坏档不备份 |
| J-7 | `commands.py:517-522` | 自动存档失败裸 `except: pass` → **虚假"已存盘"** |
| B-11 | `commands.py:430,471` | 每回合 **2 次**全量深拷贝快照（内外层完全冗余），15–51ms/次 |

### F. 前端（F-2、F-3、F-4、F-6）
| 编号 | 位置 | 一句话 |
|---|---|---|
| F-2 | `electron-builder.yml:6-8` | `files` 白名单**未含 `out/renderer/**`** → 打包后 13.3MB 舆图数据与全部立绘/背景不进 asar，**成品不可玩**（静态判定，本机无 Node 未能实跑打包闭环） |
| F-3 | `MapView.tsx` | 状态同步 effect 在三个 ref 赋值**前**就早退，依赖数组只有 `[state]` → 开局/读档后舆图不反映真实状态 |
| F-4 | 事件面板 | 「吉/凶」标注 **36%（30/83）方向判错**：7 条不认后端权威前缀式减益档位 `"-大"`，23 条只数条数不看量级（treasury ±300 万贯 与 prestige ±10 同级）→ 最严重处把**净亏 300 万贯**的选项高亮成绿色「吉」 |
| F-6 | `api/client.ts` | 后端设 `SONGZUO_SERVER_TOKEN` 后前端所有 `/api/*` **恒 401**（不发 `Authorization`，全库 0 命中） |

### G. 工程与流程（H-3、H-4、H-5、H-6、H-7、H-8、H-9、G-3）
| 编号 | 一句话 |
|---|---|
| H-3 | **B3 财政语义反转**（`TREASURY_CRISIS_LINE` 由 −500 万国库下界 → +500 万亏空深度）后，测试与 `游戏机制说明.md:476` 未同步 → **5 项测试红的唯一根因** |
| H-5 | `backend/server.py` **469 语句 0% 覆盖**；19 端点 + 鉴权零测试；`/docs`/`/openapi.json` 未鉴权；`httpx` 不在任何 requirements |
| H-6 | `ai/client.py` 29%（1510 语句，最大缺口）；真实 HTTP 传输 `_http_post_json` 全被 monkeypatch |
| H-7 | `FakeAIClient` 缺 6/9 agent 方法 → 所谓"AI 结算测试"**实为 AI 全失败降级测试** |
| H-8 | 守恒断言容差 **5000 万** vs 实测单月最大 1226 万 → 俸禄黑洞、市舶双计全被静默放过 |
| H-9 | 前端 10,864 行 TS/TSX **零测试**，无框架、无 `test` script |
| H-4 | 零 CI、无 `conftest.py`、逐文件 `sys.path` hack；`test_state_applier.py:5` 路径算错 → `pytest.exe` 单跑 collect error（全量跑因收集顺序侥幸通过） |
| G-3 | 常支文档 55 万 vs `MONTHLY_EXP_CIVIL_BASE=880_000`（2026-08-31 遗留，**确认未修**） |

---

## 四、测试基线：379 / 11 的归因

11 项失败**不是 11 个独立缺陷，而是 3 个根因簇**（H 路结论，主审认同）：

| 簇 | 根因 | 失败项数 | 应修何处 |
|---|---|---|---|
| **A** | `_resolve_path_value` 悬空（P0-1） | 4（含生产表现 `relief_region_resolve_and_refuse`） | **源码** |
| **B** | B3 财政语义反转后测试未同步 | 5 | 测试（+机制文档） |
| **C** | B1 删除市舶关税双计通道后测试未同步 | 1 | 测试 |
| **D** | cascade 规则被有意删除、测试未同步 | 1 | 测试或契约决策 |

**第 5 项（`test_treasury_stays_above_crisis_line`）的复核判定**：**测试阈值不合理，不是结构性财政赤字回归**。用户口径亦确认"国库开局下滑是游戏设定"。五条依据：① 量纲错配（拿 `treasury` 比"亏空深度"阈值，三个生产消费方全用 `deficit_depth()`）；② `TREASURY_START == TREASURY_CRISIS_LINE == 5,000,000`，`>` 断言开局第 0 步即为假；③ 10 seed × 36 月真实危机判据 **0/10** 触发而断言 **10/10** 失败；④ 120 月长程国库**回升**、亏空深度恒 0；⑤ 08-31 加锁时"全绿"诚实、B3 落地后恒假且无人重跑。

---

## 五、历史修复核实（哪些真修好、哪些没有）

**✅ 确认真修好**：`state_applier` 原子回滚 / 0-100 区间与负值拒绝 / 守恒补记账只补未配对项（酒课 60:40 任意枚举顺序精确 ΣΔ=0）/ 守恒组禁 `set|mul|remove` / 通配展开先于守恒校验 / `free_effect` 净额合计校验 / `free_effect` 暂缓月不递减 duration / `focus` 欠费月停摆 / 士绅收租改守恒转移 / 装备侧 Σ(数量×价)≈eq_total / 人口总账每步对齐（12 与 240 月全程 0 偏差）/ 起义史实时间闸 / 农存粮安全垫（但见 A-7 可被权重旁路）/ 俸禄指数化守恒 / R2-3 鉴权（设 token 后**回环来源也** 401）/ R2-17 状态判空在锁内 / 舆图构建链离线**字节级幂等** / 前端 `ActionName` 23 项与后端 handlers 集合层面全匹配 / 前端 4 个导出物逐字节新鲜 / 州层 Σpopulation 与全局锚 7% 差已归零。

**⚠️ 未真修好（换了形态）**：`resolve_event` 的"清空全部在场事件"→ 现退化为"**永不清除** + 特定标题误清"（D-3/J-4）；`active_events` 无上限（D-12）；文档常量漂移（G-3）。

**❌ 修复动作本身造成回归**：删除死代码 `_cascade_faction_power` 时**把活函数 `_resolve_path_value` 一并注释掉**（P0-1）——且删除后仍未同步 `test_cascade_faction_power`。

---

## 六、已核实为**误报 / 非缺陷**（避免后续重复上报）

| 项 | 结论 |
|---|---|
| ruff **S608** SQL 注入告警（`memory_graph.py:207,373,388`、`dialogue_memory.py:100`） | **误报**。表名/列名全为硬编码常量，一切取值走 `?` 占位符 |
| 流民账在第 103 月由 1,700,200 **一次性归零** | **非缺陷**。ΣPOP +1,596,316（其中农 +1,549,751）、账差 0、`population` 同步对齐 → 是赈济安置归农，守恒成立 |
| 国库开局下滑 | **设计**（用户明确口径），不作为问题 |
| ruff **B023** 闭包晚绑定（`build_map_geo.py:139`、`settlement_steps.py:708`） | **非缺陷**。闭包均在同一迭代内立即调用 |
| ruff **F811** 函数内重复导入（`game_state.py`、`game_state_econ.py` 等 19 处） | **非缺陷**。项目约定的"函数内延迟导入" |
| `test_cascade_faction_power` 失败 | **过期测试**（规则已被有意删除），非产品缺陷 |
| `frameowrk` 布尔恒真类弱断言（H-10） | 测试质量问题，非产品缺陷 |

---

## 七、长局与平衡观察（主审实测）

**60 月 / 240 月确定性回放**（`_replay_60m.py` / `_replay_seeds.py` / `_replay_240m.py`）：

| 检查项 | 结果 |
|---|---|
| 起义史实时间闸（1101-1103 无方腊/宋江） | ✅ 通过 |
| 农存粮安全垫 | ✅ 通过（60 月最低 3,905 万石） |
| 人口总账 \|ΣPOP+流民 − population\| | ✅ 全程 0 |
| NaN / Inf / 负值 | ✅ 0 条 |
| 米价 | ✅ 稳定 1.03–1.62（240 月 +40%，温和） |
| **民心** | ❌ 3/5 种子在第 **47–49 月归零** |
| **皇威** | ❌ **5/5 种子都在第 55 月归零**（确定性，主因党争 −1/月） |
| 流民 | ⚠️ 无上限增长至 59–169 万（120 月后长期 0，因安置渠道） |
| `active_events` | ⚠️ 只增不减（240 月累积 34 条） |
| 国库 | ⚠️ 60 月 175 万 → 240 月 6,735 万（无界增长；无玩家动作场景） |

**判定**：长局**不崩、不脏档**，但**民心/皇威会确定性归零且无硬终局**（`game_over` 240 月仍为 `False`）。这与 2026-08-31 报告"残留观察项"一致（当时只提皇威），但**民心归零是新增观察**。是否属设计需产品确认——**建议不作为缺陷上报，仅记录**。

---

## 八、端到端冒烟

| 端点 / 步骤 | 结果 |
|---|---|
| `/health`、`/api/new_game`、`/api/readouts`、`/api/save_slots`、`/api/meter`、`/api/ai_config` | ✅ 200 |
| `/api/monthly_report` | ✅ 200，返回**显式标注的本地兜底模板**（非伪造 AI 结果） |
| token 矩阵（9×`/api/*` 无 token / 错 token / 对 token、`/health` 免鉴权、`file://` base_url） | ✅ 16/16 符合预期 |
| 脱敏实测 | ✅ `loyalty`/`corruption`/`minister_estate` 全为档位词、无 `secret_loyalty`（但见 E-5/E-6 漏项） |
| **`/api/advance`（无 ai_config.json）** | ❌ **500 裸 `Internal Server Error`** |
| 回合推进（注入 AI 后，4 组 24 回合 + 1 组 60 回合） | ✅ 0 异常、不脏档 |
| 存档往返 | ✅ 全字段一致；唯一不一致 `mem_entities 109→110`（记忆库 `_slot` 粘性） |
| 缺字段旧档 / 损坏档 / 超高 schema / 不存在槽位 | ✅ 优雅失败不崩（损坏档自动 `.corrupt` 备份） |

---

## 九、工程与流程缺口

1. **零 CI**：全仓无任何 workflow / `conftest.py`；测试靠"记得清 `PYTHONPATH`"这种脆弱前置。
2. **无版本控制**：`D:\codebuddy\sz` **没有 `.git`**（`sz` 与父目录均非 git 仓库），README 引用的提交号（如 `5ca8128`）无法复核，**无法 bisect 定位回归**——这正是本次"一次误删活函数、无人发现"的制度性原因。
3. **静默失败面巨大**：75 个 Python 文件中 **292 处 `except Exception`，其中 126 处直接 `pass`**；J 路 AST 统计 **218 处 handler 无玩家可见错误信号**（pass 133 / return 67 / continue 10 / log_only 8）。**本次几乎所有 P0 都是被这些 `pass` 掩盖成"看起来正常"。**
4. **覆盖率 55%**（11803 语句 / 5261 miss）：零覆盖 6 模块（`backend/server.py` 469、`backend/client.py` 222、`ai/semantic.py` 117、`ai/vector_store.py` 103、`ai/model_setup.py` 32、`core/external_view.py` 29）；`engine/state_applier.py` 62% 但**最核心 4 段（守恒预检、原子回滚、重校验、结果组装）全在 missing 里**。
5. **文档-代码漂移 24 项**：机制文档 19 项不一致、历史分析文档 5 项、Rust 对拍 8 项；**重复定义 18 项**（其中 6 项已实际漂移）。
6. **目录纪律**：`game/ui/__pycache__/*.pyc` 残留（README 称 `ui/` 已整体移除，且 `SongZuo.spec:17` 仍把 `ui` 打进分发）；`game/assets/map/web/*`（~890KB 已删预览链残留）；`game/.coverage` 落进本体且未被 `.gitignore` 覆盖；`content/ministers/layers/_src/` 的开发脚本 + ~30MB 源图在 game/ 本体内。
7. **凭据泄露**：`game/content/ministers/layers/_src/_i2i.py:5` **硬编码第三方 API Key**（`sk-8Rz…`），而 `SongZuo.spec:19` 把整个 `content/` 打进安装包 → **密钥随每个分发版出厂**；同类密钥另见 `_dev_tools/_scratch/agnes_*.py`（3 处）。`.gitignore` 只忽略 `*.agnes_key`/`.env`，未覆盖该文件。（本轮**未验证该密钥是否仍有效**，也未用于任何请求。）
8. **合规**：全仓**无项目 LICENSE**；`THIRD_PARTY_NOTICES.md` 只覆盖前端 9 项，**PyInstaller 打包进分发的 Python 依赖（requests/rich/Pillow/shapely/fastapi/uvicorn）零声明**；`ai/SensitiveWords.SOURCE.md` 宣称词表以 **MIT** 发布，而其所称来源 LDNOOBW 的许可证需另行核实（[疑似]，建议法务确认）。

---

## 十、修复优先级建议

| 优先级 | 动作 | 预计成本 |
|---|---|---|
| **P0-立刻** | ① 修 `state_applier.py:292`（拆行恢复 `def`，删残骸）② 落 `conftest.py` 重定向 `SAVE_DIR`（**在任何人再跑全量测试之前**）③ 重编译前端产物 | 各 <30 分钟 |
| **P0-尽快** | ④ 移除/轮换泄露的 API Key，并把 `content/**/_src/` 移出游戏本体与打包清单 ⑤ 修 `start.bat` 的 `.venv` 自举或改为可用的解释器探测 ⑥ `/api/advance` 接住 `AIRuntimeError` 并返回 `AI_NOT_CONFIGURED` | 0.5–1 天 |
| **P1** | ⑦ 补 A-1/A-2/D-4/D-6 的守恒缺口（先定语义再改码）⑧ 修 `resolve_event` 清除谓词 + 给 `active_events` 设上限 ⑨ 裁定 `era_state`/`focus` 死写入通道的语义 ⑩ 补齐快照脱敏（E-5/E-6）与回环判定（E-3） | 3–5 天 |
| **P1-工程** | ⑪ 加最小 CI（pytest + `ruff --select F821,F601` + 导出物新鲜度）——**`ruff F821` 本可在引入当天就拦住本次 P0** ⑫ 恢复 `check_docs_constants.py` 并扩到全量常量 ⑬ 同步 5+1 项过期测试与机制文档 | 1–2 天 |
| **P2** | 按各分报告清单排期（文档漂移 24 项、Rust 对拍 8 项、前端 F-2/F-3/F-4、覆盖率补测 14 条） | 按季度 |

> **最低成本的高杠杆动作**：**接入 `ruff --select F821,F601`（未定义名 + 重复字典键）到提交门禁**。它零成本、零误报（本次 7 条 F821 全部是真缺陷，2 条 F601 全部是真数据缺陷），可在引入当天拦住 P0-1、E-4，以及 `data.py:860`/`save_load.py:163` 两处重复键。

---

## 十一、分报告索引（含完整证据与可复现命令）

| 路 | 报告 | 条目 |
|---|---|---|
| A | [A-settlement.md](_scratch/audit-2026/A-settlement.md) | P0×2, P1×6, P2×12 |
| B | [B-state-save-commands.md](_scratch/audit-2026/B-state-save-commands.md) | P1×2, P2×12 |
| C | [C-ai-pipeline.md](_scratch/audit-2026/C-ai-pipeline.md) | P0×1, P1×3, P2×13, P3×1 |
| D | [D-write-channels-mechanics.md](_scratch/audit-2026/D-write-channels-mechanics.md) | P0×1, P1×6, P2×13 |
| E | [E-backend-security-rust.md](_scratch/audit-2026/E-backend-security-rust.md) | P1×14, P2×13 |
| F | [F-frontend.md](_scratch/audit-2026/F-frontend.md) | P0×1, P1×3, P2×9, P3×1 |
| G | [G-content-docs.md](_scratch/audit-2026/G-content-docs.md) | P0×2, P1×17, P2×22, P3×6 |
| H | [H-tests-quality.md](_scratch/audit-2026/H-tests-quality.md) | P0×3, P1×6, P2×8 |
| I | [I-launch-packaging-smoke.md](_scratch/audit-2026/I-launch-packaging-smoke.md) | P0×1, P1×3, P2×7 |
| J | [J-endtoend-playability.md](_scratch/audit-2026/J-endtoend-playability.md) | P0×2, P1×5, P2×6 |

主审复核脚本：[`_repro_p0_and_treasury.py`](_scratch/audit-2026/_repro_p0_and_treasury.py)、[`audit_patch.py`](_scratch/audit-2026/audit_patch.py)、[`_replay_60m.py`](_scratch/audit-2026/_replay_60m.py)、[`_replay_seeds.py`](_scratch/audit-2026/_replay_seeds.py)、[`_replay_240m.py`](_scratch/audit-2026/_replay_240m.py)、[`_probe_refugee2.py`](_scratch/audit-2026/_probe_refugee2.py)

---

## 十二、环境限制与未覆盖面（诚实声明）

- **无 Node.js**：前端 `npm run typecheck` **未执行**（`npm`/`node` 均不存在，`start.bat` 所指的 `tools\nodejs` 也不存在）；`npm run dist` / electron-builder 实际打包**未实跑**，F-2 为**静态配置判定 [确认]**。
- **无 cargo**：Rust 侧仅为**静态审查**，未编译、未运行；E-21 的"debug 构建每月 panic"为代码推理。
- **未验证泄露密钥是否有效**（未对任何第三方端点发起请求），也未尝试使用。
- **未修改任何源文件**：本报告与全部脚本仅在 `_dev_tools/_scratch/audit-2026/` 写入。审查期间测试运行在真实存档目录留下了 `.db` 残留（见 P0-2），**未自行删除**（属用户目录，需确认后再处置）。
