# 宋祚 · 修复计划（2026-09-18）

> 依据：[review_2026-09-18.md](review_2026-09-18.md)（10 路审查汇总）与 `_dev_tools/_scratch/audit-2026/` 下 10 份分报告。
> 原则：**先立安全网 → 再止血 → 再对齐 → 最后改机制**。每阶段有独立验收命令，可单独交付、单独回滚。
> 纪律：**一个批次一个可验证目标**；每批结束跑「验收三连」（`pytest` / `ruff --select F821,F601` / 定向探针），红则回滚该批。
> **硬约束**：所有改动必须遵守 **POP 挂载律**（凡"人"落 POP `size`、凡"钱粮"落 POP `wealth`/`grain` 或国库内帑、阶层变动走 POP 流动通道且 ΣPOP 守恒、禁止平行账本、每条改动配守恒断言）。全文见 [游戏机制说明.md §五](../docs/游戏机制说明.md)。

---

## 执行进度（滚动更新）

> **落地顺序（用户定稿）**：① 先修审查问题 → ② 再做 M0/M1/M2/M3 货币口径（含军俸提至史实、money sink）→ ③ 最后做官制完善。

### ✅ 已完成

| 批次 | 内容 | 验证证据 |
|---|---|---|
| **0.1** | `git init` ＋ 基线提交 `2ccf72d`（2814 文件 / 286 MB） | 工作区可回滚；`node_modules`/`target`/`_scratch` 正确排除 |
| **0.3** | 新增 `_dev_tools/game-dev/tests/conftest.py`：SAVE_DIR 重定向 ＋ sys.path 统一 ＋ 全局态复位 | ✅ 跑完整套件后真实存档目录**逐字节未变**；单文件 `test_state_applier.py` 由 **13 ERROR** → **0 ERROR** |
| **0.4** | 静态门禁 `ruff --select F821,F601` | **All checks passed**（原 9 条 → 0） |
| **1.1** | 修 `engine/state_applier.py` 被注释吞掉的 `_resolve_path_value`（P0） | AST 确认为模块级函数；4 个红用例转绿 |
| **1.2** | 按决策 1 删除过期用例 `test_cascade_faction_power` ＋ 清理未用导入 | 用例数 390 → 389 |
| **1.5** | `start.bat` 四级解释器探测（venv → `py -3` → `python` → `python3`）＋ `chcp 65001` | 干净机器复现：给出明确「未找到 Python」指引，**不再误报**依赖安装失败；有 Python 时正确选中（3.13.14） |
| **1.6** | `/api/advance` 与 `/api/action` 接住 `AIRuntimeError` → **503 ＋ 错误码**；生产者补 `code`；server docstring 勘误 | 真机冒烟：`/api/advance` → **503 `AI_NOT_CONFIGURED`**（原 500 裸错） |
| **1.7** | `ai/client.py` 补 `_http_get_json` 导入（原 NameError 被吞，`fetch_models` 恒返回硬编码表） | ruff F821 清零（属 0.4 一并修复） |
| **2.x** | 测试套件整理：同步 6 项过期断言（B3 破产语义 ×5 ＋ 市舶通道 ×1） | 见 [test_triage_2026-09-18.md](test_triage_2026-09-18.md) |
| **B-2** | ✅ **已完成**：A-5 酒课率逐月累加／A-4 畜栏产肉无买方／外销变现登记为外部注入（新增 `register_flow` 台账）／补全账户表（家产·加俸·投资本金）／行级追踪定位并闭合 D-11 家产窖藏黑洞。**60 月累计残差 +43,380,547 → −7,852 贯（收敛 99.98%）** | 见 [货币口径规范 §13.6](../docs/货币口径规范_M0M1M2.md) |
| **B-2′** | ✅ **军俸提至史实**：`BRANCH_BASE` 现金轴 ×4.66（289.5 万 → **1,349 万贯/年**）；财政改**按实付 ＋ 欠饷/欠俸备忘科目**（修 A-2 穿底造币）；各军欠饷明细落 `ArmyUnit.arrears` ＋ `/api/readouts` 下发 `equip`/`arrears`（前端可点开单军详情窗） | `deficit_depth` 全程 **0**、`game_over=False`；60 月累计欠饷 **4,992 万贯**；残差 −131 贯/月 |
| **B-3** | ✅ **已完成**：**L1 资产维持费**接入（新结算步 `_settle_upkeep`，Step 3.9）；复活死数据 `BUILDING_STD[*]["maintain"]=0.5%/月`；资产 = 工程＋POP建筑＋作坊×12万贯＋武库件数×2贯＋城防点×1万贯；国库→工匠40%/商人60% POP **精确守恒（ΔM_ALL=0）**，不足按实付记 `statistics["upkeep_arrears"]` | 开局 1,380.6 万贯资产 → **6.9 万贯/月**；新增 `test_asset_upkeep.py` **4 例**；`pytest 393/0/0`；**账本容差 H-8 结案：3,000,000 → 200**（见 §13.7） |
| **C-1/C-2** | ✅ **已完成**：新增 `core/officialdom.py`（**单一权威源**）；**消灭 `officials`/POP 双账**（旧字段降为派生镜像，唯一写入点 `sync_legacy_mirror`）；官僚 POP 身份子池（官/吏分账 ＋ 在岗/待阙/祠禄）＋ 两条不变量与自愈；俸禄按子池计价 × 磨勘指数；科举入仕落**待阙**池；**裁汰冗员修掉"人凭空消失"的 ΣPOP 违约**（现回流士绅） | 240 月：官 27,001 → 62,934（+133%）、官俸 81.0 → 152.1 万贯/月（修复前恒 +0.0%/零增长）；人口账零偏差；不变量逐月成立 |
| **C-3** | ✅ **已完成**：官制结算步 **Step 3.95**（有阙授职／待阙超限定转祠禄／每年正月致仕·磨勘·宗室／每 3 年郊祀恩荫）＋ **Step 3.96 宗室俸禄（L2c sink，内帑 → 士绅 POP）**；差遣定员 `posts_quota`；冗官 := 官额 − 定员 | 240 月：待阙被压在 13,523（上限 13,517）、祠禄承接 22,377、冗官 35,900；宗室 3,355 → 5,780；新增 `test_officialdom.py` **14 例**；`pytest 407/0/0` |
| **C-4~C-8** | ⏳ **待做**：C-4 免役税基（§13.6 最有价值的一条）、C-5 吏制（吏额脱离官×8、陋规、把持度、吏怨）、C-6 科举离散科次（每 36 月一科）、C-7 六参数入可改白名单、C-8 地方三层可见性与路级四司 | 见 [宋代官制与三冗设计.md §十八](../docs/宋代官制与三冗设计.md) |

**验收三连（当前基线）**：
```powershell
pytest            → PASSED=407  FAILED=0  ERROR=0   （整理前 379/11）
ruff F821,F601    → All checks passed                （整理前 9 条；F811 19 条为既有函数内重复导入，非本次引入，见下）
60 月回放          → 起义时间闸 ✓ 农存粮安全垫 ✓ 人口总账 0 偏差 ✓ 无 NaN/负值 ✓
240 月官制长局      → 官额 +133% ✓ 待阙有界 ✓ 祠禄承接 ✓ 人口账零偏差 ✓ 子池不变量恒成立 ✓
货币对账           → 60 月累计残差 −7,848 贯；240 月 −30,614 贯（修复前 60 月即 +43,380,547）✓
存档往返           → M0/M1full/M1working/M2/M3/M_ALL 逐项可逆 ＋ statistics 19 项全等 ＋ 官制字段(gc posts_quota/rank_index/recruit_log/clan)一致 ✓
```

> **关于 `ruff --select F811`**：全仓 19 条 `Redefinition of unused ...`，全部是**函数体内局部重复导入**（`core/game_state_econ.py`、`core/game_state.py`、`core/commands.py`、`core/registries.py` 等），属先于本次会话存在的既有写法，**非本次改动引入**（`git status` 确认本次仅动 3 个源文件，均不在 F811 名单内）。因不影响行为、且清理会无谓churn，暂不处理，仅记录。

### ⏭️ 豁免 / 受阻

| 项 | 状态 |
|---|---|
| **1.4** 泄露 API Key 移出本体与打包清单 | ⏭️ **用户明确豁免**（「Agnes 的 key 不用管」），不再处理 |
| **1.3** 运行中的 Electron 旧产物重编译 | ⏸️ **受阻**：本机无 Node.js 运行时，无法重编译。需在有 Node 的环境执行 `cd game/frontend && npm run build` |

### ⏳ 待办（按定稿顺序）

| 批次 | 内容 |
|---|---|
| **B-1** | ✅ **已完成**：`core/money.py` 货币口径（M0/M1/M2/M3）＋ 月度对账 ＋ `silver_stock`。**零行为变更**（60 月回放逐项一致）。实测 60 月累计残差 **+43,380,547 贯**，归因锁定内帑（+2.91M/月）而民间 −2.28M/月 —— 见 [货币口径规范 §十三](../docs/货币口径规范_M0M1M2.md) |

> **阶段 B 已收口**（B-1 口径观测 → B-2 守恒修复 → B-2′ 军俸史实＋欠饷 → B-3 L1 资产维持费）。
> **阶段 C 已启动并完成 C-1/C-2/C-3**（消灭双账、身份子池、官制结算步、宗室俸禄），
> **余 C-4 ~ C-8 待做**。全程符合用户定稿顺序「① 审查问题 → ② 货币口径 → ③ 官制完善」。

---

## 阶段 0 · 安全网（**动手改代码之前必须先做**）

理由：当前**跑一次全量 pytest 就会写真实玩家存档目录**（P0-2），而验收主要靠跑测试；且**仓库无 `.git`**，任何改动不可回滚、不可 bisect。

| # | 动作 | 文件 | 验收 |
|---|---|---|---|
| 0.1 | ✅ **已完成**：`git init` ＋ 基线提交 `2ccf72d`（2814 文件 / 286 MB；`node_modules/`、`songzuo_server/target`、`_scratch/` 均已正确排除；顺带放行 `_dev_tools/game-docs/`，使被 README 引为权威的机制文档入库） | 仓库根 | ✅ 已可 `git diff` / `git checkout` 回滚；工作区 0 未提交改动 |
| 0.2 | 核对玩家存档现状：`Documents\宋祚\saves` 是否已被测试污染，决定是否恢复 | 用户目录 | 用户确认存档可用；残留 `.db` 处置明确 |
| 0.3 | 新增 `_dev_tools/game-dev/tests/conftest.py`：① 统一 `sys.path` ② `autouse` 把 `SAVE_DIR` 重定向到 `tmp_path` ③ 每例复位 `CHANGE_LOG` 等全局态 | 新文件 | 改动前后 `Documents\宋祚\saves` 的 mtime **不再变化**；`pytest.exe` 单跑 `test_state_applier.py` 不再 collect error（H-4） |
| 0.4 | 接入静态门禁：`ruff --select F821,F601`（本次 7 条 F821 + 2 条 F601 **全部是真缺陷、零误报**） | CI / 提交钩子 | 门禁对当前代码应**报错**，修完 1.1、5.1 后转绿 |

**0.3 是硬前提**：不做它，后续所有批次的自测都在污染用户存档。

---

## 阶段 1 · 止血（P0，当天可完成）

| # | 问题 | 文件:行 | 改法 | 验收 |
|---|---|---|---|---|
| 1.1 | `_resolve_path_value` 被误删 | [state_applier.py:292](sz/game/engine/state_applier.py#L292) | 把 `# 已整体移除。def ...` 拆成两行（注释独立、`def` 顶格）；删除 `_cascade_farmer_wealth` 内 293–325 残骸 | 4 个红测试转绿；`_repro_p0_and_treasury.py` 输出 `treasury Δ = 100000` |
| 1.2 | cascade 契约二义 | `test_state_applier.py:60` + `state_applier.py:277-283` | **二选一（需决策 D1）**：① 恢复 `factions.*.power` 规则并允许该 path；② 正式废弃 → 删测试 + 白纸化决策 | `pytest test_state_applier.py` 全绿 |
| 1.3 | 运行中的 Electron 是旧产物 | [out/main/index.js](sz/game/frontend/out/main/index.js#L122) | 重编译前端产物；把"产物新鲜度"纳入发布检查 | `Select-String out/main/index.js -Pattern "sandbox: false"` **无命中**；`isSafeExternalUrl` 有命中 |
| 1.4 | 泄露的 API Key 随分发出厂 | [\_i2i.py:5](sz/game/content/ministers/layers/_src/_i2i.py#L5) + [SongZuo.spec:19](sz/game/SongZuo.spec#L19) | **轮换该 Key**（必须，文件已存在多份拷贝）；把 `content/ministers/layers/_src/` 迁到 `_dev_tools/`；spec 改为只打 `layers/*.png`+`_offsets.json`；`.gitignore` 补规则 | 打包清单不含 `_src/`；全库 grep 该 key 前缀 0 命中 |
| 1.5 | 干净机器启动必失败 | [start.bat:15](sz/start.bat#L15) | 解释器探测改为：venv → `py -3` → `where python` → 明确报错并给出安装指引；找不到时**不谎报"依赖安装失败"** | 在无 python 的环境复刻：给出明确可操作错误；有 python 时跳过 InstallDeps |
| 1.6 | 未配 AI 时 `/api/advance` 500 | [server.py:326](sz/game/backend/server.py#L326) | 接住 `AIRuntimeError` → 返回 `AI_NOT_CONFIGURED` + 可读提示（与 docstring 一致） | 无 `ai_config.json` 时该端点返回结构化错误码而非 500 |
| 1.7 | `fetch_models` 恒返回硬编码表 | [client.py:28](sz/game/ai/client.py#L28) | 把 `_http_get_json` 加入导入清单；**同时**核验 R2-2 的 SSRF 防护真的生效（此前是死代码） | 注入假端点可返回真实模型列表；跨主机 302 不转发 Authorization |

**1.1 / 1.3 / 1.4 建议同批发布**：三者都是"改动极小、影响极大、零设计分歧"。

---

## 阶段 2 · 测试与文档对齐（1–2 天，与阶段 1 并行无冲突）

| # | 动作 | 范围 | 验收 |
|---|---|---|---|
| 2.1 | 同步 **B3 语义反转**导致的 5 项过期测试 | `test_briefing.py`(2)、`test_core_logic.py`(2)、`test_regression_2026_08.py::test_treasury_stays_above_crisis_line` | 断言改用 `deficit_depth()`；`test_regression_2026_08` 的起手值断言改为"不穿底"而非"> 危机线" |
| 2.2 | 同步 **市舶双计通道**删除导致的 1 项 | `test_pop_identity.py::test_maritime_trade_ledger_closed` | 断言改为"关税经 `_settle_finance` 唯一入账"，删除对 `_settle_extensions` 的期望 |
| 2.3 | 同步机制文档数值漂移（先改数值类） | `游戏机制说明.md` 19 项 | 逐项与 `content/data.py` 对齐；破产线改"亏空深度"口径 |
| 2.4 | 修复并扩写常量门禁 | `check_docs_constants.py` | 按文档方式可运行；覆盖从 12 项扩到全量常量和重复键检测 |
| 2.5 | 补 H 路建议的前 6 条用例（性价比最高） | tests | 覆盖：`state_applier` 守恒预检/原子回滚、`resolve_event` 清理、`server.py` 鉴权与端点、守恒容差 |
| 2.6 | 补 `requirements-dev.txt`：`httpx`/`jsonschema`/`tiktoken` | 依赖 | `TestClient` 可用，端点测试可写 |

**验收**：`pytest` **390/390 全绿**——这是本阶段唯一的硬指标。当前 379/390。

---

## 阶段 3 · 守恒修复（3–5 天，**每项需先定语义**）

> 全部集中于 `core/settlement_steps.py`，修改互不重叠，可并行。

| # | 问题 | 位置 | 建议改法 | 守恒断言 |
|---|---|---|---|---|
| 3.1 | 囤粮超硬上限**销毁 2.65 亿石** | `settlement_steps.py:2033-2042` | 出清余量必须有对手方（转为农/太仓/损耗显式记账），禁止"抹去" | 单步 `Σ粮(全部池) == 0`（损耗单独记账） |
| 3.2 | 穿底后仍全额支出 → **造币==亏空** | `settlement_steps.py:2928-2963,3003-3015` | 国库不足以支付时按实付额发放（欠俸/欠饷记账），不得凭空入民间 | 60 月 `|Δmoney − 0| → 有明确对手方`；`Δ亏空` 不再等价于"新造货币" |
| 3.3 | 粮饷 `// len(paths)` **丢余数** | `registries.py:295-303` | 末条吃尾差（沿用 `CASCADE_REASON_FIX` 已有模式） | 国库扣减 == ΣPOP/武库入账（逐分） |
| 3.4 | 政务 savings **无上限 → 常费转负** | `focus_mechanic.py:477-479` + `settlement_steps.py:2904` | `savings` 设上限（≤ 基准常费的某比例）；`expenditure = max(0, BASE − savings)` | 长局 `expenditure ≥ 0`；国库不因该路径增多 |
| 3.5 | 畜栏产肉/酒坊累加**无对手账户** | `settlement_steps.py:2150-2156` | 肉/酒入账需买方（民间消费支出）或明确标记为"产出型外部入账"并单列科目 | 货币总量方程闭合，或外部入账有显式科目 |
| 3.6 | 家产四账不闭合 | `estate_mechanic.py:77-80,99-100` | 窖藏需对手账户（家产→窖银池） | 实测 Σ(家产+POP+窖银) 恒等 |
| 3.7 | 农超卖盈余 24 倍（**旁路已修的安全垫**） | `settlement_steps.py:1769-1784` | 卖方权重保底不得覆盖"不超盈余"硬约束 | 农存粮 ≥ 1 月口粮，36 月不破 |
| 3.8 | 流民 5% 上限双向钳制致**静默灭失** | `settlement_steps.py:752-755` | 超额部分按"未安置"保留或显式记因，不得静默丢弃 | 流民 + ΣPOP 每步守恒 |

**每项都要新增回归断言**，否则同类问题会再次回归。

---

## 阶段 4 · 机制与通道语义（需产品决策，2–3 天）

| # | 问题 | 位置 | 待决策 |
|---|---|---|---|
| 4.1 | `resolve_event` 清除谓词错（事件**永不清除**，已处理事件可反复结算） | [commands.py:602-607](sz/game/core/commands.py#L602-L607) | 谓词改比对 `e["title"]`；并给 `active_events` 设上限 + 过期淘汰 |
| 4.2 | 压力事件通道**无 title/choices/effects**，`方腊起义` 只是一行日志；且 `/api/resolve_event` 对它 404 | `settlement_steps.py:2462-2470` | 合并到 `events.py` 富事件卡（推荐），或删除该并行通道 |
| 4.3 | `era_state` 白名单内却每月被整体重算抹平（**死写入通道**） | `era_mechanic.py:79-81` | 引入 `era_base` 分离"基线/偏移"（推荐），或移出白名单 |
| 4.4 | 国策互斥"修复未达目的"、整支不可达 | `focus_mechanic.py:294-310` | 由你确认互斥语义 |
| 4.5 | `active_decrees` 月末整表替换 → 政令次月即失效 | `settlement_steps.py:63,95-108` | 确认 duration 语义 |
| 4.6 | 终局分支早退不推进回合 → 空转 | `settlement.py:341-345` | 终局后 /api/advance 应返回明确"已终局"而非静默空转 |

---

## 阶段 5 · AI 管线与安全（可与阶段 3/4 并行）

**AI 管线**
- 5.1 [C-2] `event_narrative` 唯一调用**不传 state** → 叙事护栏从未生效：把 `state` 传进去，并断言护栏违规会被拦。
- 5.2 [C-3] 复读检测：阈值 0.6 偏低 + `_prev_texts` 跨契约全局共享 → 改为按契约隔离 + 提高阈值。
- 5.3 [C-4] 安全词表误伤战争题材 → 加语境白名单（屠城/凌迟在叙事语境放行）。
- 5.4 [C-13] `_summary_text` 脱敏失败 fail-open → 改为 fail-closed。
- 5.5 [C-17] 数值护栏单位闭集不全、不支持「亿」、不容空格 → 补全并支持空格/亿。
- 5.6 [J-2] **叙事↔数值一致性护栏**（本批最有产品价值）：`advance_and_settle` 后校验"宣称已办 vs 状态是否变化"，不符则附加披露，避免"说办了没办"。

**安全 / 脱敏**
- 5.7 [E-3] 回环判定改用 `request.client.host`，不信任 `X-Forwarded-For`（同机反代下可免 token 全放行 → 应升 P0）。
- 5.8 [E-5/E-6] 快照漏脱敏：`finance.corruption_ded`、`pops.*.wealth`、`spy_network` → 纳入脱敏闭集，并**加"新增字段默认脱敏"的守卫测试**。
- 5.9 [E-7/C-8] `_ai_failures` 分支不可达 → 移出 `_` 前缀过滤，让 AI 失败对玩家可见。
- 5.10 [E-10/E-8] Rust 端 `0.0.0.0` + `allow_origin(Any)`、直序列化真值 → 与 Python 对齐（若 Rust 后端仍在路线内）。

**前端**
- 5.11 [F-2] `electron-builder.yml` 的 `files` 补 `out/renderer/**`（否则成品不可玩）。
- 5.12 [F-3] MapView 同步 effect 早退 + 依赖数组 → 修复开局/读档后舆图不同步。
- 5.13 [F-4] 事件吉/凶判定：认前缀式减益档位 + 按量级而非条数。
- 5.14 [F-6] 前端补 `Authorization` 头；[F-10] 加 ErrorBoundary。

---

## 阶段 6 · 工程治理（持续）

1. **CI**：`pytest` + `ruff --select F821,F601` + 导出物新鲜度 + `check_docs_constants.py`。
2. **静默失败收敛**：J 路点名的 **15 条有实际后果的 handler**（自动存档、记忆写盘、投资结算等）逐个改为"记录 + 玩家可见信号"。剩余 200 余处按模块分批，禁止一次性大改。
3. **覆盖率**：从 55% 提到 70%，优先 `backend/server.py`(0%)、`ai/client.py`(29%)、`state_applier` 核心 4 段。
4. **Rust 对拍门禁**：8 项常量漂移 + 19 行公式对拍纳入 CI；决定 Rust 后端是否继续维护（E-9/E-14 显示其已严重落后）。
5. **合规**：补项目 LICENSE；`THIRD_PARTY_NOTICES.md` 增补打包进分发的 Python 依赖；核实敏感词表许可。
6. **目录纪律**：清 `game/ui/__pycache__`（并从 `SongZuo.spec:17` 摘除 `ui`）、`assets/map/web/*` 残留、`game/.coverage` 入 `.gitignore`。

---

## 待你拍板的决策（阻塞对应批次）

| # | 决策 | 阻塞 | 我的建议 |
|---|---|---|---|
| ~~D1~~ | ~~`factions.*.power` cascade 规则：恢复还是正式废弃？~~ | 1.2 | ✅ **已拍板：正式废弃**——同时删除过期测试 `test_state_applier.py::test_cascade_faction_power`，并在代码注释中白纸化该决策。相应 1.2 的工作量降为"删测试 + 补注释" |
| ~~D2~~ | ~~压力事件通道（无 title/effects）：并入富事件卡还是删除？~~ | 4.2 | ✅ **已拍板：并入 `events.py` 富事件卡**——即保留"压力累积到阈值爆发"机制，但产出的事件必须是带 `title`/`desc`/`choices`/`effects` 的完整事件卡，可被 `/api/resolve_event` 按 title 反查处置。相应 4.2 需扩为"统一事件构造入口 + 保留压力累积语义 + 补 resolve 反查覆盖" |
| D3 | `era_state` 语义：**加 `era_base`** 还是移出白名单？ | 4.3 | 加 `era_base`（保留 AI 写入能力） |
| D4 | 国库穿底时的支出纪律：**按实付**（欠俸记账）还是**允许透支入账**？ | 3.2 | 按实付 + 欠俸科目（与已有"欠税"科目对称） |
| D5 | 民心/皇威归零是否要硬终局？ | 阶段 4 | 建议**不**加硬终局（你已确认国库下滑是设计），但需确认"皇威 5/5 种子第 55 月归零"是否可接受 |
| D6 | Rust 平行后端是否继续维护？ | 5.10、6.4 | 若不在路线内，建议**冻结并标注**，避免"两套经济"误导 |
| D7 | 是否授权 `git init` 并把审计产物纳入版本管理？ | 阶段 0 | 强烈建议——否则修复不可回滚 |

---

## 不修清单（按你的口径与设计意图）

- 国库开局下滑（**设计**）。
- 民心/皇威长期归零（长局观察，**待 D5 确认**后决定是否调平衡）。
- 派系党争每月 −1 皇威（史实机制）。
- 无 AI 配置时不能推进回合（设计上"无 AI 不可玩"，只需把错误码做对，见 1.6）。

---

## 每批的验收三连（统一执行）

```powershell
# 前置
$env:PYTHONPATH=''; $env:PYTHONIOENCODING='utf-8'; $env:PYTHONDONTWRITEBYTECODE='1'
Remove-Item Env:CODEBUDDY_TOOL_CALL_ID,CODEBUDDY_SAFE_DELETE_BULK_STATE_DIR -ErrorAction SilentlyContinue
cd D:\codebuddy\sz\game; $py='D:\codebuddy\.audit-venv\Scripts\python.exe'

& $py -m pytest "..\_dev_tools\game-dev\tests" -q -p no:cacheprovider   # ① 测试
& $py -m ruff check --select F821,F601 .                                # ② 静态门禁
& $py "..\_dev_tools\_scratch\audit-2026\_replay_60m.py"                # ③ 长局回归（守恒/时间闸/人口账）
```

**判据**：① 目标批次相关用例全绿且**总数不下降**；② `F821/F601` 零命中；③ 长局无 NaN/负值、人口账差 0、1103 年前无起义。

---

## 排期建议

| 批次 | 内容 | 工作量 | 可否并行 |
|---|---|---|---|
| 批次 0 | 安全网（0.1–0.4） | 0.5 天 | 否（阻塞全部） |
| 批次 1 | 止血 1.1、1.3、1.4、1.5、1.6、1.7 | 0.5–1 天 | 部分 |
| 批次 2 | 测试与文档对齐 | 1–2 天 | 与批次 1 并行 |
| 批次 3 | 守恒修复（3.1–3.8） | 3–5 天 | 可并行（同文件，需串行提交） |
| 批次 4 | 机制语义（需决策） | 2–3 天 | 与批次 3 并行 |
| 批次 5 | AI 管线 / 安全 / 前端 | 3–4 天 | 与批次 3/4 并行 |
| 批次 6 | 工程治理 | 持续 | 随时 |

**里程碑**：批次 0+1+2 完成 → `pytest` 390/390 全绿 + 分发版可启动 + 密钥不再出厂（约 2–3 天）；批次 3+4 完成 → 长局守恒闭合（约 1 周）。
