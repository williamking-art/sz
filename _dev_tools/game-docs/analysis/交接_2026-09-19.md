# 宋祚整改 · 会话交接（2026-09-19）

> 用途：上下文过长时的续接锚点。**权威细节以 git log 与各文档为准**，本文只列骨架。

## 一、任务源（3 份文档）
- `analysis/审查修改意见_2026-09-19.md`（18 条 P0–P3 + 12 项回归测试）
- `analysis/faction_pop_optimization_plan_2026-09-19.md`（利益集团＝POP 政治生态）
- `analysis/economy_finance_engineering_technology_review_2026-09-19.md`（经济/金融/工程/科技，**GBK 编码**）
- `analysis/portrait_system_design.md`（立绘机制现状与优化方案）

## 二、已完成（15 次提交，`28048bdc29` → `f74617a604`，未 push）
1. **审查 18 条**：SSRF、AI 配置事务化、provider 每线程快照、不回传 Key 片段、
   局势终态效果原子提交 + `progress_cost` + `deadline`、前端 SessionGuard、
   存档 schema 安全解析、识字率改 POP 派生权威、SQLite 加锁+事务、损坏库 quarantine、
   PATCH 保 fallback/settle、提示词不可信边界、工具白名单、词库 fail-closed、Rust Mutex poison。
   **连带真 bug**：`engine/state_applier` 把 0–100 字段的 **add 增量**当绝对值钳制
   → 威望/民心**负向效果静默失效**（已修 + 回归）。
2. **利益集团 一/二/三期**：立场占比（`FACTION_SPLIT_INIT` + `core/faction_split.py`，比率非账本）
   + 去地域化（`subset_kind="faction"`）+ **声量由官职权限派生**（`core/faction_voice.py`，绝对影响力）
   + 科举座主门生/恩荫/致仕/祠禄/离任（`core/officialdom.py`）+ 转投（`core/faction_settle.py`）。
3. **四系统**：经济物价（固定相位、涨跌上限、工程九项声明与降效）·
   金融（有效货币供给口径、交子三闸门、贷款=债权+资产、利息不入国库、记账/市场汇率）·
   工程（五态状态机、工匠工时、**营建闭环**：`propose_project` → `_settle_projects` → 落 `buildings`）·
   科技（level 降为读数、七域节点、**只卡前置**、adoption 覆盖）。
4. **蓝图仿明末**（`BUILDING_BLUEPRINTS`）：category/branch/requires_region/outputs + 工期 1–6 月。
5. **立绘 M0**：停止写 `frontend/public`（改受控路由 `GET /api/portrait/<f>`）+
   服色随在任状态（`minister_status` → shi）+ 前端 `utils/portrait.ts::portraitSrc()`。

## 三、用户已确认口径
集团取**总集**（新法系／元祐旧臣与保守士绅／**皇党集团**／**军功集团**／**中立派**）；
**台谏是官职**（非集团、非皇党），台谏官按个人派系归属；**立场跟派系、不跟地域**；
**声量＝官职权限大小**且为**绝对影响力**（非 Σ=100 份额）；**研究只卡前置**（缺钱不拒立项）；
**俸禄与粮食市场先后＝非问题**；**计算滞后 1 月＝接受**；立绘产物删除；Rust 环境已装。

## 四、当前状态
`Python 842 passed` · `cargo test 3 passed / EXIT=0` · 前端 `tsc` exit 0 · 工作区 0 项 · 未 push。

## 五、待办（优先级序）
1. **⚠️ 立绘"全不能看"根因（已定位，待修）**：
   `content/ministers/layers/_src/_headcut.py:10` 的 **`CUT = 0.62`** —— 头层在 62% 高度才切断，
   **把原图上半身/躯干一起抠进"头层"**。证据：42 个头层中 **41 个** alpha 下沿＝**673/1080 = 62.3%**
   （唯 `head_韩世忠`＝404/37.4% 正常）。合成时躯干盖住模板身体 → 双重身体/错位。
   **修法**：`CUT` 降到 ≈0.38–0.42（或按肩线检测）→ **会重写 42 个已入库头层**，需用户确认。
2. 立绘 M1–M3：缓存迁 `userData/portrait-cache`、预生成、人物稳定 ID + `portrait_manifest.json`、
   场景姿态（召对/进谏/军议/请罪/致仕）、有限表情、服丧/待罪公共层、年龄变体。
3. 银行信贷 240 月压力测试；座主解析回落口径 / 恩荫父辈近似细化。
4. 前端会话隔离**真机人工验证**（无 vitest/jsdom，按 `sessionGuard.selftest.ts` 头部步骤）。

## 六、环境
- Python：`D:\codebuddy\.audit-venv\Scripts\python.exe`，须 `$env:PYTHONPATH=""` + `$env:CODEBUDDY_SAFE_DELETE_ENABLED="0"`
- 前端：`$env:ELECTRON_RUN_AS_NODE='1'` + `node_modules\electron\dist\electron.exe`（无系统 node）
- Rust：`C:\Users\william\.cargo\bin` + WinLibs mingw-w64（本轮新装，跑 cargo test 前需入 PATH）
- 文档编码：`economy_..._review_2026-09-19.md` 是 **GBK**，按 `gb18030` 读
- 图片体检：`_scratch/portrait_contact_sheet.png`、`_scratch/headcut_compare.png`（供人眼复核）