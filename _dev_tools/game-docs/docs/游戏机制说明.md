# 《宋祚》游戏机制说明

> 北宋徽宗朝（1101 起）AI 驱动历史推演策略游戏。本文档基于 `game/` 源码实勘整理（2026-09 更新），描述当前版本的真实机制。新机制已**融合进各对应章节**，本节仅给架构总览。
>
> **2026-09 全量审计修复要点**（详情见各章）：
> ① **守恒与原子性**：`state_applier` 写入原子化（失败回滚）/ 0-100 区间校验 / 守恒补记账只补未配对项；内帑调拨执行前复检余额；月度结算异常回滚快照；存档原子写 + 损坏档备份。
> ② **数值修正**：评价「武功」量纲；士绅囤粮/买方分摊/家产封顶/奢侈分配的守恒漏洞；时代五维棘轮 → 目标值重算；学校科技加成小数池；`EXAM_HARD_POOR_SHARE` 补 7 档；`FINANCE_DECIDE_BASE` 死数据 → 单一权威源；外邦 type 注册与图鉴死键对齐。
> ③ **AI 管线**：`AI_TIMEOUT`/`AI_AUTH_FAILED` 错误码生产者 + 网络退避重试；dialogue 遇端点不支持工具时正确降级、去除重复计费；叙事护栏支持中文数字；缓存改 LRU。
> ④ **记忆库**：对话总结冲突改合并（不再静默丢弃）；按需建表 + WAL；压缩/总结窗口不重叠。
> ⑤ **接口一致性**：5 个结算侧契约接入按需唤醒 + diff 唤醒接线（`_last_agent_diff`）；未接线契约登记 `PENDING_CONTRACTS`；新增 HTTP 端点（见 §八）。
> ⑥ **前端**：Tk 界面废弃删除（Web/Electron 为唯一前端），迁移补齐清单见 §八。
>
> **产品定位（用户定稿）**：模拟一个穿越者（玩家），通过跟大臣的对话、发布圣旨（政策），来改变历史进程（北宋徽宗朝）。**AI 是游戏的核心引擎**——所有机制都通过 AI（agent）推演驱动，harness 保证稳定与编排，程序负责数值/守恒/校验。
>
> **运行形态（2026-09-18 勘误）**：游戏内核为纯 Python（`game/`），**唯一前端为 Electron + React + TypeScript，位于 `game/frontend/`**（舆图 MapLibre 渲染）。**Tkinter 界面已废弃并删除**（原 `gui_main.py` / `宋祚.bat` / `ui/panels_*.py` 已移除）。⚠️ **勘误**：本节此前写作「`_dev_tools/frontend/`」与「`ui/` 仅保留 `map_web.py`」——两者均已过时：前端实际在 **`game/frontend/`**，且 **`game/ui/` 目录已整体删除**（仅残留 `__pycache__/*.pyc`，见下）。前端经 HTTP 调 **Python FastAPI 参考后端**（`backend/server.py`，零复制复用 LocalBackend），打包入口 `backend_server_entry.py`；内核侧另有后端抽象（`backend/client.py`：LocalBackend 进程内 / HttpBackend 远程）供联调与远程体验。

---

## 2026-09-18 全量审查与设计定稿

> ⚠️ **阅读须知**：本章及以下三份设计文档描述的是**2026-09-18 定稿的设计**。其中**货币口径与财力消耗（L1 资产维持费、军俸史实水平）已实现并生效**，**官制与三冗仍全部待实现**——逐项状态见下方 §六「落地进度」。本文档其余各章仍描述**当前版本的真实机制**。请勿把待实现部分当作已生效能力——这正是 2026-09-18 审计指出的文档漂移问题的反面教训。

### 一、审查产出

| 文档 | 内容 |
|---|---|
| [review_2026-09-18.md](../analysis/review_2026-09-18.md) | 全量审查汇总：P0×9（去重后）、测试基线 379/11 及三簇归因、历史修复核实、误报澄清 |
| [fix_plan_2026-09-18.md](../analysis/fix_plan_2026-09-18.md) | 修复计划：阶段 0–6、逐项改法与验收、待拍板决策 |
| `_dev_tools/_scratch/audit-2026/`（10 份分报告） | A 结算经济 / B 状态存档 / C AI 管线 / D 写状态通道 / E 后端安全与 Rust / F 前端 / G 内容与文档 / H 测试质量 / I 启动打包 / J 端到端 |

### 二、设计定稿（三份，均待实现）

| 设计 | 文档 | 一句话 |
|---|---|---|
| **货币口径** | [货币口径规范_M0M1M2.md](货币口径规范_M0M1M2.md) | 引入 M0/M1/M2/M3 与月度对账恒等式，使"凭空造币/销毁"**在构造上可观测**；含仓鼠症与封桩分层 |
| **财力消耗** | [财力消耗设计.md](财力消耗设计.md) | 六层 money sink；核心是 **L1 资产维持费**（随资产存量增长）与 **L2 军俸补足至史实** |
| **官制与三冗** | [宋代官制与三冗设计.md](宋代官制与三冗设计.md) | 官/职/差遣分离、**冗官量化定义**、政务量 W 界定、地方三层可见性与路级四司、科举离散科次、吏员制度、公务员化 |

### 三、已拍板的设计决策

1. **废弃** `engine/state_applier.py` 的 `factions.*.power` cascade 规则（path 不在白名单、`FactionState` 无 `power` 字段，规则永不触发），并同步删除过期测试 `test_state_applier.py::test_cascade_faction_power`。
2. `core/settlement_steps.py::_settle_events` 的压力事件通道**并入 `core/events.py` 富事件卡**（保留"压力累积到阈值爆发"语义，但产出事件必须带 `title`/`desc`/`choices`/`effects`，并可被 `/api/resolve_event` 按 title 反查处置）。
3. **不为公务员制做专门的转轨系统**（不做"归一入口"、不做转轨开关）。公务员化由玩家在局内**通过现有政令链路**自行达成；工程上只需保证六个参数（编制官额/吏额、吏职级薪基、恩荫规模、磨勘年限与考绩、世袭比例、差役与募吏结构）落在可改路径白名单内并被结算消费。

### 四、已勘误的过时描述

| 位置 | 过时表述 | 实际 |
|---|---|---|
| 本文档头部「运行形态」 | 前端在 `_dev_tools/frontend/` | **`game/frontend/`** |
| 本文档头部「运行形态」 | `ui/` 仅保留 `map_web.py` | **`game/ui/` 已整体删除**（仅残留 `__pycache__` 中 2 个 .pyc） |
| `game/README.md` | `ui/` 包整体移除，但 `SongZuo.spec:17` 仍把 `ui` 打进分发 | 需从 spec 摘除 |

> 其余文档-代码数值漂移（24 项，如 `MONTHLY_EXP_CIVIL_BASE` 88 万 vs 文档 55 万）**尚未修正**——它们须与代码改动同批处理，不在本轮文档更新范围内，清单见 [G-content-docs.md](../../_scratch/audit-2026/G-content-docs.md)。

### 五、实现硬约束：POP 挂载律

> **本约束由用户定稿，适用于上述三份设计的全部落地改动。**

现行代码已有反例：`prefectures[].officials`（`户数 × 0.00135` 派生）与 `pops["官僚"].size`（`(官+吏) × 9`）是**两本账**，30 年实跑官僚 POP +23.1% 而 `officials` +0.0%，导致"养更多官、一分钱不多花"。**本律即为杜绝此类再次发生。**

| # | 律条 |
|---|---|
| **1** | 凡**"人"**（官 / 吏 / 宗室 / 兵 / 编制）→ 必须落在 **6 类 POP 的 `size`** 上，**不得另立人口账本** |
| **2** | 凡**"钱 / 粮"** → 必须落在某 POP 的 `wealth` / `grain`，或国库 / 内帑；POP 之外**只允许 `core/money.py` 作为只读对账视图**，不得成为写入权威源 |
| **3** | 凡**"阶层变动"** → 必须走既有 POP 流动通道（科举 / 城市化 / 回乡 / 恩荫 / 致仕 / 差役），且 **ΣPOP 守恒**（或显式记为人口自然增减） |
| **4** | **禁止**任何与 POP 平行的独立存量账本 |
| **5** | 每条改动都要配**守恒断言**，并进回归测试 |

**各计划改动的 POP 挂载表**（实现前必须逐行确认）：

| 计划改动 | 人数轴 | 财富轴 | 流动轴 | 守恒断言 |
|---|---|---|---|---|
| 官僚 POP 身份子池 | `官僚.size` 不变，内部拆 `on_post`/`waiting`/`sinecure` | 俸禄按子池入 `官僚.wealth` | — | `size == on_post + waiting + sinecure` |
| **消灭双账** | `officials`/`clerks` 改为由 `官僚` 子池**派生** | — | — | `officials + clerks == 官僚.size` |
| **离散科举（每 36 月）** | `官僚.size` ↑（落**待阙**池） | — | `士绅 / 农 → 官僚`（寒门/士绅子弟按 `EXAM_HARD_POOR_SHARE`） | ΣPOP 不变；`state.exam["cohorts"]` 记科次台账 |
| 恩荫（绑郊祀） | `官僚.size` ↑ | — | `士绅 → 官僚` | ΣPOP 不变 |
| 致仕 | `官僚.size` ↓ | — | `官僚 → 士绅` | ΣPOP 不变 |
| 荫补子弟 | `官僚.size` ↑ | — | **新增人口**（非转移）→ 须与人口自然增长口径一致 | 记为增长而非转移 |
| 吏禄 → 陋规 | — | 农 / 工商 `wealth` ↓；国库入库 ↓ | — | 差额去向显式（吏所得 ＋ 流失） |
| 差役来源 | `农 / 士绅.size` ↓，`吏.size` ↑ | — | 转移 | ΣPOP 不变 |
| **军俸提至史实** | — | 国库 ↓，`兵.wealth` ↑ | — | `ΣΔ(国库 + 兵) == 0` |
| M0/M1/M2 口径 | — | POP wealth 是 **M0 主体** | — | 月度对账残差 == 0 |
| L1 资产维持费 | — | 国库 ↓ → `工匠.wealth` 40% ＋ `商人.wealth` 60% ↑ | — | `ΣΔ(国库 + 工匠 + 商人) == 0` |
| 宗室俸禄 | **挂 `士绅` POP 的 `clan` 子池**（不新开第 7 类） | 国库 ↓ → `士绅.clan.wealth` ↑ | — | ΣΔ 闭合 |

> **推论（已按本律修正原设计）**：原 [宋代官制与三冗设计.md](宋代官制与三冗设计.md) 的 L2c 拟新增独立 `clan_pop` 字段——**违反律条 1**。现改为：宗室作为 **`士绅` POP 的子池 `clan`**（宗室本就属官户/士绅一类，免役、有俸），不新开账本。

### 六、落地进度（滚动更新，区分「已生效」与「待实现」）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **① 审查问题** | P0×9（去重后）：被注释吞掉的 `_resolve_path_value`、`_http_get_json` 漏导入、F601 重复键、`AIRuntimeError` 未接、`start.bat` 解释器探测、测试套件过期断言等 | ✅ **已修**（见 [fix_plan](../analysis/fix_plan_2026-09-18.md) 阶段 0–2） |
| **② 货币口径** | **B-1** `core/money.py`（M0/M1/M2/M3 ＋ 月度对账，零行为变更）→ **B-2** 守恒修复（60 月残差 +43,380,547 → **−7,852 贯**）→ **B-2′** 军俸提至史实（1,349 万贯/年）＋ 按实付/欠饷（修穿底造币）→ **B-3** L1 资产维持费（新结算步 Step 3.9，零残差转移） | ✅ **已生效**（见 [货币口径规范 §13](../docs/货币口径规范_M0M1M2.md)、[财力消耗设计 §三](../docs/财力消耗设计.md)） |
| **③ 官制完善** | **C-1/C-2** 消灭 `officials`/POP 双账 ＋ 官僚 POP 身份子池 → **C-3** 官制结算步（Step 3.95）＋ 宗室俸禄（Step 3.96）→ **C-4** 免役→役钱税基口径显式化 → **C-6** 科举改**离散科次** → **C-5** 吏制（Step 3.97）→ **C-7** **编制参数入可改白名单**（9 参数经 `institution` 字段政策令调整，各被结算消费） | 🔶 **大部分已生效**（C-1～C-7 已落地并验证）；**待做**：C-8 地方三层可见性与路级四司、C-5′ 吏制专用诏令文案与奏议外显 —— 见 [宋代官制与三冗设计.md §十八](宋代官制与三冗设计.md) |
| **④ 历史↔游戏性取中** | 见下方 **§七 取中校准**：新增**农户粜粮完税**与**结余补发积欠**两个通道 ＋ 四个校准旋钮（待阙俸率/祠禄折俸/吏薪基值/吏额膨胀率）；**史实锚点不动** | ✅ **已生效**（441 例测试全绿；240 月残差 −30,907 贯） |

> **本章下文「二、设计定稿」中，货币与财力的实施状态已如上行所示**；官制的 **C-1～C-7 已生效**，**C-8 与 C-5′ 仍待实现**，阅读时请勿当成已生效能力。

**官制已生效部分的关键机制（当前版本真实行为）**：

- **官额不再是常量**：`p["officials"]` 降级为 `pops["官僚"]` 的派生镜像（唯一写入点 `core/officialdom.sync_legacy_mirror`）。科举入仕、恩荫、宗室入官让**待阙池**增长，致仕/裁汰让官回到**士绅**（士大夫再生产循环闭合，ΣPOP 守恒）。
- **俸禄按子池计价**：在岗全俸、待阙半俸、祠禄折俸，再乘**磨勘指数**（年 +0.6%，封顶 1.5）。
- **冗官 = 官额 − 差遣定员**（定员 = Σ中央机构岗位 ＋ 1350 × 路数）。240 月实跑：官 27,001 → 62,934、冗官 35,900、待阙被压在定员一半、祠禄承接过额；官俸 81.0 万 → 152.1 万贯/月。
- **宗室**：`士绅.clan` 子池按 3%/年复利，俸禄 30 贯/人/月由**内帑**支给（消耗囤积）。
- **吏制（Step 3.97，`core/clerks.py`）**：吏额由**政务量**驱动（不再恒等 `官×8`），编制存量因世袭/请托**自我膨胀**（"吏额有增无损"）→ **冗吏**；吏禄不足（3.5 vs 生计线 7.2 贯/月）→ **陋规**（民间三池 → 官僚 POP 的纯转移，零货币残差）＋ **民心代价**；**吏怨**缓动且不归零；**把持度**压低**诏令执行率**；**有效吏力 = 吏数 ×(1 − 吏怨/100)** 做出「**冗而不足**」；政务积压增长由 `W − C_eff` 取代原 `random.randint(0,3)`（去掉"无载体随机"）。吏不具名，面板只给**定性四档**（案牍清明／吏习其事／吏胥弄权／吏弊丛生）与 `/api/readouts` 的 `clerks`。
- **编制参数（阶段 C-7，`core/institution.py`）**：9 个参数 —— **定编宽严 / 吏职级薪基 / 荫补之门 / 磨勘年限 / 祠禄比例 / 世袭比例 / 募吏比例 / 考课黜落 / 资产维持费**。AI 经 **`free_effect` 的 `institution` 字典字段**提议改动（未授权参数**整单拒绝**，值域由 `INSTITUTION_PARAM_SPEC` 单点钳制），**每个参数都被结算真正消费**。`state.institution_params` 只存**改过的键**（旧档天然兼容），读数见 `/api/readouts` 的 `institution`。
  > **决策 3**：不为公务员制做转轨系统 —— 宋代官吏制 ＝ 这组参数的一组取值，公务员制 ＝ 另一组取值；**"穿越者知道该往哪走"就是玩法**。§17.5 的"先定编、再提薪"路线现在玩家可自行走通（一步并轨到官同薪仍会崩）。

### 七、历史 ↔ 游戏性「取中」校准（2026-09-18 第二轮，用户拍板）

> 用户口径：**在历史与游戏性之间取中**。原则是 **不动史实标尺（`ARRIVAL_BASE`/`ROUTE_POST_QUOTA` 等锚点保持原值），而是把"通道"修对** —— 凡是历史上本该发生、模型里却恒不发生（或恒发生）的环节，修它，而不是改数字。

#### 7.1 取中清单

| # | 项目 | 历史侧 | 游戏性侧 | **取中值** | 实测效果 |
|---|---|---|---|---|---|
| 1 | **农户税负通道** | 农户现金少，但**卖粮换钱完税**（免役钱、二税折色皆需现钱），买主是豪强/粮商 | 税基要真的能用，否则"三冗→加征→民怨"在农户一侧断链 | **新增「粜粮完税」**：农现金不足时向本路 `士绅`/`商人` 粜粮换钱缴税（**钱粮双向守恒**），售粮后须留 3 个月口粮、单月不超存粮 5% | 农欠税 60 月 **4,399 万 → 1,196 万贯**；累计粜粮完税 **3,115 万贯** |
| 2 | **实际到库率** | 隐漏与拖欠严重（原 ≈40%） | 国库要能运作 | **不动 `ARRIVAL_BASE=0.45`**，靠修第 1 项把**有效到库率从 40% 提到 ≈52%** | 平均月度亏空 **−105.8 万 → −1.4 万贯**（60 月） |
| 3 | **待阙俸率** | 待阙多**无俸**（=0） | "待阙堆积"必须有财政成本，否则冗官不痛 | **0.35**（`WAITING_PAY_RATIO`） | 待阙 3,154 人（60 月）、12,821 人（240 月），皆有成本 |
| 4 | **祠禄折俸** | 约半俸且偏低 | 需体现祠禄成本 | **0.4**（`SINECURE_PAY_RATIO`） | 祠禄 2,455 人（240 月） |
| 5 | **吏薪基值** | 吏禄极薄（2.0 贯 → 实得 ≈3.5 贯/月，仅及家庭口粮一半） | "先定编、再提薪"的公务员化路线要能走通 | **2.4 贯/月**（实得 ≈3.9 贯，**仍在 7.2 贯生计线之下 → 陋规与吏怨依然存在**）；玩家经 `clerk_pay_mult` 可继续上推 | 吏怨 22.0、陋规 25.9 万贯/月（60 月） |
| 6 | **吏额自我膨胀** | 吏额冗滥严重（"吏额有增无损"） | 20 年内要可感知 | **0.10%/月**（`CLERK_ENTITLEMENT_GROWTH`） | 冗吏率 60 月 **5.6%** → 240 月 **17.4%** |
| 7 | **积欠处理** | 丰年**补发积欠**俸饷 | ——（取中后出现的内部矛盾：国帑丰裕而欠饷单向累积＝"国库满、军队欠饷"，一眼假） | **新增「补发积欠」**：结余超应急安全库存（50 万贯）时，每月用其 25% 补发；国库→兵/官僚 POP 纯转移，同步冲减各军欠饷 | 240 月 `pay_arrears` **4,833 万 → 176 万贯**；60 月仍 3,086 万贯（紧绑期不补，**压力保留**） |

#### 7.2 取中的三条纪律（写进方法论）

1. **修通道，不改标尺**：史实锚点（`ARRIVAL_BASE`、`ROUTE_POST_QUOTA`）原值不动。若"历史值"让游戏不可运作，先查**是不是某个环节恒不发生**（本次就是农户现金恒低于保底线 → 农税通道整体死账），而不是直接把历史参数改软。
2. **取中不等于取悦**：第 5 项刻意让吏禄**仍低于生计线**，陋规与吏怨继续存在；第 7 项刻意**只补结余的 25%**且保留 50 万贯安全库存 —— 压力必须留在局里。
3. **每项都配守恒断言**：粜粮完税是 `士绅/商人.wealth → 农.wealth` 且 `农.grain → 买方.grain`（钱粮双向守恒，`ΔM_ALL == 0`）；补发积欠是国库 → 兵/官僚 POP（`ΔM_ALL == 0`）。见 `tests/test_middle_calibration.py`。

#### 7.3 取中后的长局形态（240 月实测）

| 指标 | 数值 |
|---|---|
| 平均货币月入 / 月出 | 3,630,832 / 3,353,715 贯（**后期转盈余 +27.7 万/月**） |
| 末年国库 / 内帑 | 212 万 / 1.116 亿贯 |
| 欠饷欠俸 / 维持欠费 / 农欠税 | 185 万 / 73 万 / 220 万贯（均为**余额**，非累计） |
| 冗官率 / 冗吏率 | 36.1% / 17.4% |
| 货币对账残差 | **−30,907 贯 / 240 月**（≈−129/月，截断量级） |
| ΣPOP + 流民 == population | 逐月**零偏差** |

> **长局形态是"开局窘迫 → 中期紧绑 → 后期转丰并补发积欠"**，与北宋实际（熙宁/元丰 封桩）同向。
> 后期国帑转丰后的**仓鼠问题**仍待封桩分层（货币规范 §12）与 L4/L6 层 sink 处理。

### 八、结算步专项检查（2026-09-18 第二轮）

对月度结算流水线做的专项核对：**真实管线 ↔ 测试镜像 ↔ 文档**三方对照 ＋ 逐步守恒实测。

| # | 发现 | 性质 | 处置 |
|---|---|---|---|
| 1 | **测试镜像 `_STEPS` 缺 `legacies` / `focus` 两步** | 🔴 审计盲区：逐步账本测试对这两步完全不可见 | ✅ 补全，并新增 `test_test_mirror_covers_full_pipeline` **逐位比较函数对象**；已做**反证**（抽掉 `focus` / 交换 `finance`↔`clerks` 都报红），确认断言非空 |
| 2 | **国策度支凭空销毁货币**：`focus_mechanic.settle_focus` 用裸 `state.change_treasury(-cost)`，无对手方、未登记 burn | 🔴 **货币守恒破坏**（实测：国策 10,000 贯/月 × 3 月 → 累计残差 **−30,416 贯**，`burned=0`；正常基线仅 −418） | ✅ 改为**支出回流**（国库 → 工匠40%/商人60% POP，统一口径 `GOV_SPEND_TO`）；**财政成本保留、货币守恒恢复**（残差回到 −414） |
| 3 | **三个帝国修正是"假效果"**：冗官冗费/隐田蔽课/辽夏边患只改**累计统计量**（`total_expenditure`/`total_income`），一分钱没动 | 🟠 与项目自己在 `focus_mechanic.py:473` 的 **B7 修复**同类（该处注释已判定"扣累计量属假效果"）；且**污染数据** —— `flow_summary` 面板与 `ai/client_utils`（AI 读财政做推演）拿到注水数字 | ✅ 三个修正改为**纯叙事标签**，真实成本由子系统承担（官俸/吏俸、`hidden_rate`、军俸）——避免与 `total_income/expenditure` **双重计数**。<br>⏳ **待用户定夺**：这三个修正**消除后应给什么真实奖励**（目前消除只是移除标签） |
| 4 | **文档 §二 流水线缺 4 步**（3.5.5 `region_deepen`、3.97 `clerks`、6.9 `legacies`、6.10 `focus`） | 🟡 文档漂移：机制文档比真实管线少 4 步，且 6.9/6.10 自始未记录 | ✅ 补齐为 26 条（含 **28 个步骤函数**的说明） |
| 5 | `settlement.py` 注释称吏制步"**诏令执行**也要读本月吏额与吏怨"，但诏令执行在 Step 1、吏制在 Step 3.97 | 🟡 注释与实现不符（效应只是**构造性滞后 1 月**，非 bug） | ✅ 更正注释，并在文档注明滞后为构造性 |

**检查为「非问题」的项（已实测排除）**：

| 项 | 实测结论 |
|---|---|
| `_settle_clerks`（3.97）在 `_settle_economy`/`_settle_granary` **之后**搬动农→官僚 POP，是否污染已完成的口粮/增长计算 | 月度搬动量仅 **20~37 人**，而农 POP 月度净变化 2.5~8.5 万 → 相对影响 **< 0.05%**，无实质影响 |
| `_settle_treasury`（Step 5）在镜像里但是空操作 | 确为显式占位（docstring 已注明"不得再写财政逻辑"），保留无害 |
| `statistics["redundant_officials"]`（C-3 写的**冗官人数**）与 `legacies["redundant_officials"]`（帝国修正 key） | 不同命名空间、不互相覆盖；但**同名不同义易混** → 建议后续把统计键改名（如 `redundant_officials_count`）。低风险 |

> **方法论（本次最大教训）**：**镜像缺失不会让任何测试变红** —— 它只会让"新结算步偷偷漏钱"从所有断言底下溜过。
> 所以「账本镜像必须与真实管线逐步一致」这件事本身要有一条**可反证的**断言，而不是靠人记得同步。

---

## 〇、AI 驱动架构总览

- **AI 通过结构化契约返回变更**（2026-09 审计对齐实现现状）：各角色 `*_decide` 契约（JSON 输出 + validate 校验 + 失败回喂）为**主通道**；大臣办差走 **function calling**（`enable_tools` 三档：auto/on/off）；`STATE_TOOL_SCHEMAS`（`update_state`/`query_state`/`trigger_event` 3 工具 + `tool_choice=required`）为**预留契约**（尚无生产调用方，见 `ai/client_utils.py` 注释）；AI 无自由文本改状态权。
- **应用层唯一改状态通道**（`engine/state_applier.py`）：验证（path 白名单/op 类型/clamp/非负）→ 多 Agent 冲突合并（按 path 分组、add 累加、set 先 add 后）→ **守恒校验**（钱/粮分组 ΣΔ==0 + reason 补记账 + 硬拒绝，不凭空造灭）→ cascade → 原子写库 + 变更日志 → 返回叙事层。**补来源支持按 `share` 拆分归属（D10）**：酒课 工匠60%/商人40%、田赋 农60%/士绅40%，末条吃尾差 → 逐条取整亦保 `ΣΔ==0` 精确；其余单条规则行为不变。
- **契约统一**（`ai/contract_adapter.py`）：34 契约 → {changes, narrative} 双通道；叙事类 narrative 主（受 narrative_guard 数字/来源闭集/人物护栏），句式库作文风参考（非锁定模板）。**待接线**（有 T5 测试覆盖，尚无生产调用方）；fallback 已排除记录/意图/控制字段（tone/risk_hint/prepared 等），防「记录字段被当作 changes」。
- **Agent 按需唤醒**（`core/agent_router.py`）：economy 必调（核心推演）；military/relief 按真实军情/灾荒唤醒（morale<30/入侵事件/流民>8000）；**2026-09 接线**：`decree_execute`（诏令执行）/`survey`（清丈）/`faction`（党争）/`land_local`（田亩地方）/`granary`（仓漕）五个契约接入按需唤醒（结算侧本就消费其槽位，此前无生产者恒走兜底）；**diff 唤醒已接线**（`applier_pipeline` 回写 `_last_agent_diff`，路径匹配支持段级 `*` 通配）；未唤醒 Agent 不耗 token；narrative 始终低 token。未接线契约登记于 `PENDING_CONTRACTS`（`_reform_ai` 无契约方法、`_finance_ai` 有生产者无消费者、`_invest_ai` 无调用方）。
- **记忆库（SQLite 一轮一库）**：主库 `saves/slot_{slot}.db`（圣旨/口谕/决策/事件：去重 + 6 回合压缩 + 12 回合总结 + 精确调动）；对话记忆库 `saves/slot_{slot}_dialogue.db`（召对对话：每 3 回合总结去重，与主库分离，防记忆漂移/膨胀）。**结算失败回滚（A2）**：两库各有 `rollback_after(turn)` 按水位截断——记忆库含 SQLite 连接、不可深拷贝，故无法随 `state` 快照还原；只删 `> turn`，不误删同回合合法写入。主库内存镜像本就在快照内（`MemoryGraph` 可深拷贝），但 SQLite 文件行不在，故仍需此截断。**检索与容量完善（2026-09-18）**：① `keyword_search` 中文按 2-gram 切分（原「连续中文整段」取词使玩家自然语言查询恒 0 命中，拟旨「既往同类诏令」注入通道长期静默空转）；② 内存 `query`/`keyword_search` 补 `turn <= self.turn` 封顶，与 `query_sql` 同口径（读旧档不得「记得未来」），且 `load_game` 在 `memory.load()` 后按主存档水位重新对齐 `memory.turn`（审查 B-1/J-10）；③ `archive()` 由 `finish_turn` 每 12 回合接线（容量治理：w_eff 低于阈值打 archived 标记、不物理删除，检索不再注入陈旧史；原实现零调用方）；④ 补只读审计接口 `query_change_log`（`change_log` 表此前只写不读）与对话库 `ref_ids` 下钻（`fetch_dialogues_by_ids`/`summary_refs`/`list_summaries`），对话总结改为回补全部未总结窗口（原只处理当前 3 回合窗口，跳期即永久滞留）；⑤ 槽位 db 缺失（新开局/换档）时 SQL 路径回退内存镜像（原只在未落盘时回退）；⑥ 新增只读端点 `GET /api/memory` 与前端「记忆」面板（史略·概要·近关系·召对纪要·变更留痕）。**会话回放完善（2026-09-19）**：① **两侧对白对称入库**——`dialogues` 原先只有 `audience_dialogue_apply` 写入的大臣回奏，「朕言」只进 `state.dialogue_history`（内存/存档）从不落库，玩家回看只剩单边对白；现 `audience_dialogue_prepare` 同点补写 `speaker="朕"` 行，`topic` 与 `_topic_key` 同源使问答同组。② **短路路径补写**——本地预过滤模板与召对缓存复用两条分支在 `prepare` 之前 return，整轮对话既不进对话库也不进后续总结（面板回看「整轮空缺」）；现统一经 `_record_dialogue_row` 落库（失败不阻断召对）。③ **概要标注发言者**——补写朕言后，`summarize_dialogues` 若仍不标 speaker，陛下口谕会被并入大臣之言；该概要随即作为「卿前番言」喂回 AI，等于让大臣把上意记成己见，故按 `speaker：text` 标注。④ **只读回放接口**——新增 `list_dialogues`（完整会话流：不因 `summarized` 丢行、不截断正文、按时间正序、含两侧）与 `list_sessions`（会话列表：末条预览/条数/末次回合），补上「已总结原文取不回」与「面板无会话列表数据源」两个缺口。⑤ **端点会话视图**——`GET /api/memory?minister=<名>` 返回该大臣会话流 + 其按期纪要（`dialogue_summaries` 收窄）+ 涉其近关系（`minister_relations`）+ 全量 `sessions`；不传参即原全局视图（契约不变，独立面板不受影响）。
- **异步化**（`core/async_ai.py`）：**未接线（审查 B7）**——全库无生产调用方，其线程纪律以**已移除的 Tkinter `ui.after(ms, fn)` 轮询**为前提；现 Web 架构的非阻塞由 `backend/server.py` 同步端点（FastAPI 自动置于线程池）+ 前端 HTTP 异步承担。设计本身（后台只做 AI 网络调用 + 纯函数校验；写状态/前端更新回主线程；同一 AIClient 实例按互斥锁串行化；网络瞬时错误有限次退避重试、调度失败转错误回调不静默丢弃）保留为「日后若需服务端后台推演」的参照实现；其**拆分思想**已被沿用（`audience_dialogue_prepare`/`_apply`、`advance_month`/`settle_local`：先取值、后台只算、主线程落地）。接线前不计已生效能力。
- **回合时序（用户定稿）**：**AI 推演（先）→ 系统结算（后）→ 形成叙事（最后）**。
- **核心原则**：AI 只做「判断+叙事」，可确定的计算本地化；**AI 保混沌**；**记账对应**（来源=去向，非总量守恒）；**AI 缺失 → 不执行 + 明确报错**（6 错误码），绝不伪造。
- **省 token**：按需查询（召对基础注入 ~120 字符，较全量 -98%）、本地计算优先、档位词 7 档（无/微/小/中/大/巨/极 + 丰富表达归一 + 正负方向）。
- **大臣个性保障**：不同大臣对话/拟旨体现差异（蔡京逢迎/陈瓘直谏/章惇峻切）；persona 0-100 + style_decree 拟旨风格；忠诚/贪腐数值绝不注入。
- 详细机制融合见各章：月度结算（§二）、诏令（§三）、经济（§四）、派系人事（§六）、事件（§七）。

---

## 一、游戏定位与基本框架

- **起点**：1101 年 1 月（建中靖国），玩家扮演宋徽宗赵佶。
- **回合制**：以"月"为基本结算单位，`run_monthly_settlement()` 每回合按 11 步流水线结算。
- **三种难度**：史实（默认）/ 轻松 / 艰难，参数见 `DIFFICULTY_PRESETS`（威望起点、到账率、事件压力倍率、外部势力发育倍率等）。
- **目标**：不是固定胜利条件，而是"活下来并留下好结局"——结局由七维评分决定（详见第九节）。

### 核心状态（GameState）
| 维度 | 关键字段 | 说明 |
|------|---------|------|
| 时间 | year / month / turn / era_name | 年号按史实推进（崇宁 1102、大观 1107、政和 1111、重和 1118、宣和 1119） |
| 皇帝 | emperor_health(75) / art_mastery(85) / taoism_leaning(25) / pleasure_leaning(30) | 健康与三项个人倾向 |
| 皇威 | prestige(55) | 0-100，权威/执行率的全局乘数 |
| 财政 | treasury(500万贯) / imperial_treasury(100万贯) / granary(1500万石) | 国库、内帑、太仓三账分理 |
| 民生 | population(8000万) / population_satisfaction(55) / 各路 refugees | 全局民心 + 各路流民池 |
| 派系 | factions{} | 各派系 influence/satisfaction/cohesion |
| 军事 | army_units[] / defense_lines{} | 兵额唯一真账（实体列表）+ 防线聚合 |
| 机构 | yamen{} / central_orgs{} | 六部衙门 + 中枢机构（权限/效率/积压） |
| 地方 | prefectures{} | 各路州县：户数/田亩（五类归属）/粮产/民情/治理/动乱/粮价 + 六类 POP 明细 |

---

## 二、月度结算流水线（agent 化）

每回合 `run_monthly_settlement()` 顺序执行；**推演层由 agent 驱动**（经济/清丈/外交/军事/事件/灾荒），**守恒/数值步保持程序本地**（agent 只给档位词，数值程序换算封顶，绝不触碰守恒数值）：

1. **Step 0 破产兜底**：国库 < -2000 万贯 → 直接"国用耗竭"游戏结束；国库 < -500 万贯 → 触发"库藏空虚"危机事件（增商税/裁冗费/发内帑/卖度牒 四选一）。
2. **Step 1 诏令执行**：按执行率逐条执行待办诏令（见第三节）。
3. **Step 2 派系结算 + 大臣立场演化**：凝聚力/满意度/影响力小幅随机漂移；势力悬殊 > 40 → 党争减威望；**每大臣立场按程序公式演化**（派系满意度 0.25 + 影响力 0.15 + 事件 0.25 + 皇威 0.15 + 民心 0.10 + 国运 0.10 × 性格调制；**国运取 `population_satisfaction` 同源维度**〔2026-09 修复：原读不存在的 `national_mood`，该项恒 0〕；仅对 `MINISTERS` 在册且在朝者演化），家产随 corruption 膨胀。
4. **Step 3 经济基础（推演官）**：人口自然增长；各路流民按民情/动乱/治理吸纳回人口；**economy_decide**（agent）推演景气/士绅/生产/窖银/城市化/回乡/科举 + **5 金融字段**（交子信任/钱荒/市舶/银行/物价趋势）→ 程序换算落地。
5. **Step 3.5 田亩地方（按察使）**：粮产率随科技演进；荒田开垦；隐田率缓慢回升；田亩归属与士绅隐田流转；**survey_settle**（agent）清丈隐田/抑兼并 → 退田/抄没；田赋本色入太仓、折色入国库；粮价/物价全盘重算。
5b. **Step 3.5.5 地区模型深化**：`_settle_region_deepen` —— 各路的民心/士绅抵抗/城防/地方财政等区域字段的自然演化（参考《明末：捞金模拟器》的地区模型）。
6. **Step 3.6 扩展维度 + 金融调制**：交子信任度/市舶关税/科举人才池/科技水平自然演进 + 研发管线推进；**经济金融 AI 档位落地**（交子/钱荒/市舶/银行/价格系数，调制幅度与值域统一取自 `FINANCE_DECIDE_BASE` 单一权威源）+ **投资回报分期**（settle_investments，回报按**外部经营收益**口径记账 `statistics.invest_return`）。
7. **Step 3.7 长期政务 + 外部政权模拟（史官/使节）**：在办公开事务/密令按进度推进（**记忆图谱写 progresses**）；外部政权按发育曲线成长；**diplomacy_decide**（使节 agent）推演外交态度/岁币/盟约。
8. **Step 3.8 仓廪漕运**：漕运汇聚（受漕路阻塞影响）、雀鼠耗、本色俸禄支取、常平仓平抑粮价、经济认知层更新（守恒步，agent 不碰）。
9. **Step 3.9 资产维持费（L1 sink，2026-09-18 新增）**：`_settle_upkeep` —— 各类资产按 0.5%/月计维持费，从国库支出、全额转入**工匠 40% / 商人 60%** 的 POP `wealth`（**零残差转移**，`ΔM_ALL == 0`）；国库不足时**按实付**，缺口记 `statistics["upkeep_arrears"]`。排在财政步之前，使"可支财力"在财政步可见。
10. **Step 3.95 官制（2026-09-18 新增）**：`_settle_officialdom`（实现在 `core/officialdom.py`，**单一权威源**）——
    ① 旧档官僚 POP 子池迁移修复 ② 不变量校验（`size == officials + clerks`；`officials == on_post + waiting + sinecure`）③ **有阙授职**（待阙 → 在岗）④ **待阙超「定员 × 0.5」超额转祠禄**（宫观官折俸）⑤ 派生镜像同步（`p["officials"]`/`["clerks"]` 的**唯一写入点**）；
    每年正月追加：**致仕**（在岗 → 士绅）、**磨勘**（品阶上浮，`official_rank_index` 封顶 1.5）、**宗室**（`士绅.clan` 复利 3%/年 ＋ 年 2% 入官）；每 3 年郊祀追加：**恩荫**（士绅 → 待阙，规模随皇威缩放）。
    排在财政步之前，使 `calc_official_*` / `calc_clerk_*` 读到本月官额。
11. **Step 3.96 宗室俸禄（L2c sink，2026-09-18 新增）**：`_settle_clan` —— **内帑 → 士绅 POP**，30 贯/人/月，按实付（`ΔM_ALL == 0`）。
12. **Step 3.97 吏制（2026-09-18 新增）**：`_settle_clerks`（实现在 `core/clerks.py`）—— 吏额由**政务量 W** 驱动（脱离 `官 × 8`）、编制存量因世袭/请托**自我膨胀**（冗吏）；吏禄不足 → **陋规**（民间三池 → 官僚 POP，纯转移）＋ 民心代价；**把持度**与**吏怨**缓动；导出「有效吏力」与吏治四档。排财政步之前，使俸禄/贪腐读**本月**吏额与吏怨。
    > 注：`Step 1 诏令执行` 用的把持度折扣读的是**上月**吏治状态（本步在上月写入）—— 构造性滞后 1 月，非缺陷。
13. **Step 4 财政**：货币税收入（工商+役钱+市舶+二税折色+盐课）vs 支出（常支+折色俸禄+岁币+**大臣家产划转**+**建筑维护费**），结余/亏空入国库，内帑抽成（守恒步）。**2026-09-18 起改为「按实付」**：国库不足时只支可支部分，缺口记 `pay_arrears`（军俸）等备忘科目并按标准俸额比例摊入各军 `ArmyUnit.arrears`，不再穿底造币。**同年新增两条守恒通道**：① 征收段内的**农户粜粮完税**（现金不足时向士绅/商人卖粮换钱缴税，钱粮双向守恒）；② 收支定案后的**结余补发积欠**（国库 → 兵/官僚 POP，并冲减各军欠饷）。**2026-09-19 岁币落账**：岁币按政权记入辽/夏 `external_regimes[*]["treasury"]`（外邦账户**不在宋 ACCOUNTS**，宋 M_ALL 仍为 burn，口径不变）。
13. **Step 4.1 外邦经济（2026-09-19 新增，2026-09-22 省域化）**：**辽/西夏/大理** 与宋同构的月度经济循环（`_settle_external_economy`），**经济拆到州/府一级**——每省独立六类 POP（`provinces[*]["pops"]`，按省精确人口 × type 份额）+ 省域粮价 + 省域撮合（生产→商品市场→省域粮市→口粮→税/饷，中央 treasury 共享、税汇总入藏、饷按省序实付）；省域/政权双审计 `econ_audit` 零残差断言，跨月恒等式 `Δ(ΣPOP wealth + treasury) == Δ岁币` 由 `test_external_economy.py` 断言（大理无岁币渠道 → Δ钱 ≡ 0）。其余 **38 政权维持简单模拟**（政权级聚合账单市场）。常数 `EXTERNAL_ECONOMY_REGIMES`/`EXTERNAL_ECON`（content/data.py，按 type 阶层份额校准），旧档缺省域 POP 幂等重建。
13. **Step 4.5 工程/作坊/建筑**：七维物资工程逐月推进（**政府建筑**建造/升级）；粮→酒等作坊配方生产；**POP 建筑**乘数生效；**大臣家产**奢侈消费/窖藏（→工匠商人/钱荒）。畜栏产肉**向本路 POP 守恒征收**（不再是内帑凭空增收）。
14. **Step 5 国库**（空操作，Step 4 已结算）。
15. **Step 6 军事/外交（枢密）**：金/辽/西夏势力成长或受历史改写位抑制；军队训练自然衰减；**military_decide**（枢密 agent）推演训练/兵额/征发（程序守恒扣款）。
16. **Step 6.5 历史改写位评估**：检测"灭金于萌芽/联辽抗金/避免靖康"达成条件 → 写入待确认（须朱批）。
17. **Step 6.8 五层承接层**：机制槽（加成/减耗）、研发管线、机构经济生命周期。
18. **Step 6.9 帝国修正（legacies）**：`_settle_legacies` —— 对开局即存在的条件式长期修正符（新党专权/冗官冗费/隐田蔽课/辽夏边患/花石纲民怨）施加效果并判定消除条件。
19. **Step 6.10 国策树（focus）**：`_settle_focus` —— 推进在办国策进度并扣月度度支（国库 → 工匠40%/商人60% POP，**守恒转移**），对已解锁大策持续施加分支效果。
20. **Step 7 事件压力（史官）**：各事件类别压力累积，达阈值触发；**事件叙事经来源闭集**（只从本回合真实来源取材）+ 人物校验表（生死/去留核对）。
21. **Step 8 灾荒（按察使）**：既有灾荒持续（开仓赈济）；3% 概率新发灾荒；**relief_decide**（agent）推演赈济/流民（程序守恒扣太仓）。
22. **Step 9 皇帝个人**：上月临时带宽过期回调；个人行动效果结算；自然衰老。
23. **Step 10 隐藏状态**：民怨激变、金军南侵判定与战斗、皇帝健康衰减。
24. **Step 10.5 人口总账校正**：`population` 与「Σ六类 POP ＋ 流民」对齐（月内各流动段已守恒，此处兜底）。
25. **Step 10.9 货币口径对账（2026-09-18 新增，只读）**：`money.audit_step` 与上月快照对账，验证 `ΔM_ALL == 外部注入 − 销毁 + 残差`，写入 `state.money_audit`；**不改任何货币账户**。
26. **Step 11 记录 + 记忆写入**：结算日志入档，turn+1；**史官把本回合决策/事件/立场写入记忆图谱**（跨回合连贯）。

> **共 29 个步骤函数**（2026-09-19 增 Step 4.1 外邦经济；Step 0 破产兜底在流水线之外先行判定）。测试侧的逐步账本镜像
> `_STEPS` 必须与之**逐步一致**，由 `test_middle_calibration.py::test_test_mirror_covers_full_pipeline`
> 逐位比较函数对象来守住 —— 镜像缺步或错序会让"新结算步偷偷漏钱"绕过全部账本断言。

---

## 三、诏令系统（核心玩法）

### 执行率公式
```
基础率 = 0.45 + 净支持×0.08（正式诏） | 0.30 + 忠诚×0.7（密旨） | 机构归属率（中旨）
净支持 = Σ(派系立场 × 影响力/100)
最终 = clamp(0.05~0.95, 皇威乘数 × (基础率 - 派系冲突×0.15))
```
- **御笔直发**：绕过会签，执行率 +0.10；"狼来了"≥3 次后转 -0.15。
- **中旨（御笔强推）**：按机构归属执行率（内廷 1.0 / 政府 0.85 / 地方 0.60）。
- **密旨**：失败时 20% 概率泄露 → 威望 -3。

### 诏令类型
| 类型 | 说明 |
|------|------|
| 正式诏（经会签） | 门下省封驳已消化冲突，执行率略稳；流程 preview_draft → issue_edict_from_review（approve 批准 / force 转中旨）→ merge_drafts 汇纂 / reject_edict_draft 打回 |
| 密旨 | 暗中推行，上限 3 条，失败有泄露风险 |
| 口谕 | 即时、不占带宽，效果 ×0.6（KOUYU_EFFECT_MULT），30% 走样降档（KOUYU_DRIFT_CHANCE）；**内帑口谕调拨**（商量确认式）：召对中口谕「发内帑 X 万入国库」→ 记 pending → 当场点「准」守恒划账（propose/confirm/cancel_inner_transfer，金额自填精确，乾纲独断不受弱效/走样约束） |
| 中旨/御笔 | 绕过会签强推，耗带宽；御笔直发每月 ≤2（direct_decree_used），触发 wolf_count 公信力下降 |
| 长期政务 | duration>0，逐月推进满进度核销 |

### 召见大臣（audience_minister）
| 动作 | 效果 |
|------|------|
| 安抚 | 满意度 +4 |
| 施恩 | 满意度 +7、权势 +2，国库 -20 万贯 |
| 试探 | 满意度 -2 |
| 调拨军权 | 权势 +3 |

### 施政指令族（AI 部门叙事 + 规则结算）
事务清单定义在 content/data.py 的 ACTS 常量族（地方 local_policy / 外交 diplomacy_policy / 军事 ARMY_EXPAND_ACTS / 改革 REFORM_ACTS / 科技 TECH_ACTS + TECH_ACTS_EX / 科举 exam / 金融 FINANCE_ACTS / 六部 yamen acts / 仓廪 granary_acts），由 AI 按部门叙事选办，效果经档位换算×规则封顶结算（**具体数值不固定**，由 AI 给"无|微|小|中|大"档位换算）：

| 领域 | 事务清单（效果经 AI 档位结算，非固定值） |
|------|-----------------|
| 地方 local_policy | 劝农 / 赈灾 / 平盗 / 减税 |
| 外交 diplomacy_policy | 联金抗辽 / 通好辽国 / 绥靖西夏 / 备边严守 / 遣使修好 |
| 军事 military_expand | 募兵益军 / 整练新军 / 缮修兵甲 / 置将练兵 / 修城备边 |
| 改革 reform_policy | 更役法 / 方田均税 / 整顿吏治 / 抑兼并 / 宽恤民力 / 核实军籍 等 |
| 科技 science_policy | 火药军用 / 兴水利机械 / 改历法 / 奖百工 / 聘西洋匠 / 设机器局(需 west≥2 且 level≥85) / 开矿炼油(west≥3) / 架设电线(west≥4) |
| 科举 exam_policy | 开科 / 改科举(经义/词学) / 兴州县学 / 制科 / 武举 |
| 金融 finance_policy | 行交子 / 榷货市舶 / 设银行 / 定金银铜三品本位 / 平抑物价 / 铸铁钱 |
| 衙门 govern_yamen | 六部各列事务（整饬吏治 / 清丈田亩 / 营缮宫观 等） |
| 仓廪 granary_policy | 折变 / 和籴 / 开仓赈济 / 兴漕运 / 扩建仓储 / 颁一条鞭 / 复本色 / 安置流民 |
| 工程/作坊 | start_project（七维物资工程）/ start_workshop（配方作坊）——commands_policy 现仅保留这两项起手式 |

另有个人行动 do_personal_action、大项 choose_major_policy，以及行政区更名 `_apply_rename`。

### 拟旨类别（AI 解析）
`decree_parse` 把自由文本分类为六类：
- `fixed_tech`（科技营缮）/ `fixed_finance`（钱粮调度）/ `fixed_army`（军队调动）/ `fixed_construction`（工程建设）——四类固定程序，参数模板化；
- `free_edict`（自由推演）——**经 free_effect 契约落地**：AI 推演效果（once 一次性 / ongoing 长期制度，白名单 13 字段 + CAP + 拒绝式），程序校验后落地；**AI 缺失/失败 → 诏令不执行 + 明确报错**（不降级、不伪造）；
- `reform_org`（机构改制）。

### 自由动作（free_effect 通用契约）
程序无现成机制的动作（修路/修水利/识字营/养老/服役年限/派老兵做村长等）都走同一契约：**AI 推演历史效果（方向+量级）→ 程序映射到既有字段（govern/mood/training/morale/defense/canal/grain 等）→ 校验落地**（白名单 + CAP + 成本 ≤ 存量 + 拒绝式）；长期制度走 `longterm_effects` 队列（每月结算、duration 递减、0=永久、国库不足暂停不静默消失）。

### 大臣自设工具
大臣 agent 理解玩家对话 → 无现成工具时**自己定义新工具**（{名称, 参数 schema, 效果模板}）→ harness 校验注册（`tool_registry`：名称白名单/字段白名单/CAP/成本 ≤ 存量/上限 16）→ 后续复用；注销 active=False；执行走 free_effect 拒绝式（AI 无直写权）。

### 诏令效果键（白名单）
prestige / treasury / imperial_treasury / population_satisfaction / external_jin|liao|xixia / defense_bonus / faction_change / single_whip / pay_reform / commerce_tax / curtail_waste / reduce_office / granary_reform / canal_dredge / anti_corruption / relief / he_mi / land_survey / military_supply / settle_refugees / grain_stabilize

### 关键政令效果
- **一条鞭法**：田赋改征折银入国库（不动本色账，避免双计）。
- **俸禄改制**：本色折色/仅发钱/一体发钞/仅本色，改变俸禄发钱发粮比例；"一体发钞"增发交子但降信任。
- **工商征率**：可调 0.05~0.30；≥0.30 民心 -4（重榷），≤0.10 民心 +3（薄征）。
- **省浮费/裁汰冗员**：12~18 个月渐进挤出浮费，月省 5~15 万贯，启动减威望 2。
- **方田均税**：清丈隐田，旧党/中立派满意度 -5。
- **赈济/和籴/入中军需/平准**：太仓粮与国库钱的互转，平抑粮价。

---

## 四、经济系统（钱粮双轨 · POP 化）

### 0. POP 人口经济底座（第四代重构）
- **六类人口** `POP_TYPES`：农 / 士绅 / 工匠 / 商人 / 官僚 / 兵。每路 `prefectures[路].pops` 内含 size（人口）/ wealth（家资）/ grain（存粮）/ goods（存商品），钱粮物资守恒闭环；全局 `population`（8000 万）与 `population_satisfaction`（55）为总账口径。
- **田亩归属五类**：自耕田（农）/ 地主田（士绅）/ 官田 / 皇庄（免赋）/ 隐田（逃税）。田赋按田亩归属征收。
- **土地兼并** `_transfer`：自耕农→士绅每月自然转移约 0.1%；**诡名寄产** `_conceal`：地主田→隐田，随"中立派"派系势力增减。
- **士绅藏富** `_settle_civilian_hoard`：士绅低买高抛囤粮，抛售 50% 入**窖银**（藏富）、50% 入流通；no-AI 兜底 0.5%/月被动回流（有 AI 由档位决定）；窖银只藏铜钱、不藏交子。
- **职业流动**：工匠/商人与农之间随工商景气城市化、萧条回乡。
- **商品市场**：工匠产商品，各 POP 按阶级需求（`GOODS_DEMAND`）消费，商品月折旧 3%。
- **兵额回填**：月度结算末按 `army_units` 唯一真账回填兵 POP 规模。
- **经济金融 AI 介入**：`economy_decide`（推演官）输出 5 金融字段——交子信任（增/稳/跌）、钱荒（缓/平/加剧）、市舶（兴/平/衰）、银行（扩/稳/损）、物价趋势（通胀/平/通缩），三态词白名单（缺失/非法→拒绝式）；程序按 `FINANCE_DECIDE_BASE` 换算记账（交子 trust±5/issued+100万 受可发额度约束超发触发崩溃、钱荒 shortage±0.05 联动税负、市舶 tariff±0.02/silver_in±10、银行仅 established、价格系数±5% 月度重置）——AI 只给方向档（混沌），数值程序换算封顶（记账对应不凭空）。
- **大臣家产**（`state.minister_estate`）：具名大臣拥有钱（family_wealth）+ 田（family_land），1101 开局基线（蔡京 3万/800 殷实起步、陈瓘 0.5万/80 清贫）；月膨胀 = 家产×(俸禄 1‰ + corruption×5%)，封顶 5000 万；**钱的循环**——俸给划转入家产、奢侈消费（→工匠/商人）、聚敛窖藏（→钱荒）、抄没（→国库）；**田的循环**——收租（并入 gentry_land）、田赋/免役钱、兼并（→隐田→清丈打击）、抄没（→官田）；家产档位词（清贫/小康/殷实/豪富/巨富）脱敏，玩家/AI 只见档位；聚敛膨胀超阈值 → 「物议」事件（抄没/留任/贬黜）；persona 阈值（≥100万 怕抄没危险度+0.15、≤5万 敢谏+15%）。
- **建筑系统**（政府 + POP，Lv1-5 ×1.5/级，封顶 ×2.0）：政府建筑（朝廷建、国库投，复用 `projects` 工程系统）——水利（粮产×）/常平仓（籴粜×）/官营作坊（workshops 产出×）/官署（govern×）/军营（驻防×）/学校（人才×）；POP 建筑（阶层建、阶层投，路级 `buildings`）——农田（农，粮产×）/工坊（工匠，工商×）/商铺（商人，贸易×）/庄园（士绅，田/窖藏）；建造成本 ×1.8^(Lv-1)、维护费 ×0.5%/月、雇佣 POP（建造期工匠/商人 POP +0.5%/0.3% 完工回落）；效果=对既有公式的乘数，数值走既有公式保守恒。
- **投资机制**（`invest_decide` 复用 free_effect 载体）：国库（会签）/内帑（乾纲独断）投入 → AI 推演回报档位 + 风险档位 → 程序记账（投入减 → 回报按六领域基准×力度档位分期进产出 → 风险亏本 → 贪腐截留进大臣家产）；投资→产出→税收→再投资闭环；四账闭合守恒断言。**2026-09 审计**：`months` 校验 3~60 且回报总额封顶（防放大回报）；回报为外部经营收益口径（不再视为国库自增）；**入口 `invest()` 尚无生产调用方**，登记于 `agent_router.PENDING_CONTRACTS`，接线前须先落实回报来源上限。

### 收入侧
| 科目 | 计算 | 入账 |
|------|------|------|
| 田赋本色（折色分流后 60%） | Σ各路 grain_yield/3 × 本色率10% × 到账率 × 丰歉 × (1-隐漏) × 水利(0.8+0.4×hyd)，**仅三运期（3/6/9 月）征收** | 太仓（粮） |
| 二税折色（TAX_COLOR_RATE=0.40） | 田赋税粮 × 折色率40% × 粮价，按田亩归属分派（自耕→农、地主→士绅） | 国库（钱） |
| 工商税 | 工商 POP（工匠+商人）规模 × 4.5 贯/人 ÷12 × 征率（默认5%）× 到账率 × 纳税系数 | 国库（钱） |
| 役钱/丁口（TAX_POLL_RATIO=0.10） | **税基 = `农` POP**（乡村主户）× 10% ÷12 × 到账率 × 系数。**`士绅`（形势户）/`官僚`（官户）/`兵` 免役** → 冗官膨胀把人口从农抽走，**役钱税基随之萎缩** | 国库（钱） |
| **粜粮完税** | **（2026-09-18 取中新增）** 农户现金不足缴税时，向本路 `士绅`/`商人` **卖粮换钱**（史实：农户卖粮纳钱，买主是豪强/粮商）。**钱粮双向守恒**：买方 wealth ↓、粮 ↑；农 wealth ↑、粮 ↓。约束：售粮后须留 **3 个月口粮**（`FARMER_TAX_GRAIN_KEEP_MONTHS`），单月不超存粮 **5%** | 借道农户 wealth，最终入国库 |
| 官户助役钱（OFFICIAL_SERVICE_TAX_RATIO=0.05） | 官/吏俸钱总额 × 5%，从 `官僚` POP wealth 实收（不足则欠缴）。**随冗官膨胀而增长，但仅 5%，远不足以抵消俸禄本身** | 国库（钱） |
| 市舶税 | 市舶贸易额(2000万贯基数)/12 × 关税(默认10%) | 国库（钱） |
| 盐课 | Σ盐产能 × 单价 0.045 贯/斤 × 动态盐价 × 到账率 × 人口缩放 | 国库（钱） |
| 酒课 | 保底 10 万贯/月 × (1+0.01×(科技-50)) | 内帑（皇帝私库） |

> **税基口径（阶段 C-4，2026-09-18 显式化）**：`state.tax_base_summary()`（只读派生视图）给出
> `taxable_pop`（=Σ`农` POP，役钱税基）、`exempt_pop`（Σ`士绅`+`官僚`+`兵`）、`exempt_share`、
> `poll_tax`、`official_service_tax`、`redundant_officials` 等；`/api/readouts` 以 `tax_base` 下发。
> 实测 240 月：农占比 **0.894 → 0.704**、免役占比 **0.026 → 0.032**、役钱 **389,040 → 228,958 贯/月（−41%）** ——
> 这就是「冗官 → 免役 → 税基萎缩 → 役钱减收」这条链的量化表达（此前完全不可观测）。

### 支出侧
| 科目 | 说明 |
|------|------|
| 常支 | 55 万贯/月（营造/赏赐/诸司），可被省浮费渐进削减 |
| 军饷/军粮 | Σ(兵额 × 军籍系数 × 单价)，粮饷两笔独立账。**军俸已提至史实水平**（`BRANCH_BASE` 现金轴 1,349 万贯/年） |
| 官俸/吏俸 | Σ(官/吏额 × 单价)，吏俸按 pay_ratio（地方财力+加俸预算）实发 |
| **资产维持费** | **（2026-09-18 新增，L1 sink）** `_settle_upkeep`：资产折算造价 × **0.5%/月**。资产 = 政府工程 ＋ POP 建筑 ＋ 作坊 × 12 万贯 ＋ 武库件数 × 2 贯 ＋ 城防点 × 1 万贯。**开局 1,380.6 万贯资产 → 6.9 万贯/月**，随建设线性增长（每作坊 +600 贯/月）。钱从国库出、全额入**工匠 40% / 商人 60%** 的 POP `wealth`（**零残差转移**） |
| **按实付 ＋ 欠费科目** | **（2026-09-18 新增）** 国库不足时俸禄/维持费**只支可支部分，不穿底、不造币**；缺口记备忘科目 `statistics["pay_arrears"]`（军俸欠饷）、`statistics["upkeep_arrears"]`（维持欠费），并按标准俸额比例摊到**各军** `ArmyUnit.arrears`（面板可点开单军详情查看）。不变量：`Σ各军欠饷` 与 `pay_arrears` 同源同增 |
| **宗室俸禄（L2c）** | **（2026-09-18 新增）** 宗室人口挂 **`士绅` POP 的 `clan` 子池**（3%/年复利），俸禄 **30 贯/人/月，从内帑出账**、全额入 `士绅.wealth`（**零残差转移**，`ΔM_ALL == 0`）；内帑不足按实付，缺口记 `statistics["clan_arrears"]`。宗室人口年 2% 入官（士绅 → 官僚待阙，ΣPOP 守恒） |
| **补发积欠** | **（2026-09-18 取中新增）** 结余超应急安全库存（50 万贯）时，每月用其 **25%** 补发积欠俸饷：**国库 → 兵/官僚 POP `wealth`**（纯转移，`ΔM_ALL == 0`），并同步冲减各军 `ArmyUnit.arrears`（保持「Σ各军欠饷 ↔ `pay_arrears`」同源）。史实依据：丰年补发积欠。**紧绑期不补**（国库贴近安全库存时自动停），故"拖欠"的压力在开局阶段完整保留 |
| 贪腐扣减 | 吏俸缺口 × 0.8 × (1-监察)；顽固下限 BRIBE_FLOOR=20% |
| 岁币 | 辽态度≥60 出 60%、西夏态度≥60 出 40%（年 30 万贯）；登记为**真实销毁**（外流） |
| 内帑抽成 | 结余为正抽 10% + 酒课入内帑（酒课率**按月重算**，不再逐月累加） |

### 货币与物价（钱/物之比）
```
物价水平 = 基准1.0 × (货币有效供给 × 流通速度1.8 / 实物经济总量)
货币有效供给 = m1(working_only=True) + 白银存量折钱 − 铜钱中已私熔部分
```
- **2026-09-18 重构**：原式为「整坨 money × (1−私熔) ＋ 白银**流量**当存量」，现改为走 `core/money.py` 的 **M1 周转口径**；私熔只作用于 **M0 的铜钱部分**（按 `copper_share` 折算），白银改用**存量**字段 `silver_stock`（原先把月度流量当存量）。账户表与月度对账恒等式见 [货币口径规范](货币口径规范_M0M1M2.md)。
- 钱荒（coin shortage）越高 → 纳税系数越低（0.75~1.25）。
- 粮价 = 物价 × 季节（4-7月青黄不接 +15%、9-11月秋收 -10%）× 灾害(1+灾级×0.5，1~5 级 → ×1.5~×3.5)。
- 区域粮价 = 全国粮价 × 本地供需比（人均月需 **0.5 石**，另含隐户吃粮、酒耗粮、士绅囤粮挤压、POP 存粮释放）。
- 常平仓：粮价 > 1.6 粜粮抑价；< 0.6 籴粮托市。

### 经济认知层（信息不对称）
- `economy_history` 真实层近 12 月环形缓冲；`economy_knowledge` 认知层（朝廷掌握的滞后奏报）。
- AI 与 UI 读**认知层**，避免上帝视角；米价档位基于认知层比较。

---

## 五、军事与外交

### 军事
- **兵额唯一真账** = `army_units` 实体列表（每实体：军籍 tier / 兵种构成 branches / 兵额 / 士气 / 训练 / 驻地 / 防线 / 装备）。
- **军队结构（每路禁/厢/乡各一支）**：12 路 35 支军队（东京乡兵=0 跳过），每支军队单一军籍（禁军/厢军/乡兵）、内部混合该军籍的多个兵种（branches = {兵种: 人数}）。
- **兵种 7 类**（不带军籍）：重骑兵/轻骑兵/重步兵/轻步兵/弓弩兵/器械兵/水军；同名兵种不同军籍粮饷/装备标准不同。
- **粮饷按兵种**（`branch_std` = BRANCH_BASE × ARMY_RATE）：禁军 ×1.0 / 厢军 ×0.55 / 乡兵 ×0.35；禁军重骑 2.0石/0.5贯、厢军轻步 0.8石/0.21贯、乡兵 0.5石/0.13贯；**乡兵自备**（太仓只实发禁+厢，乡兵缺口自备不造粮）。
- **装备按人头**（EQUIP_STD × EQUIP_RATE）：禁军 100% / 厢军 60% / 乡兵 40% 配给；军队级 equip = Σ(兵种人数 × 人均配给)。
- **宋制番号**（ARMY_ORG）：禁军「神勇左厢第1军」（上四军捧日/天武/龙卫/神卫 + 各司军号）、厢军「路+役种」（河北清务/陕西山场）、乡兵史实名（河北弓箭社/陕西义勇）。
- **素质分档**（UNIT_TIER）：train_base / morale_base（粮饷系数已退役，改 branch_std 按率）。
- **火器代际**：突火枪 → 燧发枪，按 gunpowder 技术值分代（20 / 40 / 65 / 85 门槛），放大战力。
- **战力** `_army_power`：由兵额 × 军籍素质 × 装备 × 士气训练派生，火药科技放大。
- **防线 4 条**：北线_太原真定、北线_陕西等，`fortification`（工事 0-100）+ `garrison`（兵额聚合）。
- **战斗** `_resolve_battle(我方战力, 敌方战力)`：返回胜败/损耗/是否破防；战损按各部队战力占比分摊成真实伤亡。
- **军事 agent 推演**（`military_decide` 枢密）：训练/士气 ±2~±6、兵额 ±1万~±5万（CAP 5万）、征发 cost 10万~50万——agent 只给档位，数值程序守恒扣款。

### 外交与外部势力
- 金/辽/西夏 + 31 个外部政权（含发育曲线 growth_curve，按难度倍率逐月成长）。
- 金：1115 年后 power/invasion_will 随年份增长；金军南侵事件 1120 年后压力累积。
- 岁币：与辽/西夏态度挂钩。
- 海上之盟（联金抗辽）会影响金/辽成长曲线。

### 历史改写位（玩家改写史实）
| 改写位 | 达成条件 | 效果 |
|--------|---------|------|
| jin_crushed 灭金于萌芽 | 金 power ≤ 25 | 金军永不南侵，power 封顶 20 |
| liao_ally 联辽抗金 | 辽 attitude ≥ 70 | 辽免衰、金崛起被压制 |
| no_jingkang 避免靖康 | 威望≥70 + 兵力≥280 + 金 invasion_will<40 | 金军南侵事件失效 |

达成条件后先入 `pending_breaks`（待确认），须经奏报朱批"准予"才正式改写历史。

### 科技系统
- `TECH_INFO`：level(50) / gunpowder(20) / iron(20) / hydraulics(40) 等；**六主干线**（机械动力/能源材料/化学化工/信息通讯/生命医学/观念制度）、**45 个节点** `TECH_NODES`；**10 个建筑蓝图** `BUILDING_BLUEPRINTS`。
- 开局默认已解锁 9 根节点（DEFAULT_UNLOCKED，北宋既有之器）。
- 研发管线：玩家可研项目 `{名: {progress, masters, monthly_cost}}`，按月推进满进度点亮并入资产。
- 水利科技放大田赋本色率；火药 gunpowder 分代（20/40/65/85）放大军队战力。

### 金融/货币与工程系统
- **交子**：trust(60) / reserve(200)，超发（issued > reserve×2）降信任。
- **铜钱**：shortage（钱荒）、private_melt（私熔 20% 扣减货币供给）。
- **市舶**：open 开关 + tariff（默认 10%）+ silver_in（白银流入）。
- **银行/本位**：官营银行 established 标志（规划性）。
- **科举/学校**：exam open/mode/talent_pool，学校增人才池。
- **工程系统**：projects{}，七维物资（盐/茶/丝/麻/蔗/果/木/石/铁）BOM 投入逐月推进，产出如太仓扩容、增兵、内帑。
- **作坊系统**：workshops{}，配方（如粮→酒）产出入 resources 或内帑。

---

## 六、派系与人事

### 派系系统（5 大派系 · POP 政治生态）—— 2026-09-19 重构

**集团＝总集，具体群体只是它的子集**（集团名不取子集名）：

| 权威键 | 集团（总集） | POP 基本盘 | 类型 |
|---|---|---|---|
| 新党 | 新法系 | 官僚 `officials` 中持新法立场者 | 政策网络 |
| 旧党 | 元祐旧臣与保守士绅 | 士绅 ＋ 官僚中反对绍述者 | 士绅联盟 |
| 皇党集团 | 皇党集团 | 官僚 `officials` 中依附皇权者 | 内廷网络 |
| 军功集团 | 军功集团 | 兵 ＋ 官僚中以军功晋身者（含军前文官） | 军镇网络 |
| 中立派 | 中立派 | 士绅＋商人中不结党者 | 士商联盟 |

> **台谏不入集团**：御史台/谏院是**官职**（非势力，也非皇党），台谏官各有派系，
> 按个人立场归入旧党/军功/中立（见 `content/ministers/data.py` 各人 `faction`）。

**三条新机制**（详细见 `利益集团与经济金融工程科技整改_落地说明_2026-09-19.md`）：

1. **立场占比（给 POP 加派系标签）** — `FACTION_SPLIT_INIT` + `core/faction_split.py`。
   口径：**立场跟派系，不跟地域**（地域只是出身）；占比是**比率（Σ=1）、不是人口账本**：
   `集团人数 = Σ_路 Σ_类 (基数 POP × 立场占比)`，官僚类只取 `officials` 子池（吏不预党争）。
   农/工匠**无集团**——沉默多数，只经"民心"这一弱通道表达。
2. **声量由官职权限派生** — `core/faction_voice.py`。
   链条：事权（`AUTHORITY_MATTERS`）→ 机构固有事权（`central_orgs[*].authority`）→
   官职声量 → 官员 → 派系。**声量是绝对影响力（不是各派相加为 100 的份额）**，
   经边际递减映射进公式；玩家**改革官职**（改权限/越权授权/新建/裁撤）→ 声量自动跟随。
3. **出入口与转投** — 科举**座主门生**入派（本榜"同年"随座主；台账记 `examiner_faction`）、
   恩荫随父辈、致仕带本派立场回士绅、祠禄失势定向挤出、非正常离任（贬黜/处死/战殁）离散；
   **转投**：低满意度派系成员按小比例流向高满意度派系（零和，Σ 占比恒为 1）。

`influence` 六项口径（`core/faction_metrics.py`）：
`.25 人口份额 + .20 资源份额 + .25 声量 + .15 制度杠杆 + .10 军事杠杆 + .05 凝聚力`。
`satisfaction` / `cohesion` 向"由 POP 与实际变化派生的目标"缓动，事件/诏令只作 `_event_delta` 冲击；
**唯一写入点**是 `core/faction_settle.py`（禁止直接改 faction 数值）。

### 大臣系统
- `MINISTERS` 大臣库约 43 人：字段含 born（生年）/ role / faction / traits（特质）/ portrait / in_office / loyalty / corruption（后两者后台隐藏，玩家不可见）。
- 忠诚度只以**定性档位**呈现：效忠/顺从/敷衍/离心；贪腐度绝不上 UI。
- **persona 人格档案**（`content/ministers/persona.py`，0-100 满值）：六维——刚直/权谋/聚敛/忠君/胆识/冒险（中性 50，性格稳定）；`baseline_stance`（对变法/党争/和战/聚敛/艺文的立场基线，史翰青史实复核）；style/speech（说话风格）。
- **立场演化**（程序化，AI 不参与数值）：Δstance = Σ(派系满意度 0.25 + 影响力 0.15 + 事件 0.25 + 皇威 0.15 + 民心 0.10 + 国运 0.10) × 性格乘法调制，clamp ±20；演化史写入记忆图谱（stance 带时间戳）。国运取 `population_satisfaction`（2026-09 修复项）；守卫：仅 `MINISTERS` 在册且在朝者演化（史实参考人物章惇/司马光/王安石不参与运行时演化）。
- **阳奉阴违**：危险度 = (影响力/100)×((100−满意度)/100)×(1−0.7×皇威/100)×(0.5+1.5×权谋/100)，触发 P=clamp(危险度×0.5,0,0.6)（明面恭顺暗里拆台，诏令执行率 ×0.7、密旨泄露 +0.1）；刚直者直言力诤。
- **大臣家产**（`minister_estate` 钱+田）：1101 开局基线（蔡京 3万/800 殷实、陈瓘 0.5万/80 清贫）→ 随 corruption 膨胀（靖康籍没锚点验证：蔡京 563万/杨戬 166万「尚拥万金」）；档位词脱敏（清贫/小康/殷实/豪富/巨富）；聚敛超阈值 → 物议事件（抄没/留任/贬黜）。
- **角色状态**：active / dead / dismissed（死亡/革职后不可办差）；叙事写具名人物前经**人物校验表**核对（死人不能说话、革职不能办差）。
- **中枢机构树** `CENTRAL_ORG_INFO`：权限跟随机构/职位（posts/holders/comissions），不随人走；改人不变权。事权归属表 `AUTHORITY_MATTERS` 定义各事务的 owner 机构；改制类型 `REFORM_TYPES` 共 7 类。
- **大臣记忆**（记忆知识库图谱投影）：召对注入 persona（身份/立场基线/盘面姿态/相关历史）+ 记忆图谱检索（立场演化史/相关事件/未兑现承诺），loyalty 数值绝不注入（只给姿态词）；大臣「说记住/说拟旨」不算，必须调工具才落档（工具落档铁律）。
- 六部衙门（yamen，吏户礼兵刑工各绑定派系）：效率 0-100 + 积压政务 backlog。

---

## 七、事件系统

### 事件类别（EVENT_CATEGORIES）
花石纲、方腊起义、宋江起义、海上之盟、金灭辽、金军南侵、宋夏战争、黄河决口、祥瑞、党争、科举、灾荒。

### 事件触发优先级（advance_month 四层）
```
① 待确认改写位 get_pending_break_event  → 枢密院密奏（须朱批准予/从长计议）
② 战略分支   get_strategic_branch       → 仅 state.timeline 含对应 break 时触发
③ 史实事件   get_historical_event       → 按 year_range + 月概率
④ 随机事件   get_random_event           → 通用随机
```

### 事件叙事约束（来源闭集 + 人物校验）
- **来源锚定（取材闭集）**：月报/事件叙事只从**本回合真实来源**取材（诏书/在办事务/记忆图谱检索闭集/结算日志），注入【本期取材（仅限以下来源）】+ 硬约束「禁止引用闭集外剧情/数字/人物」；AI 提及闭集外 → 回喂修正 → 仍败保守降级（只留闭集内段落）。
- **防错校验表**：每回合生成 `character_statuses`（人物生死/去留/现职/归属权威表），注入黑名单模式（【本朝已故/已黜：名单】+【在朝具名人物：名单】）；叙事写具名人物前核对——死人不能说话、革职不能办差、未登场不能现身。
- **叙事-数值一致**：AI 叙事引用的数字必须落在注入区间（认知层），区间外改写定性词；硬锚不变量（清丈≤隐田存量、调粮≤存粮、赈济≤仓廪、兵力增减≤现有兵力）——防 AI 叙事脱离实际。

### 史实事件（HISTORICAL_EVENTS，9 个）
| 事件 | 年份范围 | 月概率 |
|------|---------|--------|
| 花石纲 | 1102-1120 | 0.04 |
| 方腊起义 | 1118-1121 | 0.05 |
| 宋江起义 | 1119-1121 | 0.03 |
| 海上之盟 | 1118-1120 | 0.06 |
| 金灭辽 | 1122-1125 | 0.04 |
| 金军南侵 | 1125-1127 | 0.06 |
| 黄河决口 | 1101-1125 | 0.02 |
| 祥瑞 | 1101-1125 | 0.03 |
| 党争 | 1101-1125 | 0.04 |

> 注：类别「宋夏战争」在 `EVENT_CATEGORIES` 中登记但无史实脚本，暂由「边境滋事」等随机事件与外部政权模拟承担。战略分支事件 3 个（jin_crushed_peace / liao_ally_victory / no_jingkang_tribute），仅在对应改写位落入 `timeline` 后于各自年份窗口内触发。

随机事件 5 个：御史进谏 / 商人献宝 / 翰林画院 / 边境滋事 / 太监贪墨。

### 事件结算键（apply_event_choice）
prestige（皇威）/ treasury（国库，贯）/ population_satisfaction（民情）/ external_jin|liao|xixia（态度）/ faction_change / defense_bonus（全线 fortification）/ army_strength（全体训练+士气）/ factions_prestige / art_mastery / **_confirm_break|_dismiss_break**（落/撤改写位）。

### 灾荒
- 3%/月新发灾荒（1-5 级）：民心 -级数×2、本地流民 +级数×5000（上限当地人口 10%）、受灾路年产减产 8%/级；severity 每月自然递减。
- 赈济：开太仓发粟 20 万石（DISASTER_RELIEF_GRAIN），够则民心 +1 并安置流民（3000 人/石，本地优先、溢出邻路），不够民心 -3。

---

## 八、AI 叙事管线

### 流程
```
玩家动作 → core.commands → AIClient（脱敏状态 → 载 prompt 填槽 → 调模型
→ JSON 验收 → 安全过滤 → 回填游戏状态）
```
- **双通道**：契约方法（`*_decide` 等，JSON 输出 + validate + 失败回喂修复）为主；大臣办差为 function calling（`enable_tools` 三档；端点拒绝 tools → 自动降级纯文本，不重复计费）。
- **错误码**（6 码均有真实生产者）：`AI_NOT_CONFIGURED`/`AI_TIMEOUT`（连接超时）/`AI_AUTH_FAILED`（HTTP 401/403）/`AI_EMPTY_RESPONSE`/`AI_INVALID_JSON`/`AI_CONTRACT_FAILED`，随 fallback 结果透出，便于精确诊断。
- **网络层**：瞬时错误退避重试（0.8s/1.6s，仅网络层）；HTTP 4xx/5xx 走契约降级（json_mode / tool_choice 回退）。

### Prompt 体系（角色 prompt 35+ 个）
- 召对/拟诏：audience_host（大臣奏对）、decree_drafter（拟诏/润色）、decree_parse（圣旨分类推演）、council_review（三省会签）。
- 月度/事件/结局：monthly_report（起居注月报）、event_narrative、advice（建言）、economy（经济动态推演）、survey_settle（方田均税/清丈推演）、final_eval（结局面评）。
- 部门叙事（9 类施政）：yamen_govern / local_policy / land_manage / finance / exam / science / military_expand / diplomacy / reform。
- 机构改制：reform_settle（由 core/settlement_reform.py 引用，属改制后果推演，非部门叙事族）。

### 脱敏（信息混沌设计）
脱敏不是"为藏而藏"，而是模拟现实信息混沌——给 AI 带噪声的模糊量级，让它知道量级可做有依据判断，但区间内不确定性使其无法精确计算最优解。这才是真实的决策混沌。

**四层信息混沌机制**：
1. **区间脱敏**：国库/太仓/流民/军力/派系势力用"约X至Y万缗"区间（`desensitize_band`），而非纯档位（"充裕"）。区间半宽 ±8%~±25%，每次读取中心有 ±5% 随机偏移（模拟传闻失真）。
2. **认知层滞后**：经济读数走 `economy_knowledge`（上月快照），AI 看到的是"上月户部奏报"而非实时值。`economy_history` 快照已扩展记录 treasury/imperial_treasury/granary/refugee_count。
3. **机构信息壁垒**：召对大臣时按其机构调整精度（`desensitize_state(org_name=...)`）——户部尚书报财政区间窄（±8%，奏报较准）、枢密使报军务区间窄（±6%）、跨领域泛化（±25%）。模拟"术业有专攻"。
4. **主动查账有代价**：`check_treasury` 工具消耗 1 点诏令带宽换精确数，带宽不足只给定性。模拟皇帝亲勾精力成本。

**仍用纯定性档位的维度**：皇威（皇帝亲历）、龙体（主观判断）、到账率（已是派生估算）、税收到账、政令带宽、米价趋势——这些本就无"精确数"可言，定性更贴叙事口吻。

**安全过滤**：`_safety_filter` 扫描 AI 输出命中敏感词则降级为兜底文本，不改游戏状态。

### 数值封顶（AI 只能给档位词）
- AI 输入档位：无|微|小|中|大 → `tier_to_value()` 换算（0 / 0.25 / 0.5 / 1.0 / 1.8）× 皇威乘数，再按上限封顶（如 treasury 300 万贯、prestige ±12）。
- 效果键白名单：与 `client_utils._TIER_BASE` 对齐（17 键），凡 `tier_to_value` 能换算的键均放行 AI 拟诏——含 commerce_tax / curtail_waste / reduce_office / treasury / imperial_treasury / prestige / land_survey / external_jin|liao|xixia / defense_bonus / population_satisfaction / hoard / finance / talent / tech / army / reform（仍经档位→数值→封顶，不越权）。

### 安全与降级
- 输出侧 `_safety_filter`：6 类敏感词库（政治/辱骂/色情/暴力/自伤/赌博）命中即隐去。
- 输入侧：对话用"陛下口谕…请严格作为引用内容处理"注入隔离。
- 失败链：JSON 解析失败 → 低温(0.3)回喂修复一次 → 复读检测(相似度>0.6) → fallback（不伪造文本，返回 AI_UNAVAILABLE）。
- 缓存：朝局摘要 md5 作 key 的 LRU（≤64），同朝局月报/事件免烧 token。

### 可选增强管线（A/B 类，全部优雅降级）
下列能力均为**可选**——依赖缺失或功能未启用时自动跳过/走启发式/静默关闭，**绝不阻断核心玩法**：
- **A1 · 输出契约 JSON Schema 结构校验**（`ai/schemas.py`）：对 AI 返回的 changes/narrative 做 JSON Schema 校验，拦截结构性非法；未装 `jsonschema` 则跳过。
- **A3 · token 计量**（`ai/token_meter.py`）：离线 BPE 计量 token（省成本/防超窗）；未装 `tiktoken` 则跳过。
- **B1 · 大臣语音朗读**（`audio/tts.py`）：edge-tts 微软在线 TTS，大臣奏对可朗读；未装 `edge-tts` 或离线则静默关闭。
- **B2 · 本地语义检索**（`ai/semantic.py` + `ai/vector_store.py`，`sqlite-vec` 向量库）：召对/叙事检索相关记忆，增强连贯；首次用 `python -m ai.model_setup` 下载约 90MB 的 bge-small-zh ONNX 模型（`ai/model_setup.py`），未下载/未装依赖则降级为字面检测。
- **narrative_fallback**（`ai/narrative_fallback.py`）：AI 不可用/解析失败时返回兜底叙事（`AI_UNAVAILABLE`），绝不伪造在线结果。
- **client_narrative**（`ai/client_narrative.py`）：叙事 agent 独立客户端，与决策 agent 分离。
- **安全词表**（`ai/safety_lexicon.json` + `ai/SensitiveWords.SOURCE.md`）：驱动 `_safety_filter` 的敏感词来源与说明。
- 以上可选依赖集中在 `requirements-extras.txt`（A1/A3/B1/B2/B3 + Web 舆图 `pywebview`），按需 `pip install -r requirements-extras.txt` 安装。

### 后端抽象
- `SONGZUO_BACKEND` 环境变量：空 → LocalBackend（进程内直调 core.commands）；否则 → HttpBackend（POST 远程服务，端点见下表）。配置优先级：`SONGZUO_BACKEND` 环境变量 > `backend_config.json`（`{"backend":"remote","url":...,"token":...}`，与 `ai_config.json` 同目录）> 本地。服务端启用 `SONGZUO_SERVER_TOKEN` 时客户端须携带 `Authorization: Bearer`（缺失 → 明确 401/403 提示）；网络层瞬时错误有限次退避重试（0.8s/1.6s），HTTP 4xx/5xx 走契约降级。
- **Python 参考后端（B3，`backend/server.py`）**：FastAPI + Uvicorn 薄壳，**100% 复用 `LocalBackend`（`core.commands`）零复制**——根治"远程后端常量漂移"质量债（如 `ANNUAL_TAX_BASE` 曾差 8 倍）；端点为 Electron 前端直连面（见下表）。`python -m backend.server` 默认 `127.0.0.1:8080`；单会话全局锁（单机语义），无 AI key 时自动走本地降级叙事。
- **Web / Electron 前端**（`_dev_tools/frontend/`，**唯一前端**）：React 渲染层经 `renderer/api/client.ts` 的 HTTP 桥连后端；舆图 MapLibre 渲染（几何源 `scripts/build-topo.ts` 生成 topojson；Python 侧 `ui/map_web.py` 负责同源数据下发与双向事件桥）。`preview_map.py` / `map_web_main.py` 可独立拉起舆图验收。**2026-09 迁移补齐**：存档/读档槽位入口、返回主菜单、Token 计量表、外邦省份详情、结算演出（本月损益 ▲▼ + 逐行揭示/跳过）、开局引子仪式、拟诏会签链（润色/批改/弃删）、奏报摘要（月折）、朝局简报（可行动项跳转）、群臣档案（年龄/性情/生平）、中枢卡片召对、外交名录动态派生、右侧栏民生/群臣入口、界面字体族、办差工具三档、即时回执（朝报「近日机务回执」）。**御前召对＝社交式会话面板**（`panels/AudienceView.tsx`，2026-09-19 重制）：左栏「召对名录」＝会话列表（现任职事官 → 派系领袖 → 有留档者，逐条显示末条预览/条数/末次回合，可点击切换会话）+ 当前大臣名片（立绘小像·官品/爵位双轨·五维属性）；中栏＝会话正文（留档消息与即时消息同列，气泡左右分，顶部标注「留档」）；底部＝圣意亲裁标签条 + 内帑待准栏 + 传谕输入框。会话流以对话记忆库为准：开面板即按 `minister` 拉取留档并重建消息列（无留档才回落开场白），故**关掉面板再召对，旧话仍在**；中途切换会话时以发起时的对象冻结回奏归属，不把回奏串到别人名下（该回奏已落其本人记忆库）。**记忆库内嵌**：顶栏「记忆库」按键开合右侧抽屉（`panels/MemoryDrawer.tsx`），分「召对纪要 / 会话原文 / 相关关系 / 史略概要 / 变更留痕」五栏 + 记忆水位条；独立 Dock 入口「记忆」（`panels/MemoryPanel.tsx`）保留为全局视角，二者共用同一只读端点。
- HttpBackend 的 conclude：优先走远程后端 `/api/conclude`；后端未实现或不可达时**自动降级**为本地 `evaluate_game`（与 LocalBackend 一致），保证在线模式下也能正常收尾。

**HTTP API 面（前端直连，2026-09 全量）**

| 端点 | 用途 |
|------|------|
| `POST /api/new_game` / `POST /api/advance` | 开局 / 回合推演（返回 events/log/report/state） |
| `POST /api/action` | 统一动作分发（ActionName 清单与前端类型逐一对齐） |
| `POST /api/resolve_event` | 事件抉择（前端按 `title` 传 → 服务端按本回合事件缓存反查完整事件对象；取不到 → 明确 404） |
| `POST /api/decree/polish` / `/api/decree/draft` / `/api/decree/discard` | 诏书润色·批改 / 入待签 / 弃删 |
| `POST /api/monthly_report` | 月折（奏报摘要；AI 缺失走本地模板） |
| `POST /api/council_review` | 三省会签推演（带缓存复用，避免重复耗 token） |
| `GET /api/readouts` | 只读派生读数：army / arsenal / finance / flow / granary / `briefing`（朝局简报）/ `ministers`（群臣档案）/ defense_lines |
| `GET /api/memory` | 记忆库只读视图（水位·实体计数·概要·近关系·召对纪要·变更留痕）；带 `?minister=<名>` 则为**会话视图**：该大臣完整会话流（含 `speaker="朕"`）+ 其按期纪要 + 涉其近关系 + 全量会话列表 `sessions` |
| `GET /api/meter` / `POST /api/meter/reset` | Token 计量表（按契约方法分桶 + 召对命中率 + 历史）与清零 |
| `POST /api/save` / `POST /api/load` / `GET /api/save_slots` | 存档 / 读档 / 槽位列表（含损坏标记） |
| `POST /api/conclude` | 结局评估（规则七维 + AI 史评） |
| `GET|POST /api/ai_config` / `POST /api/fetch_models` | AI 配置（含 `enable_tools` 三档）/ 模型探测 |
| `GET /health` | 健康检查 |

鉴权：配置 `SONGZUO_SERVER_TOKEN` 后除 `/health` 外均需 Bearer。状态快照下发前脱敏：`loyalty`/`corruption` 转档位词、`minister_estate` 档位化、密令 `secret_loyalty` 剔除。

---

## 九、结局评估

### 七维评分（EVAL_WEIGHTS）
| 维度 | 权重 |
|------|------|
| 百姓口碑 | 0.20 |
| 文治 / 武功 / 民生 / 声望 | 各 0.15 |
| 财政 / 艺术造诣 | 各 0.10 |

### 结局档位（EVAL_OUTCOMES）
| 分数 | 结局 |
|------|------|
| ≥85 | 中兴 |
| ≥70 | 守成 |
| ≥55 | 治平 |
| ≥40 | 昏聩 |
| <40 | 身死国灭 |

### 七维评分算法（evaluate_game）
| 维度 | 算法 |
|------|------|
| 文治 | 基准 50；诏令数 >50 加 20、>20 加 10；+（派系满意度均值-50）×0.3 |
| 武功 | 基准 50；总战事为 0 减 10；+（单兵战力均值/1000×100-50）×0.5；+（防线 garrison 均值-50）×0.3 |
| 民生 | 民情 population_satisfaction（直取） |
| 财政 | 国库分档：>1000 万=90、>500 万=70、>0=50、>-200 万=30、否则 10 |
| 艺术造诣 | 艺术值 art_mastery |
| 声望 | 皇威×0.6 + 派系影响力均值×0.4 |
| 百姓口碑 | 民情×0.7 +（100-流民率×100）×0.3 |

加权总分：`Σ 各维分数 × EVAL_WEIGHTS`（权重和=1.0），天然落在 0~100，不再另做缩放。

### 结束判定（check_game_over 四类）
> 注：**皇帝不会因健康归零而驾崩**（已移除死亡判定）。皇帝健康仅影响个人行动/自然衰减效果，不再触发 game over；游戏结束完全依赖下方四类条件。

1. **国库崩坏**：国库 < -2000 万贯（TREASURY_COLLAPSE_LINE）→ 国用耗竭。
2. **主动退位**（check_abdication）：民怨<20 / 国库<-500万 / 皇威<25 且健康<30 / 任一边防空虚<20 —— **任满 2 条即退位**。
3. **强制收束年**（check_reach_end_year）：year≥1135（END_YEAR）→ 收束结束，防无限拖局刷分。
4. **京城陷落**：`内线_东京城防` garrison<10 且 year≥1126 → 靖康之耻。

---

## 十、存档

- 路径：`SAVE_DIR`（content/data.py 权威源），JSON 槽位文件 `slot_{n}.json`。
- schema_version 2（经济全浮动重构 v2），含版本号、完整状态快照；版本高于本程序 → 拒绝加载。
- **原子写**（2026-09 修复）：先写 `slot_{n}.json.tmp` 再 `os.replace` 原子替换——写盘中断不会产生半截档（自动槽 0 亦受保护）。
- 加载容错：解析失败/结构非法 → 记日志并备份为 `.corrupt`，返回 None（上层提示「存档可能已损坏」）；旧档缺字段由 `_merge_regions` 等补默认。
- 槽位列表对损坏档标记 `corrupt`（UI 显示「存档损坏」而非误报空槽）。
- 记忆库随槽位落盘（`slot_{n}.db` 主库 / `slot_{n}_dialogue.db` 对话库）。

---

## 附：关键数值速查

| 常量 | 值 | 含义 |
|------|-----|------|
| START_YEAR / MONTH | 1101 / 1 | 开局时间 |
| END_YEAR | 1135 | 强制收束年 |
| PRESTIGE_START | 55 | 初始皇威 |
| TREASURY_START | 500万贯 | 初始国库 |
| GRANARY_START / CAP | 1500 / 2000 万石 | 太仓初值/容量（GRANARY_START_CAP） |
| MONTHLY_EXP_CIVIL_BASE | 55万贯/月 | 朝廷常支基准 |
| TAX_COLOR_RATE | 0.40 | 田赋折银比例（40% 折色入国库） |
| LAND_TAX_RATE_BENEFIT | 0.10 | 田赋本色率（太仓月入） |
| TAX_POLL_RATIO | 0.10 | 役钱占比（按农 POP 征） |
| PER_CAPITA_MONTH_GRAIN | 0.5 石 | 人均月耗粮 |
| SALT_PROFIT_PER_JIN | 0.045 贯/斤 | 盐课利单价 |
| SUI_GONG_ANNUAL | 30万贯/年 | 岁币年支出基准 |
| ANNUAL_TAX_BASE | 8000万贯 | 年应征基准（名义口径，役钱已改按农 POP 征） |
| ARRIVAL_BASE | 0.45 | 史实到账率基准（**史实锚点，取中时不动**）；实测**有效综合到库率 40% → 52%**（修粜粮完税通道后），差额即隐漏与拖欠——三冗玩法杠杆 |
| **WAITING_PAY_RATIO** | **0.35** | **（取中）** 待阙俸率：史实多无俸(0) ↔ 待阙须有成本(0.5) |
| **SINECURE_PAY_RATIO** | **0.4** | **（取中）** 祠禄折俸：史实半俸偏低 ↔ 需体现祠禄成本 |
| **CLERK_PAY_PER_MONTH** | **2.4** | **（取中）** 吏薪基值：史实极薄(2.0) ↔ "先裁后提"要能走通；实得 ≈3.9 贯/月，**仍在 7.2 贯生计线之下**（陋规与吏怨保留） |
| **CLERK_ENTITLEMENT_GROWTH** | **0.0010** | **（取中）** 吏额自我膨胀/月：史实冗滥 ↔ 20 年内可感知（240 月冗吏率 17.4%） |
| **FARMER_TAX_GRAIN_KEEP_MONTHS / TAX_GRAIN_SALE_MAX_SHARE** | 3.0 / 0.05 | **（取中新增）** 粜粮完税的口粮安全垫与单月上限 |
| **ARREARS_REPAY_SHARE / ARREARS_KEEP_TREASURY** | 0.25 / 500,000 | **（取中新增）** 补发积欠：结余动用比例与国库应急安全库存 |
| TREASURY_CRISIS / COLLAPSE | -500万 / -2000万贯 | 危机/崩坏线 |
| **BRANCH_BASE[兵种].pay** | 重骑 2.33 / 轻骑 2.10 / 重步 1.96 / 轻步 1.77 / 弓弩 1.86 / 器械 1.77 / 水军 1.96（贯·月） | **（2026-09-18 提至史实）** 现金兵俸轴 289.5 万 → **1,349 万贯/年**（粮草不变） |
| **ASSET_MAINTAIN_RATE** | **0.005**（0.5%/月） | **（2026-09-18 新增）** 资产维持费率；沿用 `BUILDING_STD[*]["maintain"]` 既有口径 |
| **WORKSHOP_VALUE / EQUIP_UNIT_VALUE / FORT_VALUE / POP_BUILDING_VALUE** | 12万贯 / 2贯 / 1万贯 / 10万贯 | **（2026-09-18 新增）** 作坊/军械件/城防点/POP建筑的维持费折算基准 |
| **UPKEEP_PAY_TO** | 工匠 0.4 / 商人 0.6 | **（2026-09-18 新增）** 维持费去向（POP 挂载律：不得入影子账户） |
| 事件阈值 | 80（settlement_steps 硬编码） | 事件触发阈值 |
| 事件压力倍率 | 1.0（史实） | 难度参数 |
| 难度 | 史实/轻松/艰难 | 三档预设（另含 refugee_pressure / external_growth 倍率） |
