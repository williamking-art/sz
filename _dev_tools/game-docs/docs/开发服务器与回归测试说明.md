# 开发服务器与回归测试说明

> 定位：记录 sz 项目在腾讯云 CVM 上的开发/测试环境与使用方式（2026-09-23 建立）。
> 另含腾讯云开发 **CloudBase 的接入现状**（§八）—— 它在本项目中**只作开发辅助工具，
> 不接入游戏运行时**：游戏仍默认在客户端进程内单机运行，去掉 CloudBase 不影响任何游戏功能。
>
> **安全约定（先读）**：本仓库是**公开仓库**，故本文**不写入**服务器公网 IP、
> 登录密码、私钥内容等敏感项。实值请从腾讯云控制台与本地密钥目录获取（见 §2）。

---

## 一、这台机器解决什么问题

本机（Windows 开发机）**已具备**三套工具链。截至 2026-09-23 晚，**Node 已永久写入用户 PATH**，
`node` / `npm` / `npx` 可直接使用；**Python 与 Rust 仍未入 PATH**，需用绝对路径，
或先点源 `_dev_tools/dev-env.ps1` 注入（见 §七）：

| 工具 | 本机位置 | 版本 |
|---|---|---|
| Node / npm | `D:\codebuddy\tools\nodejs\node-v20.19.5-win-x64\` | 20.19.5 / 10.8.2 |
| Python | `D:\codebuddy\.audit-venv\Scripts\python.exe` | 3.13.14（含 fastapi / pytest / shapely / rich / requests / Pillow） |
| Rust / cargo | `C:\Users\<用户名>\.cargo\bin\cargo.exe` | 1.98.1 |

> 排查提示：**不要只用 PATH 判断"有没有装"** —— 本次就因此误判过。
> 应同时检查 `D:\codebuddy\tools\`、`.audit-venv`、`~/.cargo/bin` 等位置。

因此服务器**不是"本机跑不了"的替代品**，它的独立价值在于：

| 用途 | 为什么用服务器而不是本机 |
|---|---|
| **Linux 平台验证** | 本机是 Windows。部分用例存在平台相关行为（如立绘路由用例把 Windows 路径 `D:\...` 写死，在 Linux 下解析为空串而失败），需要在目标平台上跑 |
| **Python 版本交叉验证** | 本机 3.13 vs 服务器 3.11，取整/浮点行为可能不同；两边对比可区分「环境差异」与「真实缺陷」 |
| **Linux 产物构建** | `songzuo_server` 若要部署到 Linux，应在 Linux 上 `cargo build --release` |
| **独立 CI** | 不占用本机、不依赖本机环境，push 后自动跑回归 |
| **长跑 / 压力测试** | 240 月对账等重算可后台跑，不挡本机开发 |

> 注：2 核 2GB 是偏紧的配置，编译 Rust 前已补 2GB swap（见 §3）。
> 这台机器适合**测试与构建**，不要在未评估前承载线上服务。

---

## 二、连接方式

### 2.1 实例信息（腾讯云控制台可查）

| 项 | 值 |
|---|---|
| 实例 ID | `ins-9kvx4mqw` |
| 地域 / 可用区 | 广州六区 |
| 规格 | 标准型 S5 / 2 核 / 2GB / 3Mbps |
| 系统盘 | 通用型 SSD 云硬盘 / 50GB |
| 网络 | 私有网络（VPC） |
| 操作系统 | TencentOS Server 4 for x86_64（RHEL 9 系，内核 `6.6.117-45.11.6.tl4`） |
| 登录账户 | `root` |
| 公网 IP / 内网 IP | **见腾讯云控制台**（本文不披露） |

### 2.2 密钥登录（推荐，已配置完成）

- 本地私钥：`~/.ssh/sz_cvm_ed25519`
  （Windows：`C:\Users\<用户名>\.ssh\sz_cvm_ed25519`）
- 对应公钥（可公开，可复制到服务器）：

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIO1AVzSyU0NPBAXQ5GDq7Geg4UkOj+ezMVdXyF+L/r/F sz-cvm
```

服务器侧安装公钥（只需一次）：

```bash
mkdir -p ~/.ssh && chmod 700 ~/.ssh
echo 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIO1AVzSyU0NPBAXQ5GDq7Geg4UkOj+ezMVdXyF+L/r/F sz-cvm' >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

日常连接：

```bash
ssh -i ~/.ssh/sz_cvm_ed25519 root@<公网IP>
```

### 2.3 密码登录

**建议关闭**（`PasswordAuthentication no`），只保留密钥。

若必须临时使用密码登录：密码请走密码管理器传递，**绝不写入本仓库、绝不提交**。

> 踩坑记录：Windows 下用 `.cmd` 批处理做 `SSH_ASKPASS` **不可行**
> （`CreateProcess` 无法直接执行 `.cmd`，ssh 会拿到空密码）。
> 若需自动传密码，需编译成真正的 `.exe`。

### 2.4 安全组建议

22 端口的来源请限制为**自己的出口 IP**，不要留 `0.0.0.0/0`。

---

## 三、机器上已就绪的环境

| 组件 | 版本 | 位置 / 备注 |
|---|---|---|
| Python | 3.11.6 | 系统自带（`python3`） |
| Python 依赖 | fastapi 0.141 / uvicorn 0.53 / shapely 2.1 / rich 15 / requests 2.34 / Pillow 12 / pytest 9.1.1 | 装在 `/opt/sz/game/.venv` |
| Node.js | 20.19.5（npm 10.8.2） | `/usr/local/node-v20.19.5-linux-x64`，已软链至 `/usr/local/bin` |
| Rust | rustc / cargo 1.98.1（rustup，走 rsproxy.cn 镜像） | `~/.cargo/bin` |
| git | 2.43.7 | `dnf install` |
| gcc | 12.3.1（Tencent Compiler） | `dnf install`，Rust 链接所需 |
| Swap | 2GB（`/swapfile`，已写入 `/etc/fstab`） | **必要**：2GB 物理内存编译 Rust 会 OOM |

依赖安装命令（如需重建）：

```bash
# Python 侧
cd /opt/sz/game
python3 -m venv .venv
.venv/bin/python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple \
    -r requirements.txt pytest

# Node 侧（npmmirror 二进制，避免依赖系统仓库）
curl -fL -o /tmp/node.tar.xz \
  https://registry.npmmirror.com/-/binary/node/v20.19.5/node-v20.19.5-linux-x64.tar.xz
tar -xf /tmp/node.tar.xz -C /usr/local/

# Rust 侧（rsproxy 镜像）
export RUSTUP_DIST_SERVER=https://rsproxy.cn
export RUSTUP_UPDATE_ROOT=https://rsproxy.cn/rustup
curl --proto '=https' --tlsv1.2 -sSf https://rsproxy.cn/rustup-init.sh | sh -s -- -y --profile minimal
```

---

## 四、日常操作

### 4.1 同步代码

```bash
cd /opt/sz
git pull origin main
```

> 服务器直连 GitHub **时好时坏**：clone 时实测可达 10MB/s，但 `fetch`/`pull`
> 偶发超时（`Failed to connect to github.com port 443`、
> `Failure when receiving data from the peer`）。失败就重试；
> 若连续失败，可从本地直接 `scp` 覆盖单个文件（内容一致时不会与后续 pull 冲突）。

### 4.2 跑全量回归

```bash
cd /opt/sz/game
.venv/bin/python -m pytest           # 当前约 892 例，约 1 分钟
```

长跑建议放后台（避免 SSH 会话被切断）：

```bash
cd /opt/sz/game
nohup .venv/bin/python -m pytest > /tmp/pytest.log 2>&1 &
tail -f /tmp/pytest.log
```

> 注：`pytest.ini` 里 `testpaths = ../_dev_tools/game-dev/tests`，
> 所以**必须从 `game/` 目录**运行。

### 4.3 编译并运行 Rust 后端（已实测）

编译（**实测 1 分 07 秒，产出 3.0MB 二进制**）：

```bash
cd /opt/sz/songzuo_server
export PATH=$HOME/.cargo/bin:$PATH
nohup cargo build --release > /tmp/cargo.log 2>&1 &
tail -f /tmp/cargo.log
```

依赖走 `/root/.cargo/config.toml` 里配的 rsproxy 镜像（crates.io 在境内不稳）。

单元测试（**实测 3 passed**）：

```bash
cargo test --release
```

本地运行验证：

```bash
SONGZUO_ADDR=127.0.0.1:18080 ./target/release/songzuo_server
curl -s http://127.0.0.1:18080/healthz
curl -s -X POST -H "Content-Type: application/json" --data-binary {} \
     http://127.0.0.1:18080/api/new_game
```

**云托管就绪性（已实测）**：只设 `PORT` 时会自动绑 `0.0.0.0`（`ss` 可见 `LISTEN 0.0.0.0:<PORT>`），
正合容器探针要求；未设 `SONGZUO_SERVER_TOKEN` 时 `/api/*` 只接受回环来源。

### 4.4 构建前端（已实测）

Electron / electron-builder 的二进制默认从 GitHub 下载，在本链路会被限速卡死，
**必须先接国内镜像**：

```bash
cd /opt/sz/game/frontend
npm config set registry https://registry.npmmirror.com
export ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
export ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/
nohup npm install > /tmp/npminstall.log 2>&1 &      # 实测 733 包 / 31 秒 / 741MB
```

构建（**实测 11.11 秒**）：

```bash
npm run build        # electron-vite build
```

产物在 `game/frontend/out/`（126MB，已 gitignore）：`out/main`、`out/preload`、`out/renderer`。
其中 `out/renderer` 即浏览器可用的静态产物 —— 想发到 CloudBase 静态托管时用它，
但 Electron 桌面链路的 `electron` API 需另行剥离。

### 4.5 常驻运行参考后端（联调用）

```bash
cd /opt/sz/game
.venv/bin/python -m backend.server      # 默认 127.0.0.1:8080
```

### 4.6 构建并运行容器镜像（已实测）

机器上已装 Docker 29.7.0。Docker Hub 直连会被限速，先在 `/etc/docker/daemon.json`
配腾讯云内网镜像源（**已配置**）：

```json
{ "registry-mirrors": ["https://mirror.ccs.tencentyun.com"] }
```

构建（**实测：builder 镜像 1.23GB → 成品 134MB / 内容 33.3MB**）：

```bash
cd /opt/sz/songzuo_server
docker build -t songzuo_server:0.1.0 .
```

运行：

```bash
docker run -d --name sz-test -p 127.0.0.1:18090:8080 \
  -e PORT=8080 -e SONGZUO_SERVER_TOKEN=sz-dev-token \
  songzuo_server:0.1.0
curl -s http://127.0.0.1:18090/healthz
```

> ⚠️ **容器化必读两条**
>
> 1. **`SONGZUO_SERVER_TOKEN` 必须设置。** 源码在未配 token 时只放行回环来源
>    （`is_loopback`），而 Docker 端口映射后容器看到的源 IP 是网桥网关（172.17.0.1），
>    **不是回环** —— 结果是 `/healthz` 正常 200，但**所有 `/api/*` 一律 401**。
>    实测三种情形：无 token → 401、`Authorization: Bearer <正确 token>` → 200、
>    错误 token → 401。鉴权头格式为 `Authorization: Bearer <token>`。
> 2. **`SONGZUO_ADDR` 在容器里不要设。** 它优先级高于平台注入的 `PORT`，
>    一旦设置会覆盖端口，readiness probe 直接判失败。

---

## 五、持续集成（CI）

回归由服务器上的 `/opt/sz-ci/ci.sh` 承担，两条触发路径：

| 路径 | 触发方式 | 说明 |
|---|---|---|
| **本地直推**（推荐） | `git push server master:main` | 服务器 `post-receive` 钩子立即跑回归，**不经过 GitHub**，最可靠 |
| **定时轮询**（兜底） | cron 每 30 分钟 | `git fetch` 检查远端 main 是否有新提交；**镜像优先 + 直连回退**（各试 2 次）——服务器直连 GitHub 的 git 数据传输会被限速到 <1KB/s，必须走镜像 |

### 5.1 一次性配置

服务器侧（已完成）：

```bash
cd /opt/sz
git config receive.denyCurrentBranch updateInstead   # 允许直推并同步更新工作区
chmod +x /opt/sz-ci/ci.sh /opt/sz/.git/hooks/post-receive
```

本地侧（已完成）：

```bash
git remote add server ssh://root@<公网IP>/opt/sz
```

私钥不是默认名时，push 前指定（Windows PowerShell）：

```powershell
$env:GIT_SSH_COMMAND = "ssh -i C:/Users/<用户名>/.ssh/sz_cvm_ed25519 -o StrictHostKeyChecking=no"
```

### 5.2 日常用法

```bash
git push origin master:main     # 推 GitHub（对外）
git push server master:main     # 推服务器 → 自动触发回归
```

查看结果：

```bash
ssh -i ~/.ssh/sz_cvm_ed25519 root@<公网IP> "tail -20 /var/log/sz-ci.log"
```

日志格式：

```
[2026-09-23 10:15:58] PASS 757a68d77c  用例 892 全通过
[2026-09-23 10:12:06] 收到本地推送，开始回归
[2026-09-23 09:59:25] fetch 失败（已重试 3 次，跳过本轮）
```

失败时是 `FAIL <短SHA>  失败 M 例（用例 N）` 并附前 20 条失败用例。
每一轮的完整 pytest 输出留档在 `/var/log/sz-ci-last.log`。

> 实现备注：**完整套件跑完后 pytest 自己的汇总行会丢失**（套件内部有测试在收尾阶段
> 吞掉了 stdout，小规模子集则正常）。因此 CI 不依赖汇总行，而是：
> 收集阶段用 `--collect-only -q` 累加用例总数、正式跑用 `FAILED/ERROR` 行统计失败数，
> **最终判定以返回码为准**。

### 5.3 注意

- CI 运行前会**丢弃服务器工作区的本地修改**（`git checkout -- .`）—— 不要把服务器当开发机。
- 服务器上 `.git/hooks/post-receive` 与 `/opt/sz-ci/ci.sh` 都不入 git（前者在 .git 内，后者在仓库外），重建环境时需按 §5.1 重做。

---

## 六、安全与运维约定

1. **敏感值不入库**：公网 IP、密码、私钥内容一律不写进本仓库（公开仓库）。
2. 22 端口来源限制为个人 IP；建议关闭密码登录、只留密钥。
3. 服务器上的存档、日志、临时产物不要提交；`game/.venv` 已在 `.gitignore` 内。
4. 变更系统配置（swap、软件源、防火墙）请在本文档留一行记录。

---

## 七、已知问题 / 待办

- [x] ~~服务器直连 GitHub 不稳定~~ → 已用**本地直推 `git push server`** 规避（见 §5）；
      cron 轮询保留作兜底（带 3 次重试），失败只记日志、不影响其它。
- [x] ~~42 个大臣立绘 PNG 显示「已修改」~~ → 已查清：**不是行尾或 gitattributes 问题**，
      而是与仓库版本**编码不同的另一份 PNG**（头部相同、压缩流自第 45 字节起不同，
      体积少约 2 万字节）；`git checkout -- <目录>` 即可还原，还原后跑测试不会再变脏。
- [x] ~~建 push 后自动跑回归的 CI~~ → 已完成（见 §5）。
- [x] ~~本机三套工具链未入 PATH~~ → 已提供 `_dev_tools/dev-env.ps1` 一键注入：
      `. .\_dev_tools\dev-env.ps1`（点源，仅当前会话生效），随后即可直接用
      `python` / `node` / `cargo`。
      踩坑：PowerShell 5.1 按 GBK 读 `.ps1`，**中文注释的脚本必须带 UTF-8 BOM**，
      否则解析报错（该脚本已带 BOM）。
- [x] ~~Node 未入 PATH~~ → **2026-09-23 晚已永久写入用户 PATH**
      （`D:\codebuddy\tools\nodejs\node-v20.19.5-win-x64`；node / npm / npx 及全局 CLI 的
      shim 都在这个目录，故一条即可）。新开的终端/进程**无需再注入**。
      注意：PATH 是进程启动时继承的，**正在运行的 IDE 仍看不到**，重启一次后才对它拉起的
      子进程（含将来要装的 npx 型 MCP）生效。Python 与 Rust 仍靠 `dev-env.ps1`。
- [x] ~~若希望 CI 覆盖「非本地来源」的提交，需改善服务器到 GitHub 的网络~~ →
      **已定位并解决**：服务器**直连 GitHub 时 git 数据传输被限速到 <1KB/s**——
      表现为 `ping` 0% 丢包、`curl -I` 200/0.29s、`git ls-remote` 成功，
      但 `git fetch` 会一直卡住直到超时（开启低速保护后报
      `Operation too slow. Less than 1000 bytes/sec transferred the last 30 seconds`）。
      **处置**：CI 的 fetch 改为**镜像优先 + 直连回退**（各试 2 次），镜像用
      `url."https://gh-proxy.com/https://github.com/".insteadOf`，实测可正常完成 fetch。
      注意：这只影响**服务器→GitHub**；本地 push 到 GitHub 走 7890 代理，不受影响。
- [ ] **全量跑（约 892 例）时 pytest 自身不输出汇总行**（stdout 只剩进度条、返回码正常）。
      已排除：测试/游戏代码操作 stdout、pytest hooks、`-s` 禁用捕获、重定向方式、
      `.gitattributes`；并且**任意一半文件单独跑均能正常输出汇总** ——
      属规模相关的边缘现象，根因暂未定位。影响仅限「看不到统计数字」，不影响判定。
      需要精确统计时建议 `--junit-xml`；CI 则改用「收集阶段累计用例总数 + FAILED 行计数 +
      返回码」三重手段（见 §5.2 实现备注）。

---

## 八、CloudBase（开发辅助工具，不接游戏运行时）：接入现状 + 云端发布方法记录

> 本章三部分：**8.0 当前环境**（事实现状）、**8.6 接入方式**（怎么连上，无需安装），
> 以及 **8.1–8.5 方法记录**（原云托管部署的做法与踩坑，其部署实例已失效，仅作参考）。
>
> **状态更新（2026-09-23 下午）**：本节所述部署**已随环境一起失效** ——
> 承载它的环境 `william-1-d6genf4wb9988a74a` 已被删除，云托管服务 `songzuo-server`
> 与公网域名 `songzuo-server-318551-9-1440445995.sh.run.tcloudbase.com` 均已不存在。
>
> **本节仍有价值**：8.1–8.5 记录的是**方法与踩过的坑**（部署参数、`target/` 上传体积、
> 容器鉴权约束、冷启动适配），与环境是否存活无关，将来再发布时可直接复用。
> 文中出现的 `envId`、域名、镜像地址仅为历史留痕，**不要照抄**。
>
> **定位（重要，先读）**：CloudBase 在本项目中是**开发辅助工具**，**不属于游戏运行时架构**。
> 游戏默认在客户端进程内单机运行，`game/README.md` 的「后端连接」一节也把云端后端定义为
> **可选**的远程模式 —— 因此**删掉 CloudBase 不影响游戏的任何功能**。
> 本节记录的是「用 CloudBase 演进后端」的探索与方法沉淀，**请勿当作需要长期维护的线上依赖**；
> 它的实际用途是：给开发期提供可验证的云端通路与工具链（PG、静态托管、函数等）。

原部署事实（2026-09-23 上午，现已失效）：

- 环境：`william-1-d6genf4wb9988a74a`（ap-shanghai，体验版）—— **已删除**
- 服务名：`songzuo-server`
- 访问地址：`https://songzuo-server-318551-9-1440445995.sh.run.tcloudbase.com` —— **已失效**
- 规格：CPU 0.5 / Mem 1G；`MinNum=0`（空闲缩容到 0）、`MaxNum=2`
- 镜像：`ccr.ccs.tencentyun.com/.../songzuo-server:songzuo-server-001-<ts>`（134MB）
- 首次部署 2026-09-23 11:31，镜像构建约 1 分 20 秒

### 8.0 当前环境（2026-09-23 晚，重建后）

| 项 | 值 |
|---|---|
| EnvId | `william-1-d5grtya3z0b5e143d`（别名 `william-1`） |
| 地域 | **ap-singapore** ⚠️（与此前两个环境的 ap-shanghai 不同） |
| 套餐 | 体验版（免费），到期 2027-03-23 |
| 后端模式 | **PostgreSQL**（`RuntimeBackends = {postgresql:true, nosql:false, mysql:false}`） |
| PG 实例 | `postgres-gmd3fp37`（SHARED 租户） |
| 云托管 | **该地域不支持**（实测，见下方说明；**不是**"未开通"） |
| 静态托管 | online（`william-1-d5grtya3z0b5e143d-1440445995.tcloudbaseapp.com`，路由已启用） |
| 云存储 | 桶 `7769-william-1-d5grtya3z0b5e143d-1440445995`（legacy NoSQL 桶，**不是** pgstore 桶） |

> ⚠️ **云托管在该地域不可用**（2026-09-23 晚实测）：`queryCloudRun(action=envStatus)` 报
> `[DescribeEnvBaseInfo] region ap-singapore not in config support region` ——
> **这不是"未开通"，`initEnv` 也开不了**。结论：8.1 那套「Rust 后端上云托管」的做法在
> 当前环境**无法复用**，除非去支持地域（如 ap-shanghai）另建环境。
>
> ✅ **PG 完全可用；MCP 的 PG 工具也已修复**（修复过程见下方"已解决"）
>
> 三条路径实测（2026-09-23）：
>
> | 路径 | 结果 |
> |---|---|
> | MCP `queryPgDatabase action=sql/objects` | ✅ **已恢复**（补上地域配置后）；修复前为 ❌ `PG_NOT_READY / PG instance not found` |
> | 直连 HTTP `POST /v1/rdb/exec-pgsql` | ✅ **完全可用**（独立备用通路，见 §8.7）：建表/插入/查询/删除全通；`version()` → **PostgreSQL 17.11**、`current_database()` → **`postgres-gmd3fp37`** |
> | MCP `managePgDatabase listMigrations` | ✅ 通（管理面 `ListPGUserMigrations`） |
>
> 历史教训（本文档前两版都在此处判断错）：**不要因为 MCP 工具报错就断定云服务没就绪** ——
> 当时的现象是「管理面通、MCP 数据面不通、HTTP 直连通」，真相是 MCP 缺正确地域配置，
> 而 PG 本身早就绪。判定可用性**必须**用真实查询（`action=sql`）或 §8.7 直连实测。
>
> **疑因（已排除"站点歧义"，改判为"地域"）**：MCP 日志显示 envId 来自 IDE 注入的
> `cloudBaseOptions`（`envIdSource: "cloudBaseOptions"`），且站点已被 `applyBoundEnvSite`
> 钉为 `domestic` —— **所以"站点歧义"不成立**（本文档上一版猜错了）。真正可疑的是**地域**：
> 解析优先级为「显式选项(`cloudBaseOptions.region`) > `TCB_REGION` > `.cloudbase/project.json`
> > 站点默认」，而**国内站默认 `ap-shanghai`、本环境在 `ap-singapore`** —— 接口按默认地域去
> ap-shanghai 找 PG 实例，即报 `PG instance not found`。
> **✅ 已解决（2026-09-23 16:21 复验）**：MCP 的 PG 工具**已恢复正常**。修法是给 MCP 补上正确地域，做了两层：
>
> 1. **`.cloudbase/project.json`（含 `region: ap-singapore`）** —— 放两处：`D:\codebuddy\.cloudbase\`
>    与 `D:\codebuddy\sz\.cloudbase\`。因为 MCP 的 `readProjectConfig()` **不传 cwd**（用进程 cwd），
>    而其项目状态键是 `projects\D--codebuddy`，推断其 cwd 为 `D:\codebuddy`。**这是实际生效的那条。**
> 2. **用户级环境变量 `TCB_REGION=ap-singapore`**（第 2 优先级）—— 仅在 **IDE 重启后**才对 MCP 子进程
>    生效，作为第二层保险，防 cwd 变化。
>
> ⚠️ **两个坑（本文档都曾踩过）**：
> - **写入后不会立即生效**：MCP 进程内缓存了 manager / 地域。实测：16:05 写入后立刻复验**仍失败**，
>   直到 16:21 才刷新（**进程全程未重启**，PID 29608 与启动时间 07:52:25 均未变）。
>   所以"写完立刻测"会**误判为无效** —— 我先前据此写下的"project.json 改不动"就是**假阴性**，已更正。
> - 判定工具是否恢复，**必须用真实查询**（`action=sql`），`action=context` 在实例不可达时也会"成功"。
>
> 三步复验（全通即正常，2026-09-23 实测）：
>
> ```
> queryPgDatabase action=sql     → SELECT 1                                ✅ {"ok":"1"}
> queryPgDatabase action=objects → 列出 auth.* / cloudbase_migrations.*     ✅ 20 个对象
> queryPgDatabase action=sql     → SELECT count(*) FROM dev_econ_monthly / dev_ci_runs
>                                → 240 / 34，max|residual| = 0              ✅
> ```
>
> 云托管在该地域仍不可用（服务端限制，与本次修复无关）。
>
> ⚠️ **`action=context` 不能用来判定可用性**：它**会成功**返回 `instanceId=cloudbase-pg`、
> `role=cloudbase_postgres`，但那只是**由环境元数据推导**，并不访问实例。
>
> ⚠️ 选型不变：PG 是本环境**唯一**的业务后端（`nosql=false`、`mysql=false`）。

### 8.1 部署参数（CloudBase MCP `manageCloudRun`）

```
action          = deploy
serverName      = songzuo-server
targetPath      = <repo>/songzuo_server          # 绝对路径
serverType      = container
serverConfig:
  Port            = 8080
  Cpu / Mem       = 0.5 / 1
  MinNum / MaxNum = 0 / 2
  Dockerfile      = Dockerfile
  OpenAccessTypes = ["PUBLIC"]
  EnvParams       = {"PORT":"8080","SONGZUO_SERVER_TOKEN":"<你的 token>"}
```

部署工具会在 `songzuo_server/` 生成 `cloudbaserc.json`（内容仅本机 envId + 服务名）。
它属"本机部署上下文"而非项目源码，**已加入 `.gitignore`、不入库**。

### 8.2 ⚠️ 部署前必须把 `target/` 移出目录（本次最贵的坑）

部署工具会把 `targetPath` **整个目录打包上传**，且**不读 `.gitignore` / `.dockerignore`**：

- `songzuo_server/target/` 本地有 **1.1GB / 2748 个文件**
- 打包上传后请求被重置：`manageCloudRun/deploy ... failed, reason: read ECONNRESET`
- **报错文案是网络错误，极易误判为网络/代理问题**。实测该域名网络完全正常：
  直连、HTTP 代理、SOCKS5 三种方式 PUT 均正常返回 403

正确做法 —— 部署前移出、部署后移回：

```bash
mv songzuo_server/target /tmp/sz-target-hold      # 部署前
# ... 执行部署 ...
mv /tmp/sz-target-hold songzuo_server/target      # 部署后
```

移出后上传体积：**1.1GB → 124KB（13 个文件）**。

### 8.3 云端构建日志要点

```
检出 ZIP 包             → 13 个文件（确认 target/ 未入包）
Updating `mirror` index → 依赖 3 秒下完（rsproxy 镜像生效，Dockerfile 里配的）
空壳编译（缓存依赖层）   → 15.26s
真实编译                → 4.54s
镜像大小                → 134MB
Image pushed successfully
```

### 8.4 运行期鉴权（务必记住）

云端**必须**设置 `SONGZUO_SERVER_TOKEN`：未配 token 时 `/api/*` 只放行回环来源，
而云托管/容器中的请求源 IP 并非回环 —— 结果是 `/healthz` 一片 200、但 `/api/*` 全 401。
客户端须带 `Authorization: Bearer <token>`。

**客户端配置（Electron 前端已支持，与 Python 端同源）**

`game/frontend/src/main/index.ts` 的 `BackendConfig` 已增加可选 `token`，解析顺序与
Python 端 `backend/client.py` 完全一致：

1. 环境变量 `SONGZUO_SERVER_TOKEN`（优先级高）
2. 其次 `backend_config.json` 的 `token` 字段

传递链条：`main` 解析 → `preload` 暴露 `getBackendToken()` → `App.tsx` 取回 →
`ApiClient(base, token)` 在 `request()` 中加 `Authorization: Bearer <token>`。
两端因此可共用同一份 `backend_config.json`：

```json
{
  "backend": "remote",
  "url": "https://<你的后端域名>",
  "token": "<SONGZUO_SERVER_TOKEN 的值>"
}
```

`ApiClient` 对 401/403 会给出可行动文案（提示去配 token），而不是只报「HTTP 401」。

### 8.5 冷启动适配（不常驻实例 + 两端一致）

服务 `MinNum=0`（**不保持常驻实例**），闲置后缩容到 0；此时网关先返回 **503**，
实测实例首次唤醒约需 **1~2 分钟**。

**关键判据：503 的来源必须区分，不能一律重放。**

| 来源 | 响应体 | 处理 |
|---|---|---|
| 平台网关（实例缩容到 0） | **不是 JSON** | 请求根本没到应用 → 等待就绪后重放**安全**，非幂等端点亦然 |
| 应用自身（`backend/server.py` 的 AI 拒绝式失败） | **是 JSON** `{"detail":{"error_code":…,"message":…}}` | `/api/advance`、`/api/action` 都在其中 → 可能已进入处理流程 → **绝不重放**（C4） |

故判据取「响应体是否为 JSON」，而非「状态码是否为 503」。Rust 端 `songzuo_server/src`
本身不含 503，走的也是同一条保守判据；其余 5xx 与超时仍严格不重试非幂等端点。

**两端实现对齐**：

| 项 | Electron 前端 | Python 客户端 |
|---|---|---|
| 文件 | `renderer/api/client.ts` | `backend/client.py` |
| 无副作用就绪等待 | `ApiClient.waitReady()`（轮询 `/health`） | `HttpBackend._wait_ready()` |
| 闲置判定 | `lastOkAt` + `IDLE_BEFORE_WARMUP_MS`(45s) | `_last_ok` + `_IDLE_BEFORE_WARMUP`(45s) |
| 等待上限 | `COLD_START_WAIT_MS`(150s) | `_COLD_START_WAIT`(150s) |
| 前置防护 | 非幂等端点距上次成功过久则先等就绪再发 | 同左 |
| 首屏 | `App.tsx` 就绪轮询按时间限界（原 14 秒 → 150 秒） | — |
| 失败可见 | MainMenu 底部展示 `backendError` + 「重试」按钮 | 抛出中文可读错误 |

> 顺带修掉一处既有 bug：`client.py` 原先在多个分支各自 `e.read()`，Python 的
> `HTTPError` 响应体只能读一次，到第二个分支时只能拿到空串（错误详情丢失）。

**代价**：闲置后首次操作需等待实例唤起（1~2 分钟），但**不产生常驻费用**。
若日后改为常驻（`MinNum>=1`），本机制自然不再触发。

### 8.6 接入方式（CodeBuddy：走内置连接器，无需安装）

按官方《CloudBase Setup Skill》（https://docs.cloudbase.net/skill.md）的分级方案**实测**如下 ——
**结论：不装插件、不手写 MCP 配置，用 IDE 内置连接器即可**：

| 文档节点 | 实测结论 |
|---|---|
| §0 前置条件 | **曾是唯一缺口，已修复**：Node 20.19.5 装在 `D:\codebuddy\tools\nodejs\node-v20.19.5-win-x64`，但用户/系统 PATH 里都没有 → `node`/`npm`/`npx` 全部 NOT FOUND。已把该目录写入用户 PATH（见 §七） |
| §1 plugins CLI | **不适用**：`npx plugins targets` 实测支持 Claude Code / Cursor / Codex / Grok Build / Kimi Code / GitHub Copilot CLI / VS Code(Preview)，**不含 CodeBuddy** |
| §3.C 内置连接器 | **即正解**：文档要求「自带 CloudBase 的 IDE 优先用内置路径、不要重复安装」。CodeBuddy 里它是**「集成」**（`integrationId: tcb`），**不写 `mcp.json`** —— `~/.codebuddy/mcp.json` 一直是空的，与"能用"并不矛盾 |
| §2 Skills | **无需另装**：内置知识库已含 27 份 CloudBase 技能文档，直接 `searchKnowledgeBase(mode="skill", skillName="...")`，如 `postgresql-development-cloudbase`（PG 开发）、`minimal-web-baas-demo`（最小 Web+库 Demo）、`cloudbase-cli` |
| §6(e) 重复实例 | 遵循「不要在同一 IDE 叠加 插件 + 手写 MCP + Skills」，保持单一内置路径 |

验证（真实调用，非推断）：`auth` → `auth_status: READY`（账号级登录，region `ap-singapore`）；
`envQuery` → 当前环境 `william-1-d5grtya3z0b5e143d`；`queryFunctions listFunctions` 与
`queryHosting listFiles` 均正常返回（各 0 条）→ **控制面 / 云函数 / 静态托管通路通**；
MCP 的 PG 工具不通（`PG_NOT_READY`），但 **PG 本身完全正常** —— 改走 §8.7 的 HTTP 直连，
已实测建表/插入/查询/删除全通。

> 操作提示：PATH 是进程启动时继承的，**正在运行的 IDE 仍看不到新 PATH**；
> 重启一次后，IDE 拉起的子进程（含以后要装的 npx 型 MCP）才会继承。

### 8.7 PG 直连（备用通路，**实测可用**）

> ✅ MCP 的 PG 工具**已修复**（见 §8.0），日常应优先用它（自带 migration 管理与 schema 校验）。
> 本节是**独立备用通路**：MCP 不可用时，或在没有 MCP 的脚本 / CI 里直接操作 PG 时使用。

云开发网关的 `exec-pgsql` 端点可直连：

```
POST https://<envId>.api.tcloudbasegateway.com/v1/rdb/exec-pgsql
Authorization: Bearer <管理员 API Key>
Content-Type: application/json

{"sql": "<单条 SQL>", "role": "cloudbase_postgres"}
```

- **凭据**：API Key 即可（也支持 TC3 签名 / B 端管理员 Token）；非管理员返回 403。
  本环境已签发一枚：`keyName=sz-dev-pg-http`、`keyId=nf5xFmMJRqabcV4dnqW5QA`、
  到期 **2026-10-23**。**密钥值不写入本文档与仓库**；可用
  `queryAppAuth action=listApiKeys` 查看、`manageAppAuth action=deleteApiKey` 删除、
  `action=createApiKey`（`keyType="api_key"`）重新签发。
- **`role` 取值**：不传 → `cloudbase_read_only_user`（只读）；`cloudbase_postgres` → 可 DDL/DML。
- ⚠️ **一次只发一条语句**：多语句会报
  `DATABASE_42601: cannot insert multiple commands into a prepared statement`。
- **错误码**：400 `DATABASE_42601`(语法)/`42P01`(表不存在)/`23505`(唯一冲突)/`42501`(权限不足)；
  401 `UNAUTHORIZED`；403 `ACTION_FORBIDDEN`；500 `INTERNAL_ERROR`。

实测记录（2026-09-23，**全部 http=200**）：

```
SELECT 1                                      → [{"?column?":1}]
SELECT current_database(), current_user()     → db=postgres-gmd3fp37
                                                usr=cloudbase_postgres_postgres_gmd3fp37
SELECT version()                              → PostgreSQL 17.11 on x86_64-pc-linux-gnu
CREATE TABLE sz_smoke_test(id int primary key, note text)      → []
INSERT INTO sz_smoke_test VALUES (1, 'hello')                  → []
SELECT count(*) AS rows_now, max(note) FROM sz_smoke_test      → [{"rows_now":1,"note":"hello"}]
DROP TABLE sz_smoke_test                                       → []
SELECT to_regclass('public.sz_smoke_test') AS leftover         → [{"leftover":null}]   ← 无残留
```

调用示例（PowerShell / curl，Windows 上务必用 `@文件` 传 body，避免引号被吞）：

```powershell
$k = '<管理员 API Key>'
$u = 'https://<envId>.api.tcloudbasegateway.com/v1/rdb/exec-pgsql'
$b = Join-Path $env:TEMP 'q.json'
[IO.File]::WriteAllText($b, '{"sql":"SELECT 1","role":"cloudbase_postgres"}', [Text.UTF8Encoding]::new($false))
curl.exe -sS -X POST -H "Authorization: Bearer $k" -H "Content-Type: application/json" `
  --data-binary "@$b" -w "`nhttp=%{http_code}`n" $u
```

> 注意：直连写库**不经过 MCP 的 migration 历史**（`cloudbase/migrations/`）。
> 若日后要让表结构变更可追溯，正路是 `managePgDatabase action=applyMigration`（当前不可用）；
> 现阶段作开发辅助用途，直连足够 —— 建议自建表加统一前缀，便于与将来的 migration 体系区分。

### 8.8 已建开发辅助表：`dev_ci_runs`（CI 历史可 SQL 查询）

用 §8.7 的直连通道，把 CVM 上的 `/var/log/sz-ci.log` 解析入库，让 CI 历史可查询、可趋势分析：

```sql
CREATE TABLE dev_ci_runs (
  id bigserial PRIMARY KEY,
  noted_at timestamptz NOT NULL,   -- 日志时间（+08）
  stage text NOT NULL,             -- pytest / rust / frontend / fetch
  status text NOT NULL,            -- PASS / SKIP
  commit_sha text,                 -- 短 sha（日志未带则为 NULL）
  case_count int,                  -- 用例数（rust=3；日志缺失则为 NULL）
  raw_line text NOT NULL,
  CONSTRAINT dev_ci_runs_uniq UNIQUE (noted_at, stage, raw_line)   -- 保证可幂等重灌
)
```

2026-09-23 首次灌入 **34 行**（当天 09:59–16:00）。查询示例与实测结果：

```sql
SELECT stage, status, count(*) AS n, min(noted_at), max(noted_at)
FROM dev_ci_runs GROUP BY stage, status ORDER BY stage;

-- pytest PASS 20 / rust PASS 5 / frontend PASS 5 / fetch SKIP 4

SELECT noted_at, commit_sha, case_count, status
FROM dev_ci_runs WHERE stage='pytest' ORDER BY noted_at DESC LIMIT 5;
-- 550d15813b / 9a900b9064 / a1cc3b0a9a / a98deabea7 / 7d01a6a48b  均 892 用例
```

> 灌库用的是**临时脚本**（未入库，因需携带凭据）；重灌按本节 SQL + §8.7 的调用方式即可，
> `ON CONFLICT DO NOTHING` 使脚本可反复执行而不会重复。

### 8.9 已建开发辅助表：`dev_econ_monthly`（240 月经济轨迹）

用途：把长程回放的经济指标落库，用 SQL 做对账与趋势分析（替代一次性临时脚本）。

**导出**：脚本 `_dev_tools/game-docs/analysis/econ_pg_export.py`（**只写 JSON、不含任何凭据**）

```powershell
cd _dev_tools/game-docs/analysis
python econ_pg_export.py 240        # 产出 econ_monthly.json
```

> ⚠️ **回放推进的正确姿势（本脚本初版踩过的坑）**：必须用
> `core.settlement.run_monthly_settlement(state, seed_offset)`，并**每月注入 `state._economy_ai`**
> —— 与 `_dev_tools/game-dev/tests/test_officialdom.py:41-45` 的 `_run()` 同源。
> 而用 `core.commands.advance_month` + `settle_turn` 组合**根本不推进状态**：240 行输出逐行完全相同
> （年份恒 `1101-1`）、`residual`/`cum_residual` 全空。
> **判定准则：首行与末行必须不同**（脚本已内置该自检并打印 `advanced=True/False`）。

**入库**：表 `dev_econ_monthly`（30 列；`idx` 主键 + `ON CONFLICT DO NOTHING`，可幂等重灌），
按 §8.7 直连写入。2026-09-23 灌入 **240 月**（1101-01 → 1121-01）。

**实测结果**：

| 指标 | 结果 |
|---|---|
| 月度货币对账 | `max(abs(residual)) = 0`，240 行全覆盖 → **全程对账通过** |
| M2 | 3.13 亿 → 5.87 亿（20 年） |
| 国库 | 6.0 万 → 243.5 万（1111 年后快速增长） |
| 太仓 | **70 个月为 0**（1102–1120 间反复空仓） |
| 物价 | `price_level` 均值 1.212、`grain_price` 1.06–1.81（区间 [0.8, 2.8] 内） |
| 民生 | `satisfaction` 0–55；流民峰值 182 万 |

**⚠️ 一个用 SQL 查出来的待核对差异**：

```sql
WITH d AS (SELECT idx, year, month,
                  treasury - lag(treasury) OVER (ORDER BY idx) AS actual_delta,
                  net AS reported_net
           FROM dev_econ_monthly)
SELECT idx, year, month, round(actual_delta::numeric,0) AS actual_delta,
       round(reported_net::numeric,0) AS reported_net,
       round((actual_delta - reported_net)::numeric,0) AS diff
FROM d WHERE actual_delta IS NOT NULL
ORDER BY abs(actual_delta - reported_net) DESC LIMIT 8;
```

结果：**239/239 个月全部不一致**（`matched=0`），最大差 126 万；且 `reported_net` ≈ **−110 万**
而国库实际余额在**增长**。两种可能：① `finance_readout().net` 口径本就不含某些入账
（专项/内帑/地方府库等），属口径差异、非缺陷；② 记账有遗漏。
**本文只记录现象，未定性** —— 要定性需按收入项拆解比对。

### 8.10 两张辅助表已纳入 Migration 管理

§8.8 / §8.9 两张表最初是用 §8.7 的直连**裸 DDL** 建的，没有版本历史。MCP 修复后（§8.0）
已用 `managePgDatabase action=applyMigration` 补上，`listMigrations` 现为 **Total 2**：

| Version | Name | 内容 |
|---|---|---|
| `20260923170000` | `dev_ci_runs_init` | `CREATE TABLE IF NOT EXISTS dev_ci_runs (...)` |
| `20260923170100` | `dev_econ_monthly_init` | `CREATE TABLE IF NOT EXISTS dev_econ_monthly (...)` |

两次均返回 `Status: Succeed` / `Phase: RunMigrations` / `verified: true`
（TaskId `task-3825adb8`、`task-28a50862`）。SQL 用 `IF NOT EXISTS`，对已存在的表是**幂等空操作**
—— **数据未受影响**（`dev_econ_monthly` 240 行 / `dev_ci_runs` 34 行，`max|residual| = 0`）。

> ⚠️ **本地 migration 文件落在仓库之外**：MCP 按**进程 cwd** 定位 `cloudbase/migrations/`，
> 而它的 cwd 是 `D:\codebuddy`（与 §8.0 的推断一致，也被状态目录 `projects\D--codebuddy` 印证）。
> 因此 SQL 落在 `D:\codebuddy\cloudbase\migrations\`，**不随仓库做版本管理**。
> 若希望迁移脚本入库，需以仓库为 cwd 运行 MCP，或手工把该目录同步进仓库。

> 📌 **查迁移历史请用工具**：平台控制表 `cloudbase_migrations.control_schema_migrations`
> 对 `cloudbase_postgres` 角色**不可读**（会报 `42501 permission denied`），
> 用 `managePgDatabase action=listMigrations` 才是正路。

### 8.11 工具链一键自检

把历次踩过的坑（PATH、地域配置、缓存假阴性、直连通路、CI）固化成一个脚本：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File _dev_tools\cloudbase-check.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File _dev_tools\cloudbase-check.ps1 -Fix   # 自动修可修项
```

检查项与 2026-09-23 实测（**OK=11 FAIL=0 SKIP=2**）：

| 节 | 检查 | 实测 |
|---|---|---|
| 1 | node / npm / npx / Python / cargo / dev-env.ps1 | ✅ 6 项 |
| 2 | 用户级 `TCB_REGION` + 两处 `.cloudbase\project.json`（envId/region/site） | ✅ 3 项 —— **MCP 的 PG 工具能否工作的关键**（§8.0） |
| 3 | MCP 进程状态 + 最近日志是否仍有 `PG_NOT_READY` | ✅ 2 项 |
| 4 | PG 直连备用通路（可选：设 `TCB_PG_API_KEY`，或写 `<cwd>\.cloudbase\pg_api_key.txt`） | SKIP（未提供 Key） |
| 5 | CVM / CI 连通性（可选：设 `SONGZUO_CVM_HOST`；主机名不入公开库） | SKIP（未提供主机名） |

`-Fix` 只做两件安全的事：**重建缺失/错误的 `.cloudbase/project.json`**、**补写用户级 `TCB_REGION`**；
其余项只报告并给出修复提示（已实测：移走配置文件后 `-Fix` 能正确重建）。

> ⚠️ 该脚本必须存为 **UTF-8 with BOM** —— PowerShell 5.1 按 GBK 读 `.ps1`，中文注释无 BOM 会解析报错
> （同 §七 的 `dev-env.ps1`）。若编辑器另存导致 BOM 丢失，用下面一行补回：
>
> ```powershell
> $p='_dev_tools\cloudbase-check.ps1'; [IO.File]::WriteAllText($p, [IO.File]::ReadAllText($p), [Text.UTF8Encoding]::new($true))
> ```

> 📌 第 2 节改完**不必重启 IDE**，只需等 MCP 进程内的 manager 缓存刷新 —— 复验时别急着下"无效"结论（§8.0 假阴性教训）。
> 另：MCP 工具无法从该脚本调用（stdio 协议），第 3 节只能查进程/日志；要用真实查询复验请走
> `queryPgDatabase action=sql`（`SELECT 1`；或 `SELECT count(*) FROM dev_econ_monthly` → 240）。

### 8.12 浏览器 MCP（Playwright）接入

给 IDE 加一个浏览器自动化工具（页面验证 / 截图 / 取控制台日志），**走 MCP**：

- 服务端：`@playwright/mcp`（全局安装，本机 **0.0.82**）
- 浏览器：**复用本机 Edge**（`--browser msedge`）—— **不需要下载 Chromium**（省约 500MB）
- 关键点：**Windows 下不要让 IDE 直接执行 `.cmd`**（`child_process` 对 `.cmd` 支持不稳），
  所以 `command` 用 **`node.exe` 绝对路径**、把 `cli.js` 放进 `args`；这样**不依赖 PATH**
  （已实测：清空 PATH 后仍能启动）。

配置写入 `C:\Users\<用户名>\.codebuddy\mcp.json`（**该文件在仓库外，不入库**）：

```json
{
  "mcpServers": {
    "playwright": {
      "command": "D:\\codebuddy\\tools\\nodejs\\node-v20.19.5-win-x64\\node.exe",
      "args": [
        "D:\\codebuddy\\tools\\nodejs\\node-v20.19.5-win-x64\\node_modules\\@playwright\\mcp\\cli.js",
        "--browser", "msedge", "--isolated"
      ]
    }
  }
}
```

**不依赖 IDE 的验证方法**（本次实测通过）：把 MCP 的 `initialize` + `tools/list` 请求写进文件，
用 `Start-Process -RedirectStandardInput` 喂给服务端，则它会应答

```
{"result":{"protocolVersion":"2024-11-05","serverInfo":{"name":"Playwright",...}},"id":1}
{"result":{"tools":[{"name":"browser_close",...},{"name":"browser_resize",...},
                    {"name":"browser_console_messages",...},{"name":"browser_handle_dialog",...}, ...]},"id":2}
```

并在 stdin EOF 后正常退出 —— 说明**配置与入口都可用**，只等 IDE 加载。

> 📌 **生效需重启 IDE**（重启前工具不会出现在工具列表里）。
> `--isolated` 让浏览器 profile 只驻内存（不留残留目录、不碰你日常 Edge 配置）；
> 若想复用已登录的 Edge 站点，去掉该参数即可。
> 与 CloudBase **不冲突**：CloudBase 走 IDE 内置「集成」，不读 `mcp.json`，不存在重复实例问题。
> 自检脚本第 6 节会检查这项（§8.11）。

### 8.13 MCP 服务器清单（6 套，全部握手实测通过）

统一配置在 `C:\Users\<用户名>\.codebuddy\mcp.json`（**仓库外，不入库**）。
**每条都用 `node.exe` 绝对路径作 command** —— Windows 下不让 IDE 直接执行 `.cmd`，
且不依赖 PATH（理由见 §8.12）。

| 名称 | 用途 | 服务端版本 | 入口（Node 全局目录下） |
|---|---|---|---|
| `playwright` | 浏览器自动化（复用本机 Edge） | Playwright 1.64.0-alpha | `@playwright/mcp/cli.js` + `--browser msedge --isolated` |
| `codebase-memory` | 代码库索引/记忆（15 个工具） | 0.11.0 | `codebase-memory-mcp/bin.js` |
| `memory` | 跨会话知识图谱记忆 | 0.6.3 | `@modelcontextprotocol/server-memory/dist/index.js` |
| `sequential-thinking` | 复杂任务分步推理 | 2026.8.31 | `@modelcontextprotocol/server-sequential-thinking/dist/index.js` |
| `sqlite` | SQLite 查询（默认指向存档 `game/saves/slot_1.db`） | 1.0.0 | `mcp-sqlite/mcp-sqlite-server.js` + DB 路径 |
| `context7` | **库文档查询**（按版本查 API 用法，**无需 Key**） | 4.1.1 | `@upstash/context7-mcp/dist/index.js` |

**不依赖 IDE 的验证方法**（改动配置后都该跑一遍）：把 MCP 的 `initialize` + `tools/list`
请求写进文件，用 `Start-Process -RedirectStandardInput` 喂给每个服务端，检查 stdout 是否出现 `serverInfo`。
2026-09-23 实测：

```
[ OK ] playwright           -> Playwright v1.64.0-alpha-1789764292000   (26 工具)
[ OK ] codebase-memory      -> codebase-memory-mcp v0.11.0            (18 工具)
[ OK ] memory               -> memory-server v0.6.3                   (10 工具)
[ OK ] sequential-thinking  -> sequential-thinking-server v2026.8.31  ( 2 工具)
[ OK ] sqlite               -> mcp-sqlite-server v1.0.0               ( 9 工具)
[ OK ] context7             -> Context7 v4.1.1                        ( 3 工具)
```

> 📌 **生效需重启 IDE**。自检脚本第 6 节会逐个校验 command 与入口是否存在（§8.11）。

> ⚠️ **npm 装包一律加镜像**：本机 `npm config get registry` 是官方源 `registry.npmjs.org`，
> 在本链路**时通时断**（实测直接 `curl` 该域名 20 秒超时；装 `@upstash/context7-mcp` 首次即报
> `npm error network ... behind a proxy`）。处置：装包时加
> `--registry=https://registry.npmmirror.com`（实测 107 包 / 18 秒成功）。

> ⚠️ **`codebase-memory-mcp` 的安装坑（本链路必踩）**：它的 postinstall 要从 GitHub Releases
> 下载约 40MB 二进制，**直连 120 秒超时**（`install failed — Download hop timed out`）。处置：
>
> ```powershell
> npm install -g codebase-memory-mcp --ignore-scripts     # 先装包，跳过那步下载
> # 用镜像取二进制（gh-proxy.com 在本链路可用，实测 88s / 39.8MB）
> curl.exe -sSL -o $env:TEMP\cbm.zip `
>   https://gh-proxy.com/https://github.com/DeusData/codebase-memory-mcp/releases/download/v0.11.0/codebase-memory-mcp-windows-amd64.zip
> # 解到包内 bin/（install.js 期望 BIN_DIR=<pkg>/bin，Windows 二进制名 codebase-memory-mcp.exe）
> Expand-Archive $env:TEMP\cbm.zip -DestinationPath `
>   "<node 全局目录>\node_modules\codebase-memory-mcp\bin" -Force
> ```
>
> 验证：`node <pkg>\bin.js --version` → `codebase-memory-mcp 0.11.0`。

> 📎 另注：`@modelcontextprotocol/server-git` **不存在**（官方 git 服务是 Python/uvx 版），
> 故未纳入；本机已有 `tcb` CLI 与 git 命令行，`filesystem` 类服务与本环境既有文件工具重复，同未纳入。

### 8.14 项目文档接入本地检索（codebase-memory 索引）

**背景（实测）**：IDE 的 `RAG_search` **查不到本项目文档**。它连的是 8 个腾讯/微信官方知识库
（腾讯云API、微信云开发、腾讯云实时音视频、TDesign、微信支付、微信小程序、微信小游戏、腾讯地图小程序），
用项目专属词查询只会返回「压测大师计价表」「米大师虚拟支付」之类**完全无关**的内容，
且**没有任何配置能让它指向本地目录**。

**落地方式**：改用 `codebase-memory`（本地代码/文档图谱，已在 §8.13 的 MCP 清单中）索引本仓库。

```jsonc
// MCP: index_repository
{ "repo_path": "d:/codebuddy/sz", "mode": "moderate" }   // moderate = 过滤（skip vendor/生成物）+ 语义
```

实测：**6437 节点 / 23002 边**（24MB）；排除 `.cloudbase`/`.git`/`.pytest_cache`/`.ruff_cache`/`_scratch`
及 175 个图片类文件；`songzuo_server/Dockerfile` 有 1 处部分解析（21-58 行，best-effort）。

> ⚠️ **关键坑：`docs/` 目录默认被排除**。该工具内置跳过清单会把名为 `docs` 的目录当"非代码目录"跳过 ——
> 首次索引后 `_dev_tools/game-docs/docs`（含《货币口径规范_M0M1M2》《游戏机制说明》等**最关键的机制文档**）
> 根本没进图谱。修复是在仓库根加 **`.cbmignore`**（`!` 否定规则可覆盖内置清单，已实测）：
>
> ```
> # 让 codebase-memory 索引项目文档（默认跳过名为 docs 的目录）
> !_dev_tools/game-docs/docs/
> !_dev_tools/game-docs/docs/**
> ```
>
> 加后重索引：节点 **6091 → 6437**，`check_index_coverage` 对 `_dev_tools/game-docs/docs`
> 由 `"excluded"` 变为 `"no_recorded_issue"`。

**验收（实测）**：

```
search_code pattern="货币口径"  file_pattern="*.md"  → 9 个文件，含
    docs/货币口径规范_M0M1M2.md、docs/游戏机制说明.md、docs/财力消耗设计.md、docs/宋代官制与三冗设计.md …
search_code pattern="m1_working" file_pattern="*.md" → 命中 docs/货币口径规范_M0M1M2（1-642 行），
                                                       并给出 4 处匹配行号 [185,414,430,462]
```

即文档已作为**图节点**入库（带行号定位），既可按内容检索、也可按结构查询，强于纯文本 grep。

> 📌 工具自带**图谱可视化 UI**：`ui_enabled=true`、`ui_port=9749` → http://localhost:9749

> 🔁 **重建索引**：改完文档后可重跑 `index_repository`（增量处理）。索引存放在工具缓存目录
> `~/.cache/codebase-memory-mcp/`（`D-codebuddy-sz.db`），**不在仓库内**。

---

## 附：首次接入时的实测记录（2026-09-23）

- 免密登录验证通过；`ssh -i ... root@<IP> whoami` → `root`
- 全量回归首跑：`634 passed / 57 failed / 1 skipped`
  （失败根因是 `_settle_external_economy` 的两处未完成草稿：`agg_res` 未定义、
  `_res_after_prov` 中 `rd` 未绑定），修复后 → `888 passed / 3 failed / 1 skipped`
- 修复提交：`62ea799106`、`e8608944d2`
- 后续收口：Phase F 草稿按设计文档 0.1 节方案 α 删除（`93aa621873`）；修两处平台脆弱用例
  并把六阶 POP 改为最大余数法（`e815d9384a`）→ **两侧全绿**（服务器 `891 passed / 1 skipped`，
  本机 892 例全通过）
- 构建链实测（2026-09-23 下午）：
  - `cargo build --release` → **1m07s / 3.0MB 二进制**；`cargo test --release` → **3 passed**
  - Rust 服务在 Linux 上跑通：`/healthz` 200、`/api/new_game` 返回完整开局状态；
    仅设 `PORT` 时绑定 `0.0.0.0`（云托管就绪性成立）
  - 前端：`npm install` → **733 包 / 31s / 741MB**；`npm run build` → **11.11s**，
    产物 `game/frontend/out/`（126MB）
- 容器化实测（2026-09-23 下午）：Docker 29.7.0 + 腾讯云内网镜像源；
  `songzuo_server:0.1.0` 镜像构建成功（134MB / 内容 33.3MB）并跑通健康检查与开局接口。
  过程中确认了两条部署硬约束：
  1. 容器内**必须设 `SONGZUO_SERVER_TOKEN`** —— Docker NAT 使容器看到的源 IP 是网桥
     网关而非回环，不设 token 时 `/api/*` 会全部 401（`/healthz` 不受影响，容易误判为"服务正常"）；
  2. **不可设 `SONGZUO_ADDR`** —— 它优先级高于 `PORT`，会顶掉平台注入的端口，
     导致 readiness probe 失败。
- CloudBase 接入复测（2026-09-23 晚；**定位：开发辅助工具，不接游戏运行时**）：
  - 官方接入文档 §0 的唯一缺口已修：**Node 目录写入用户 PATH**
    （此前 `node`/`npm`/`npx` 在本机全部不可见）
  - 接入路径实测：`npx plugins targets` **不含 CodeBuddy** → 按文档 §3.C 走**内置连接器**，
    **不装插件、不写 `mcp.json`**（详见 §8.6）
  - `auth` → `READY`（账号级，region ap-singapore）；当前环境 `william-1-d5grtya3z0b5e143d`
  - 通路实测：控制面 / 云函数（`listFunctions`）/ 静态托管（`listFiles`）均通（各 0 条）
  - ⚠️ PG 的判定反复过两次，最终结论（已用直连端点证实，见 §8.7）：
    **PG 本身完全正常** —— PostgreSQL 17.11，库名 `postgres-gmd3fp37`，
    建表 / 插入 / 查询 / 删除全部 http=200；**是 MCP 的 PG 工具在该环境解析不到实例**
    （`PG_NOT_READY / PG instance not found`；而管理面 `listMigrations` 反而通）。
    两次误判的教训：① `action=context` 只推导环境元数据、不访问实例，**不能**用来判定 PG 可用性；
    ② **不能因为 MCP 工具报错就断定云服务不可用** —— 必须换独立通路（HTTP 直连）验证。
  - 直连通路已打通并留档：`POST /v1/rdb/exec-pgsql` + 管理员 API Key（详见 §8.7）；
    环境内已签发 `sz-dev-pg-http`（到期 2026-10-23）
  - `queryCloudRun envStatus` → `region ap-singapore not in config support region`
    → **云托管在该地域不可用**（是地域不支持，不是"未开通"）
  - 旧环境 `william-1-d6genf4wb9988a74a` 及其中部署的 `songzuo-server` 均已不存在
    （`env not found` / 服务列表 0 / 公网 404）
